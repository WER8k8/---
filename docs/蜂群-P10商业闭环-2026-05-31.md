# 蜂群 P10 商业闭环 — 2026-05-31

> 承接 P9：审计操作人/目标筛选、探针 QR 展示、ngrok 公网可达性。

## 范围（W33–W35）

| ID | 交付 | 边界 |
|----|------|------|
| W33 | 审计按操作人/改派目标筛选 | `actor_user_id` / `to_assignee_id`；列表 + CSV 同源 |
| W34 | 探针 QR inline | 支付运维页展示支付宝/微信 `code_url` 二维码 |
| W35 | ngrok 公网探测 | `discover_ngrok_public_url` + `GET /ops/staging/public-reachability` |

## 后端

### W33

- `query_assignment_audit_rows(..., actor_user_id=, to_assignee_id=)`
- API：`/assignment-audit` 与 `/assignment-audit/export` 增加 query 参数

### W34

- 前端 `payment-ops.vue`：`qrImg()` + `alipayCodeUrl` / `wxCodeUrl`

### W35

- `discover_ngrok_public_url()` — 读 `127.0.0.1:4040/api/tunnels`
- `check_public_health()` / `run_public_reachability_check()`
- `GET /api/v1/payment/ops/staging/public-reachability?public_url=`
- `staging_notify_http_check.py --discover-ngrok`

## 前端

| 页面 | 变更 |
|------|------|
| `/admin/inquiries` | 审计 Drawer：操作人 / 改派目标下拉 |
| `/admin/finance/payment-ops` | 探针 QR 图 +「公网/ngrok 探测」 |

## 测试

```powershell
python -m pytest tests/unit/test_commercial_loop_p10.py -q
python scripts/staging_notify_http_check.py --discover-ngrok --base-url http://127.0.0.1:8001
```

## 后续（P11）

- 公网 notify 经 ngrok URL 端到端 POST（非仅 health）
- 审计：操作人显示名 join User 表
- 探针历史记录落库（ops_audit 表）
