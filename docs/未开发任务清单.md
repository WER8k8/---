# 未开发任务清单（全仓扫描）

> **扫描日期**：2026-05-24（登录/OAuth 对接见 [`管理端统一登录与OAuth对接.md`](管理端统一登录与OAuth对接.md)，2026-05-24 已闭环）  
> **扫描范围**：`backend/`（FastAPI）、`frontend/`（Nuxt 租户站）、`frontend/admin/`（管理端）、`seo-backend/`（Node 矩阵）、`seo-admin/`、`docs/` 产品文档与 `C:\Users\97907\Desktop\汇总\开发文档\`  
> **权威产品定义**：`docs/产品俯视-租户旅程与三轨计费.md`、`docs/产品对话总览-定稿.md`  
> **图例**：✅ 可用 · 🟡 有代码/页面但未闭环 · ❌ 缺失或未接线 · 🔌 已实现未挂载路由

---

## 一、扫描结论（Executive Summary）

| 维度 | 数据 | 说明 |
|------|------|------|
| 已挂载 FastAPI 路由 | **331** 条 | `python` 启动 `app.main` 统计 |
| 超管 API（代码有、运行无） | **138** 条 | `app/api/v1/super_admin/` 未 `include_router` |
| 高级 SEO API（代码有、运行无） | **72** 条 | `app/api/v1/seo/` 未挂到主路由 |
| 呼朋唤友 API | **6** 条 | 🔌 `referral_router` 已 import 未挂载 |
| 管理端 Vue 页面 | **~195** | 大量页面依赖未挂载的 `/super-admin/*` |
| 规划实体（全仓无代码） | `content_master`、`egress_endpoint`、`browser_profile`、枢纽页、财务四维台账 | 产品文档已写，仓库无实现 |
| 双后端 | FastAPI + `seo-backend` | 发布/矩阵能力重复，需统一或明确主从 |

**核心判断**：仓库 **代码量多、卖货主链未产品化**——最大缺口是 **路由接线**、**统一发布母版**、**IP/指纹执行面**、**总站枢纽**、**财务中台**，而非从零写业务模块。

---

## 二、P0 — 不接线就无法演示/售卖

| ID | 任务 | 现状证据 | 验收标准 |
|----|------|----------|----------|
| P0-01 | **挂载 `referral_router`** | `routes/__init__.py` L34 import，无 `include_router` | `GET /api/v1/referral/leaderboard` 返回 200 |
| P0-02 | **挂载 `super_admin` 整包** | `super_admin/__init__.py` 19 子模块；管理端 20+ 文件调 `/api/v1/super-admin/*` | `GET /api/v1/super-admin/dashboard` 等可访问 |
| P0-03 | **挂载 `app/api/v1/seo/*` 高级 SEO** | 运行中仅有 `routes/seo.py` 5 个 stub；`site-audit`/`eeat` 等为 False | 管理端 SEO 子页与 `docs/4-API接口定义.md` 对齐 |
| P0-04 | **替换/下线 stub SEO** | `routes/seo.py` 返回硬编码 `total_keywords:150`、空 `items` | 仪表盘读真实库或明确标注 demo |
| P0-05 | **`content_master` 数据模型 + CRUD** | 全仓 grep 无匹配 | 一篇母版关联租户、canonical、素材 |
| P0-06 | **统一发布台 MVP** | 分散在 `seo-matrix/publish`、`publish_service`；无母版入口 | 选母版 → 生成变体 → 创建 `publish_task` |
| P0-07 | **平台主数据（40 清单）** | `platform` 有适配器，无 cn/global 20+20 定稿表 | `platforms.region` + 套餐 SKU 可勾选 |
| P0-08 | **`egress_endpoint` + `browser_profile` 表** | 无模型 | 槽位购买、绑定 `platform_account` |
| P0-09 | **发布 Worker 骨架** | 无独立 worker 进程文档化 | 队列消费 `publish_task`，region=cn/global |
| P0-10 | **租户独立域 Host 解析** | `domain.py` 仅 CRUD `custom_domains` JSON | Nuxt/网关按 Host 解析租户 + 404 隔离 |
| P0-11 | **独立域 SSL 自动签发** | `docs/saas/dns-setup-guide.md` 有说明，自动化未验 | 绑定域后 HTTPS 可用 |
| P0-12 | **物流轨迹 API** | 前台 `logistics/index.vue` 调 `GET /logistics/track`；后端无此路由 | 运单查询返回轨迹或对接第三方 |
| P0-13 | **物流前台 API 基址** | 硬编码 `http://localhost:8000` | 使用环境变量 + 与 Nuxt 代理一致 |

---

## 三、P1 — 商业闭环与文档已定义能力

| ID | 任务 | 来源 | 现状 |
|----|------|------|------|
| P1-01 | **总站权重枢纽** | 产品俯视 §6 | ❌ 无 `/industry/{slug}/{id}` 路由与 CMS |
| P1-02 | **发布双链策略** | 主链租户域 + 次链枢纽 | ❌ `publish_task.primary_url/secondary_url` 未建 |
| P1-03 | **枢纽 sitemap / GSC 推送** | 产品俯视 §6.7 | ❌ |
| P1-04 | **财务中台：营收台账** | `汇总/开发文档/财务模块等.md` | 🟡 仅 `payment`/`tenants` 片段 |
| P1-05 | **财务中台：成本台账** | 同上（IP 采购、模型 API） | ❌ |
| P1-06 | **财务中台：代理分润结算** | 同上 + `agent/commission.vue` | 🟡 `agent-tree` 有统计无结算单 |
| P1-07 | **财务中台：利润与分层看板** | 超管/省代/租户 | ❌ |
| P1-08 | **Excel 对账导出** | 财务文档 | ❌ |
| P1-09 | **Token 账本与余额不足停服** | 三轨计费 | 🟡 `ai_usage` 有监控无强制停服 |
| P1-10 | **付费 → 自动开户 → 配额/IP 槽位** | 产品俯视 P1 | 🟡 注册/套餐有，Provisioning 未闭环 |
| P1-11 | **到期冻结租户** | SaaS 运营 | 🟡 需与订阅状态联动 |
| P1-12 | **呼朋唤友 ↔ 计费打通** | L1/L3/L5 奖励 | ❌ `referral_service` 未接 `payment` |
| P1-13 | **合并 inquiries v1/v2** | 双路由 `/inquiries` + `/inquiries-v2` | 🟡 数据与前端入口分裂 |
| P1-14 | **废弃租户侧 v2ray 产品化** | 产品决策 | 🟡 仍有 `admin/v2ray/*`、`super_admin/v2ray_tracker`；改静态 IP 管理 |
| P1-15 | **GEO 引擎与超管 GEO 页面对齐** | `geo-engine`、`super_admin/geo*` | 🔌 依赖 P0-02 挂载 |

---

## 四、P2 — 差异化与合规

| ID | 任务 | 说明 |
|----|------|------|
| P2-01 | **12 语种 + IP 动态 IM** | `汇总/开发文档/谷歌排名机制_*.md`；`im-routing` 有配置，全链路未验 |
| P2-02 | **租户 RAG（建材 TDS、禁乱报价）** | `building-wiki`、`knowledge` 有；销售话术约束未产品化 |
| P2-03 | **物流 ↔ 订单联动** | `orders` 有 `tracking_number` 字段级展示；无回填轨迹 |
| P2-04 | **竞品分析（动态）** | 评估报告：静态对比表 |
| P2-05 | **SEO 报告 PDF/定时导出** | 文档路线图有，未实现 |
| P2-06 | **出海计 App（必做）** | 仅有 mobile H5 + 片段 API；见 `docs/出海计-App产品规格.md`（APP Sprint） |
| P2-07 | **用户行为分析与个性化推荐** | `ai-recommendations` 路由有，业务浅 |

---

## 五、P3 — 增值包（明确非主叙事）

| ID | 任务 | 说明 |
|----|------|------|
| P3-01 | **海外社媒养号周期** | `财务模块等.md` 养号状态机；全仓无 `nurture` 模型 |
| P3-02 | **排行榜人工发奖核销** | 呼朋唤友方案：京东卡线下 + 财务核销 |
| P3-03 | **实验室模块默认折叠** | v2ray、CDN、认知智能、多媒体工厂等与建材卖货弱相关 |

---

## 六、按产品模块展开（详细）

### 6.1 租户卖货七步闭环

| 步骤 | 产品要求 | 代码/页面 | 缺口 |
|------|----------|-----------|------|
| 1 开通 SaaS | 套餐、租户、白标 | `tenants/*`、`payment` | 付费自动开通、冻结 |
| 2 独立域 | www.xxxx.com | `domain.py`、`tenants/domain.vue` | Host/SSL/前台渲染 |
| 3 一篇母版 | content_master | — | **P0-05～06** |
| 4 40 平台分发 | SEO+GEO+队列 | `seo_matrix`、`publish_service`、`seo-backend` | 清单、Worker、双栈统一 |
| 5 询盘 | 表单+IM | `inquiries`、`inquiries-v2`、StickyImBar | 合并入口 |
| 6 订单 | quotes/orders | 已实现 | 与物流、支付闭环 |
| 7 物流查询 | 租户站可查 | `pages/logistics` | **无 `/track` API** |

### 6.2 三轨收费

| 轨道 | 应有能力 | 现状 |
|------|----------|------|
| SaaS | 套餐、订阅、发票 | 🟡 `tenants/plans`、`billing.vue` |
| Token | 扣费、余额、停服 | 🟡 `ai_usage`、`ai-config`；无 ledger 表 |
| 静态 IP+指纹 | 槽位、绑定账号 | ❌ 无表；v2ray 菜单误导 |

### 6.3 呼朋唤友 + 小计划

| 项 | 路径 | 状态 |
|----|------|------|
| 模型/服务 | `models/referral.py`、`services/referral_service.py` | ✅ |
| API | `routes/referral.py` | 🔌 **未挂载** |
| 管理端 | `views/referral/*` | ✅ UI |
| 注册带码 | `tenants/register?ref=` | ✅ |
| 有效邀请判定（认证+首付费） | 方案文档 | ❌ 未接支付 webhook |
| 奖励发放 | 方案文档 | ❌ |

### 6.4 总站权重枢纽

| 项 | 状态 |
|----|------|
| Nuxt 路由 `/industry/...` | ❌ |
| `hub_summary`、`show_on_hub` 字段 | ❌ |
| 发布文案 UTM 双链 | ❌ |
| 租户站不展示总站友链 | ✅ 策略已定，需代码 enforce |
| 超管枢纽收录看板 | ❌ |

### 6.5 全链路财务（外部文档）

对照 `C:\Users\97907\Desktop\汇总\开发文档\财务模块等.md`：

| 能力 | 代码 |
|------|------|
| 套餐/订单收入汇总 | 🟡 |
| IP/模型/公摊成本录入 | ❌ |
| 省代多级分润与结算 | 🟡 |
| 利润 = 营收 - 成本 - 分润 | ❌ |
| 租户/代理/超管分层报表 | 🟡 |
| 按需购买 IP 与财务联动 | ❌ |

### 6.6 SEO / GEO

| 能力 | FastAPI | seo-backend (Node) | 管理端 |
|------|---------|-------------------|--------|
| SEO 矩阵 | ✅ `/seo-matrix` | ✅ 重叠 | ✅ |
| 高级 SEO（审计/EEAT/Schema） | 🔌 `api/v1/seo/*` | 部分 | ✅ 页面在，API 404 |
| 简易 SEO dashboard | 🟡 stub | — | 可能显示假数据 |
| GEO 引擎 | 🔌 super_admin/geo-engine | — | `admin/geo-engine` |
| llms.txt 生成 | 高级包有 | — | `seo/llms-txt.vue` |

### 6.7 超管后台（套件）

`docs/admin-route-task-board.json` 67 项任务中，大量 **超管套件**（code-tools、file-manager、automation、scheduler）标记 **无 API**，属演示壳：

| 路由前缀 | API | 判定 |
|----------|-----|------|
| `/admin/code-tools/*` | 无 | 🟡 演示/待砍或接 AI |
| `/admin/file-manager/*` | 无 | 🟡 |
| `/admin/automation/*` | 无 | 🟡 |
| `/admin/ai-engine/*` | 应走 `/super-admin` | 🔌 P0-02 |
| `/admin/geo-engine` | `/super-admin/geo-engine` | 🔌 P0-02 |

### 6.8 实验室 / 非核心（评估报告建议折叠）

| 模块 | 后端前缀 | 建议 |
|------|----------|------|
| v2ray | `super_admin/v2ray_tracker` | 下架或仅内部运维 |
| edge-cdn | `/edge-cdn` | P3 折叠 |
| cognitive | `/cognitive` + super-admin RAG | P3 |
| media-factory | `/media-factory` | 可并入统一发布 |
| developer 生态 | `/developer` | P3 |
| international 采集 | `/international` | 与卖货链评估后保留 |
| system-health 压测 | `/system-health` | 运维工具，非租户功能 |
| ai-learning | `/ai-learning` | 已挂载，非 MVP |

---

## 七、前后端不一致清单（易踩坑）

| 现象 | 位置 | 应做 |
|------|------|------|
| 管理端调 `super-admin`，运行 404 | 如 `dashboard/index.vue`、`ai-center/models.vue` | P0-02 |
| SEO 页调 `/seo/site-audit` 等 | `views/seo/*.vue` | P0-03 |
| 呼朋唤友页调 `/referral/*` | `views/referral/*` | P0-01 |
| 租户站物流调 `/logistics/track` | `pages/logistics/index.vue` | P0-12 |
| 物流 API 写死 localhost:8000 | 同上 | P0-13 |
| `routes/seo.py` 与 `api/v1/seo/` 双实现 | 后端 | 合并路由，删除 stub |

---

## 八、技术债与架构

| 项 | 说明 | 建议任务 |
|----|------|----------|
| **双后端** | FastAPI（主）+ `seo-backend` MySQL 矩阵 | 定主库；矩阵 API 迁 PG 或网关聚合 |
| **双管理端** | `frontend/admin` + `seo-admin` | 统一入口或标注废弃 |
| **AI Mock 回退** | `ai_engine.py` 无 key 时 MockLLM | 生产环境禁止 silent mock |
| **SQLite vs PostgreSQL** | JSONB 语法差异 | 生产强制 PG（见评估报告） |
| **smart_agent_workflow exe** | 6 进程无守护 | 纳入 systemd/k8s 或移除 |
| **inquiries 双版本** | v1 routes + v2 package | 数据迁移 + 单路由 |
| **文档声称完成 vs 代码** | 多份 Phase 验收/完成报告 | 以本清单 + 路由探测为准 |

---

## 九、已有代码索引（便于排期）

| 功能 | 路径 |
|------|------|
| 路由汇总 | `backend/app/api/v1/routes/__init__.py` |
| 超管（未挂） | `backend/app/api/v1/super_admin/` |
| 高级 SEO（未挂） | `backend/app/api/v1/seo/` |
| 呼朋唤友 | `backend/app/api/v1/routes/referral.py` |
| 域名绑定 | `backend/app/api/v1/routes/domain.py` |
| 发布服务 | `backend/app/services/publish_service.py` |
| SEO 矩阵 | `backend/app/api/v1/routes/seo_matrix.py` |
| Node 矩阵 | `seo-backend/src/services/publishService.js` |
| 租户站物流 UI | `frontend/pages/logistics/index.vue` |
| 管理端任务板 | `docs/admin-route-task-board.json` |
| 产品俯视 | `docs/产品俯视-租户旅程与三轨计费.md` |
| 呼朋唤友方案 | `docs/呼朋唤友-客户裂变营销方案.md` |
| 架构评估 | `docs/evaluation/2026-05-20-product-architecture-assessment.md` |

---

## 十、建议迭代顺序（给排期）

```text
Sprint A（1～2 周）：P0-01～04 路由接线 + P0-12 物流 track
Sprint B（2～3 周）：P0-05～11 母版/发布/IP/独立域
Sprint C（2 周）：P1-01～03 总站枢纽 + P1-04～11 财务与计费
Sprint D：P2/P3 按销售合同开启
```

---

## 十一、修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-24 | 首版：全仓路由探测 + 产品文档对照 + 前后端不一致表 |
| 2026-05-24 | 管理端登录/OAuth 已对接并写入专项文档（E-01～E-04）；邮箱验证码登录 UI 仍待做 |
