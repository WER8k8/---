# 蜂群冲刺 · P0 商业闭环（2026-05-31）

> **指挥模式**：M3 蜂群 · 五包并行 · 合并后 pytest  
> **目标**：修复 PM 审查 P0 断链，不碰 `routes/__init__.py` 大规模改动

## 依赖图

```text
[W1-CHURN]     churn.py + churn_service 持久化
[W2-REFERRAL]  TenantRegisterSchema + register_tenant
[W3-BILLING]   /tenants/invoices* + tenant_service
[W4-TOKEN]     token.vue + token_ledger 去重
[W5-FINANCE]   /admin/finance/commissions
[MERGE]        pytest tests/unit/test_*commercial* test_churn* test_invoice*
```

## 工作包验收

| 包 | 路径 | 验收 |
|----|------|------|
| W1 | `routes/churn.py`, `services/churn_service.py` | `/churn/at-risk` 200；mark-contacted 持久化 |
| W2 | `schemas/tenant.py`, `routes/tenants.py` | 注册带 referral_code 写入 ReferralRecord |
| W3 | `routes/tenants.py`, `tenant_service.py`, `billing.vue` | GET/POST/PUT `/tenants/invoices*` |
| W4 | `token.vue`, `token_ledger.py` | `/token/quota-status`, `/token/ledger?tenant_id=` |
| W5 | `finance/commissions.vue` | POST `/finance/commissions/{id}/settle` |

## 合并后必跑

```powershell
cd backend
python -m pytest tests/unit/test_commercial_loop_p0.py tests/unit/test_invoice_application_service.py tests/unit/test_churn*.py -q
```

## 仍留 P1（本冲刺不做）

- 真实微信/支付宝 PSP
- Token 付费 SKU
- 到期 cron / 自动续费
- 全电票 API
