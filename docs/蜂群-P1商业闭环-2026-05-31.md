# 蜂群冲刺 · P1 商业闭环（2026-05-31）

## 工作包

| 包 | 交付 |
|----|------|
| W6-TOKEN | `order_addon_service` + `/payment/addon/token-pack` + `/client/tokens` |
| W7-RULES | `PUT /finance/commission-rules/{id}` + `/admin/finance/commission-rules` |
| W8-LIFE | `scripts/run_tenant_lifecycle.py` + `/finance/expiring-tenants` |
| W9-GUARD | 生产环境 `mock-pay` 门禁（`PAYMENT_ALLOW_MOCK`） |

## 业务规则

- Token 加购 subject：`[addon:token:{数量}]`，支付后幂等充值
- 加购订单 **不计代理分润**，财务 category=`token_pack`
- 分润规则首单合计 ≤3000bp，续费 ≤1000bp

## Cron

```powershell
# 每日 02:00
python backend/scripts/run_tenant_lifecycle.py
```

## 验证

```powershell
cd backend
python -m pytest tests/unit/test_commercial_loop_p1.py tests/unit/test_commercial_loop_p0.py -q
```
