# 架构重设计 v2 启用说明（T-ARCH-1）

**权威**：`docs/adr/架构设计-2026-目标架构与性能.md`

## 启用门槛（硬门禁）

在 **T-G3 / T-G4 / T-G5** 全部 PM 签字前，仅允许：

- BFF 聚合接口（如 `GET /api/v1/bff/client/bootstrap`）
- 文档与对照清单
- 单测 / 冒烟

**禁止**在未过门槛时切换生产流量至新 BFF 网关或拆除旧路径。

## 已落地（研发）

| 项 | 路径 |
|----|------|
| 租户首屏 BFF | `backend/app/api/v1/routes/client_bff.py` + `services/client_bff_service.py` |
| Platform 首屏 BFF | `GET /api/v1/bff/platform/overview` |
| Agent 首屏 BFF | `GET /api/v1/bff/agent/summary` |
| BFF 缓存 | `backend/app/services/bff_cache_service.py`（Redis + 内存兜底） |
| 管理端 lazy chunk | `frontend/admin/vite.config.ts` → `copilot` |
| Client 壳 bootstrap | `useTenantBrand.ts` → `/bff/client/bootstrap` |

## PM 签字后

按 ADR 第七章执行 seo-backend Phase 0→下线与 Platform BFF 扩展。
