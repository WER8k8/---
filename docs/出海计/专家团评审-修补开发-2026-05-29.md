# 专家团评审 · 修补开发（2026-05-29）

> 对应 [`专家团评审-第三轮-2026-05-29.md`](./专家团评审-第三轮-2026-05-29.md) 行动项

## 已落地（研发）

| # | 行动 | 实现 |
|---|------|------|
| P0 #2 | starlette CVE | `requirements.txt`：`fastapi>=0.118` + `starlette>=1.0.1,<1.1` |
| P0 #2 | bcrypt/passlib | `app/core/security.py` 兼容补丁 + `bcrypt>=4.0.1,<4.1` |
| P0 #1 | 预发 Postgres preflight | 新增 `scripts/run-staging-preflight.ps1` |
| P0 #3 | Celery 真跑 | `docker-compose.dev.yml` 增加 `celery-worker` / `celery-beat`（profile ops） |
| P2 #7 | sprint preflight 误调 | `sprint-final-close.py` 改调 `run_production_preflight.py --production` |
| P2 #6 | npm audit | `npm-audit-admin.ps1` 优先 `registry.npmjs.org` |
| — | 登录白屏 | `frontend/admin/src/api/response.ts` 补 `unwrapFetchedJson` |
| — | 本地开发 | `scripts/start-dev-admin.ps1`；Vite 代理改 `127.0.0.1:8001` |

## 一键命令

```powershell
# 本地管理端 + 后端
powershell -ExecutionPolicy Bypass -File scripts/start-dev-admin.ps1

# 预发栈 + 生产形态 preflight（需 Docker）
powershell -ExecutionPolicy Bypass -File scripts/run-staging-preflight.ps1

# Celery 常驻（需 Docker profile ops）
docker compose -f docker-compose.dev.yml --profile ops up -d celery-worker celery-beat
```

## 仍须人工 / 运维

| 项 | Owner |
|----|-------|
| PM L3 实机签字 | 产品 |
| 真实 AI Key + `MVP_LAUNCH=0`（G4 商用） | 研发+运营 |
| HTTPS 演示域 | 运营 |
| 生产 `.env.prod` 强密钥与 pg_dump 备份 cron | 运营 |

## 触发第四轮评审

生产 preflight `--production` 0 fail + G4 绿 + PM L3 签字 + Celery 日志归档。
