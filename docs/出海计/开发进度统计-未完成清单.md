# 开发进度统计 · 未完成清单

> **统计日**：2026-05-25（BUG 汇总见 [开发进度与BUG汇总-同步版.md](./开发进度与BUG汇总-同步版.md)）  
> **排序铁律**：① 开发 / 验真 → ③ 测试 → ④ 上线部署 → **② 演讲 / 录屏 / 彩排（最后）**  
> **主线**：字节 DeerFlow + 阿里 AccioWork 对标 · 商业飞轮 OS

---

## 一、总览（一眼看懂）

| 维度 | 已完成 | 未完成 | 完成率 |
|------|--------|--------|--------|
| **夜间队列 N1–N5** | 5 | 0 | **100%** |
| **飞轮 P0 代码（D7 单测）** | D7 + 全仓 unit **237** | 6 项需环境/人工 | **单测 100%** · 环境 **~14%** |
| **Accio 技能对标（20 项）** | 14 全实现 + 2 部分 | 4 gap（501 已占位） | **75%**（含 partial 折半） |
| **阶段 ② 演讲/录屏** | 0 | 全部后置 | **刻意不做** |
| **阶段 ③④ 测试/部署** | **unit 237/237** | E2E、生产迁移、HTTPS | 单测过关；环境待验 |

```text
当前应做：飞轮 D1–D6 环境验收 + 验真 T1–T4 + Accio Phase A 缺口
明确不做（直到①③④过关）：七步录屏、30 分钟彩排、对外 SKU 实机演讲
```

---

## 二、商业飞轮 OS · P0 验收（① 开发）

| ID | 任务 | 代码/单测 | 环境/人工 | 状态 |
|----|------|-----------|-----------|------|
| D1 | Alembic `025`–`028` 生产库 `upgrade head` | 含 `028_ubrain_action_audit` | `scripts/check-flywheel-migration.ps1` | ⬜ 生产执行 |
| D2 | 副驾三根支柱 `/commercial-os/status` | ✅ 服务层 + 路由直调单测 | 租户登录实点 | ⬜ UI 未确认 |
| D3 | Research Brief `accio_actions` ≥2 | ✅ `test_flywheel_d2_d6_api` | 副驾点「生成简报」 | ⬜ UI 未确认 |
| D4 | 异步 `market_research` + `commercial_os_hook` | ✅ 入队单测 | 副驾「跑飞轮」实机 | ⬜ UI 未确认 |
| D5 | `UBRAIN_AUTO_PIPELINE=1` 编排入队 | ✅ integrations/sidecar 单测 | 看 `pipelines` 有 job_ids | ⬜ 未确认 |
| D6 | `feedback/sync` → `research_hints` | ✅ `collect_sales_feedback` 单测 | 副驾「同步反馈」后支柱更新 | ⬜ UI 未确认 |
| D7 | 单元测试 | **`tests/unit` 237 passed**（含 auth/products/飞轮/AI 引擎） | `e2e-flywheel-validation.ps1` | ✅ 全绿 |

**飞轮 P0 代码完成度（估）**：实现 **~95%** · 环境验收 **0/6 确认**

### 已交付 API（飞轮相关）

| 能力 | 路径 |
|------|------|
| 状态 / 支柱 | `GET .../commercial-os/status` |
| 编排列表 | `GET .../commercial-os/pipelines` |
| 洞察 / Brief / 反馈 / webhook | insights、research-brief、feedback/sync、webhook |
| Gap 占位 | `GET .../gaps`、`GET .../gap/{id}` → 501 |

---

## 三、Accio 对标目录（20 技能）

| 状态 | 数量 | 技能 ID |
|------|------|---------|
| ✅ 已实现 | 14 | market_research、find_buyers、blue_ocean、export_feasibility、lead_content_pack、geo_submit_pack、outreach_letter_pack、inquiry_reply_draft、negotiation_draft、inquiry_score、logistics_track、weekly_lead_report、sync_feedback、commercial_os_pipeline |
| 🟡 部分 | 2 | matrix_publish、team_rbac |
| ⬜ gap（501 已说明，不删条目） | 4 | image_sourcing、auto_shopify、paid_ads_creative、supplier_rfq |

**覆盖率**：`catalog_summary()` → **75%**（14 + 2×0.5 / 20）

---

## 四、Accio Phase A（工作台形态 · ① 内）

| 项 | 说明 | 状态 |
|----|------|------|
| A1 | 商业飞轮 OS 副驾单页 | ✅ 已改名+飞轮 UI |
| A2 | UBrain + **真实询盘列表** 上下文草稿 | ✅ 代码（`inquiry_context_service` + 话术「回复最新询盘」） |
| A3 | 询盘意向评分（高/中/低） | ✅ 代码（自动读最新询盘） |
| A4 | 经营快照（未回询盘/发布失败/SSL） | ✅ 快照含发布失败/SSL 统计 + 副驾 hints |
| A5 | 对外动作审计日志（除 confirm 外全链路） | ✅ `ubrain_action_audits` + `/action-audit` + 副驾列表 |

---

## 五、验真技术 P0（`接下来怎么做` · 先于演讲）

| 序号 | 项 | 状态 |
|------|-----|------|
| T1 | HTTPS 演示独立域 | ⬜ |
| T2 | 询盘手机号必填 | ✅ API + 单测 + `StickyImBar` 前端校验；⬜ 真机 H5 点一次 |
| T3 | `source_channel` 全链路 | ✅ 列表/导出筛选 + CSV 列 |
| T4 | 线索周报 CSV 导出 | ✅ `GET /inquiries/export` 含 source_channel |
| T5 | 发布 Worker 生产 cron 或人工策略书面化 | ✅ `发布Worker-生产策略.md` + `ops-publish-worker-cron.ps1` |
| T6 | UBrain 接真实询盘列表草稿 | ✅ A2 已接最新询盘；⬜ 接 LLM 生成 |

---

## 六、产品全景 `module-progress.json`（与飞轮并行）

| 模块 | 完成度 | 主要 gap（非演讲） |
|------|--------|-------------------|
| 七步② 独立域 | 9/10 | 实机 DNS + 真证书 |
| 七步⑤ 询盘 IM | 9/10 | 企微/抖音生产密钥联调 |
| 七步④ 40 平台 | 10/10 | PM 正式表签字 |
| **ops 运维** | 9/10 | ~~七步实机录屏~~ → **后置到 ②** |
| **pm-qa** | 5/10 | 5+5 平台、HTTPS 演示域 |
| UBrain-X | 10/10 | 代码层标满；验真 T6 仍待做 |
| 出海计 App | 9/10 | 商店上架实机包 |

**PM 阻塞项（7）**：演示 HTTPS 域、40 平台表、套餐金额、矩阵签字、免责声明法务等 — 见 `docs/module-progress.json` → `pm_blockers`。

---

## 七、P2 外挂（① 验收后再做）

| 项 | 状态 |
|----|------|
| n8n 自托管常驻 | ✅ 回调 API + `n8n-自托管与飞轮回调.md` + 示例 JSON |
| Mem0 双写 | ✅ 洞察落库后 HTTP 双写（`MEM0_API_URL`） |
| PostHog 埋点 | ✅ 反馈/任务成功事件（`POSTHOG_*`） |
| 官方 DeerFlow 旁路部署 | ✅ `DEERFLOW_SIDECAR_URL` + 文档 + 回退 Lite |
| GraphRAG / Browser Use / Ragas | GraphRAG 占位文档 + `integrations.graphrag` 状态位 ✅ |

---

## 八、明确后置 · 演讲 / 录屏 / 彩排（② 最后）

以下 **不计入当前开发完成率**，全部排在 ①③④ 之后：

| 项 | 参考 |
|----|------|
| 七步实机录屏 | `module-progress` ops gap → **后置** |
| 30 分钟彩排脚本 + 录屏 ≤1 卡顿 | `接下来怎么做` P1 |
| 对外 SKU 实机话术彩排 | `DeerFlow-Accio-本系统-验真话术与30天指标.md` |
| 飞轮副驾演示路径彩排 | `商业飞轮OS-开发总文档` §8.1 |

---

## 九、建议下一迭代（蜂群可拆 3 包）

| 优先级 | 包 | 内容 |
|--------|-----|------|
| P0 | 运维 | D1 `alembic upgrade head` + `run-flywheel-migrations.ps1` |
| P0 | 验真 | `e2e-flywheel-validation.ps1`（设 `SMOKE_TOKEN`）+ T2 H5 表单真机 |
| P1 | 蜂群八轮 | GraphRAG 占位文档 或 副驾 Playwright 冒烟（不做录屏） |
| P1 | Accio A | A2 询盘上下文草稿 + A3 意向分 |

---

## 十、BUG 汇总（速查）

> 详表：[开发进度与BUG汇总-同步版.md](./开发进度与BUG汇总-同步版.md)

| 状态 | 数量 | 说明 |
|------|------|------|
| ✅ 已修复（本轮） | 11+ | 契约基建、auth/products 单测、products 路由、ai_engine、部分 P0/P1 单测 |
| ⬜ 仍开放 P0 | 8 | D1 生产迁移、D2–D6 UI 实点、T1 HTTPS、T2 H5 真机、e2e 脚本 |
| 🟡 质量债 P1 | 4 | 询盘三入口、副驾裸 fetch、integration 旧断言、PM 阻塞 7 项 |

**验收**：`cd backend && python -m pytest tests/unit -q` → **237 passed**（2026-05-25）

---

## 十一、变更记录

| 日期 | 说明 |
|------|------|
| 2026-05-25 | 新增 [开发进度与BUG汇总-同步版.md](./开发进度与BUG汇总-同步版.md)；同步桌面出海计知识库 |
| 2026-05-25 | BUG 收尾：`tests/unit` **237/237**；`ai_engine` 去掉多余 `None` 参数、重试用例补全 optimize 三元组；P0/P1 单测统一 `api_helpers` |
| 2026-05-25 | BUG 清单：`api_helpers` + `conftest`；`test_auth`/`test_products` 全绿；产品路由 `_guard_product_user`、`POST …/increment-view`、slug 路径 `/slug/{slug}` |
| 2026-05-26 | 蜂群八轮：GraphRAG 占位文档、`graphrag_integration_status`、round8 单测 |
| 2026-05-26 | 蜂群七轮收尾：D2–D6 改为服务层验真单测（21 passed）、`e2e` 含 round7 |
| 2026-05-26 | 蜂群七轮：DeerFlow 旁路、D2–D6 API 单测、`cron-deerflow-jobs.ps1` |
| 2026-05-26 | 蜂群六轮：Mem0/PostHog 接线、`/integrations`、P2 文档 |
| 2026-05-26 | 蜂群五轮：DeerFlow 任务审计、T2 表单校验、`e2e-flywheel-validation.ps1` |
| 2026-05-26 | 蜂群四轮：飞轮 API 入审计、T5 发布策略、`run-flywheel-migrations.ps1` |
| 2026-05-26 | 蜂群三轮：028 A5 审计、副驾 CSV 导出、T2/T6 文档对齐 |
| 2026-05-26 | 蜂群二轮：027 source_channel、A4 快照、副驾 gap 芯片、`smoke-flywheel-copilot.ps1` |
| 2026-05-26 | 首版统计；演讲/录屏划入 §八 后置 |

**维护**：每完成 D/T/A 项，在本表改 ✅；夜间队列见 `night-autodev-queue.yaml`（当前 **5/5 done**）。
