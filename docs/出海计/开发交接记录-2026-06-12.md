# 出海计 · 占位补齐与开发交接记录（2026-06-12）

> **交接时间**：2026-06-12  
> **源码仓（唯一运行源）**：`C:\Users\97907\Desktop\上线网站`  
> **知识库镜像**：`C:\Users\97907\Desktop\出海计`（文档同步，非部署源）  
> **产品定名**：**出海计**（`ChuHaiJi` / `com.youding.chuhaiji`）—— **不是**「出海记」「出海季」（后者仅营销活动）

---

## 一、30 秒读懂

1. **本轮已补齐**：TTS 合成 API、超管工作台去假 KPI、物流运费基础公式、出海计 App 品牌标识。  
2. **仍未商用**：24 组实验室模块、部分 SEO 示意页、真实 TTS 音频上游、Capacitor 商店包。  
3. **出海计入口**：租户登录后直达 `/client/app`（PWA 四 Tab）；BFF 在 `/api/v1/app/v1/*`。  
4. **开发账号**：`admin/admin123`（超管）· `tenant/tenant123` · `agent/agent123`；先 `scripts/start-dev-admin.ps1`。

---

## 二、命名与品牌（勿写错）

| 名称 | 用途 |
|------|------|
| **出海计** | App 正式名、商店、法务、BFF `app_name` |
| **优丁助手** | App 内 AI 对话功能品牌 |
| **优丁建材 OS** | 平台/融资叙事 |
| **出海季** | 仅营销活动，**不得**作 App 名 |

---

## 三、本轮代码变更（可验收）

| 项 | 路径 | 验收 |
|----|------|------|
| TTS 合成 | `POST /api/v1/media-factory/tts/synthesize` | 开发返回 `mode: mock`；生产无 `TTS_API_URL` → 503 |
| TTS 历史 | `GET /api/v1/media-factory/tts` | `list` / `generated_count` |
| 超管 KPI | `frontend/admin/src/views/admin/index.vue` | 无写死 `98.2%`；接 analytics + system-health |
| 物流运费 | `GET /api/v1/logistics/freight-calc` | 传 `distance_km` + `weight_ton` 有估算 |
| 出海计 UI | `frontend/admin/src/views/client/chuhaiji-app.vue` | 页头显示「出海计」标签 |

**单测**：

```powershell
cd backend
python -m pytest tests/unit/test_media_factory_tts.py -q
```

**手工**：

1. 超管 → 多媒体工厂 → TTS → 合成（应成功并提示 stub）  
2. `/admin` 工作台 → 健康/待办来自接口，无假曲线时显示「暂无流量」  
3. 租户 `tenant/tenant123` → `/client/app` → 四 Tab + 助手对话

---

## 四、占位 / 未完成清单（接手勿误判为已商用）

### 4.1 平台实验室（24 组，默认菜单隐藏）

见 `frontend/admin/src/constants/stubVisibility.ts` → `PLATFORM_LAB_PREFIXES`。  
开启：三区治理 / `localStorage admin_lab_enabled=1`。

含：geo-engine、media-factory、cognitive、edge-cdn、developer、globalization、logistics 子模块、code-tools、automation 等。  
**状态**：多数有 Vue 壳 + 部分 API；无上游时须 503 或 `mode: mock`，禁止假成功。

### 4.2 租户 Stub（6 条）

| 路径 | 策略 |
|------|------|
| `/client/media-factory` | Lab + Plan Gate |
| `/client/article-to-video` | Lab |
| `/client/egress` | Enterprise Plan Gate |
| `/client/ai-scenarios` | Lab |
| `/client/app` | Hide（送检菜单隐藏，路由仍存在） |
| `/client/copilot` | Hide（卖货副驾已做，菜单隐藏） |

### 4.3 SEO / GEO 示意（勿当业绩）

权威表：`docs/admin-seo-geo-data-trust-map.md`  
重点：`/seo/baidu-tools`（无 token 为 mock）、`/seo/schema-markup`、`/seo/eeat`、`/admin/geo`（生物基因，非卖货 GEO）。

### 4.4 出海计 App 剩余（APP-1c 商店包）

| 任务 | 状态 |
|------|------|
| PWA `/client/app` 四 Tab | ✅ |
| BFF `/api/v1/app/v1/*` | ✅ |
| Push（FCM） | 🔌 需 `FCM_SERVER_KEY` |
| Capacitor 应用宝 / TestFlight | 📋 见 `docs/出海计-Capacitor构建说明.md` |
| 语音离线增强 APP-2 | 部分（Web Speech + offline 询盘缓存） |

---

## 五、关键 API 索引（出海计）

| 能力 | 方法 / 路径 |
|------|-------------|
| 首页聚合 | `GET /api/v1/app/v1/home` |
| 今日 Tab | `GET /api/v1/app/v1/today` |
| 助手对话 | `POST /api/v1/app/v1/assistant/chat` |
| 设备注册 | `POST /api/v1/app/v1/devices/register` |
| 壳配置 | `GET /api/v1/app/v1/config` |
| 离线询盘 | `GET /api/v1/app/v1/inquiries/offline` |
| 测试 Push | `POST /api/v1/app/v1/push/test` |
| UBrain 通用 | `POST /api/v1/ubrain/chat` |

实现：`backend/app/api/v1/routes/app_bff.py`

---

## 六、环境与账号

```powershell
powershell -File scripts/start-dev-admin.ps1
```

| 角色 | 账号 | 密码 | 首页 |
|------|------|------|------|
| 平台超管 | admin | admin123 | `/admin` |
| 租户 | tenant | tenant123 | `/client/today` |
| 代理 | agent | agent123 | `/agent/performance` |

- API：`http://127.0.0.1:8001`  
- Admin：`http://127.0.0.1:5173/login`  
- 出海计 PWA：`http://127.0.0.1:5173/client/app`（租户登录后）

**功能开关**：

| 变量 | 用途 |
|------|------|
| `FCM_SERVER_KEY` | 真 Push |
| `TTS_API_URL` | TTS 真实合成（未接适配器时 501） |
| `MEDIA_FACTORY_ALLOW_MOCK` | 生产显式允许媒体 mock |
| `admin_lab_enabled` | 超管实验室菜单 |

---

## 七、验证命令（合并前）

```powershell
cd backend
python -m pytest tests/unit/test_media_factory_tts.py tests/unit/test_app_bff_batch5.py -q
python scripts/validate-no-fake-delivery.py
```

```powershell
cd frontend/admin
npm run cert:login-lock
npm run cert:role-shell-lock
```

---

## 八、同步到桌面「出海计」知识库

```powershell
powershell -ExecutionPolicy Bypass -File scripts/sync-to-chuhaiji-kb.ps1
```

个人笔记（不覆盖）：`Desktop/出海计/records/IDE-交接记录.md`

---

## 九、下一接手人优先序

1. **TTS 真实上游**：接 Edge-TTS 或商用 API，返回可播放 `audio_url`；生产路径禁止无音频假成功。  
2. **Capacitor 商店包**：按 `docs/出海计-Capacitor构建说明.md` 出 Android/iOS 包。  
3. **SEO 示意页**：`schema-markup` / `eeat` 接 API 或送检模式隐藏。  
4. **Hermes / 搞钱大赛**：需 Hermes 进程；见 `docs/出海计/HERMES-GEO-排名攻坚-交接记录-2026-06-02.md`。  
5. **假交付门禁**：改生产路径后跑 `validate-no-fake-delivery.py`，P0 必须为 0。

---

## 十、相关文档

| 文档 | 说明 |
|------|------|
| `docs/出海计-App产品规格.md` | App 四 Tab、包名、路线 |
| `docs/IDE-开发交接记录.md` | 多 IDE 总览 |
| `docs/未完成开发任务表.md` | P0/P1/P2 商用 backlog |
| `docs/admin-seo-geo-data-trust-map.md` | SEO/GEO 数据可信度 |
| `docs/product/LOGIN-SINGLE-ENTRY-CHARTER.md` | 登录硬锁 |
| `docs/product/ROLE-SHELL-LOCK-CHARTER.md` | 四壳硬锁 |

---

*PM-07 · 上线网站仓 · 2026-06-12*
