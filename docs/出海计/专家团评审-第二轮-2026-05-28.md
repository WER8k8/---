# 专家团评审 · 第二轮（2026-05-28）

> **编排**：Agency 184 + ECC 38（`agency-launch.py`）  
> **依据**：`round2-dev-signoff.json`、`round2-ops-signoff.json`、`full-checklist-latest.json`、`security-audit-latest.md`、`production-preflight-latest.json`、L3 截图 `docs/出海计/screenshots/round2/`  
> **对照**：[`上线评审-严格门禁-2026-05-25.md`](./上线评审-严格门禁-2026-05-25.md)

---

## 总判决

| 场景 | 结论 | 说明 |
|------|------|------|
| **代码合并 / 研发 Round 2** | **Go（有条件）** | 300 单测、checklist fail=0、B7 10/10、JWT 遮蔽已修 |
| **对内 stakeholder 演示** | **Go（有条件）** | 需 PM 书面确认 L3 截图 + 已知 Mock AI 口径 |
| **对外商用 / 独立 HTTPS 域** | **No-Go** | 生产 preflight 2 fail、无 Postgres、Celery/Redis 未真跑 |
| **灰度单租户** | **有条件 Go** | 见 [灰度上线-执行清单.md](./灰度上线-执行清单.md) 阶段 A |

**相较 2026-05-25 评审的进步**：单测 237→**300**；一键清单 **fail=0**；pip-audit 约 74→**14**；L3 六页有截图证据；`demo-acceptance` 绿。

---

## 分角色意见

### 1. 工程质量 · code-reviewer（ECC）

**裁决：CONDITIONAL**

| 优势 | 证据 |
|------|------|
| 单测基线 | `pytest` **300/300**（venv） |
| 自动化门禁 | `full-checklist-latest.json` **fail_count: 0**；853 路由挂载 |
| 试点发布链 | B7 pilot worker **10/10 success** |
| 依赖治理 | P0 pin + venv 审计 **14 条/5 包** |

**商用前必改（优先级）**

1. 生产 **Postgres** + `preflight --production` 全绿（当前 `ready: false`，SQLite/platforms fail）  
2. 清理 **starlette / langchain-openai** 等运行时 CVE（见 `security-audit-latest.md`）  
3. **真实 AI Key** 后复测副驾（当前 `No AI providers… MVP_LAUNCH`）  
4. **Redis + Celery** 真跑 + 爬虫 Webhook 配置  
5. **passlib/bcrypt** 在 venv 仍有 trapped 告警，生产密码哈希需复验  

**L3 截图说明**：`round2-l3-screenshots.mjs` 仅证明「路由可渲染」，**不能替代**人工 UAT（导出按钮、权限、错误态、真数据）。保留作 CI smoke，商用前补 **1 轮真人点检 + 预发 HTTPS**。

---

### 2. 安全 · security-reviewer（ECC）

| 场景 | 风险 |
|------|------|
| 内测演示 | **低～中** |
| 对外生产 | **高** |

| pip-audit 项 | 处理 |
|--------------|------|
| **starlette**（3 CVE） | 上线前必升（HTTP 栈） |
| **langchain-openai** | 启用 AI 前必升 ≥1.1.14 |
| pip / pytest / setuptools | dev/CI 可暂缓，生产镜像勿带旧 setuptools |

**JWT 包遮蔽**：`app/core/security/__init__.py` 经 `importlib` 重导出，**已修复**，preflight `jwt_secret: pass`。

**运维缺口**：Redis 未启 → 限流/会话多实例弱化；Celery 未跑 → 异步任务与备份不可靠；`backup.last_run: null` → **RPO 不可接受**。

---

### 3. 产品 · product-manager + reality-checker（Agency）

**对内演示：Go（有条件）** — 自动化与截图齐全，但须口头声明：**副驾为 Mock、支付/物流为 demo、Accio 部分 501**。

**付费商用：No-Go** — 与 05-25 阻塞项大部分仍有效，仅「开发门禁」显著改善。

**PM 须亲自验证（不可交给脚本）**

1. 副驾：有 Key 时简报/飞轮是否**非 Mock**  
2. 询盘 **导出** 是否真下文件（截图未点按钮）  
3. 创始人诊断：微信绑定或 `FOUNDER_DEBUG_TOKEN` 实机路径  
4. 演示域 **HTTPS** + H5 真机（若对外）  
5. **PM-01～07** 书面签字（套餐、法务、平台名单）

**建议下一周聚焦**：预发 Postgres + Alembic + seed → preflight 绿 → Redis/Celery → 真人 L3 签字。

---

### 4. 运维 · insulation-devops-engineer（灵码）

| 项 | Round 2 状态 | 评审 |
|----|--------------|------|
| `.env` 备份 | ✅ `docs/round2-backup/` | 继续；补 **DB 路径确认** |
| `youding_dev.db` | ❌ 未备份 | **阻塞换机/灾难恢复** |
| Celery/beat | 仅文档 | 预发必须起进程并留日志 |
| 生产 preflight | 已跑，预期红 | 上 Postgres 后 **24h 内复跑** |
| cron | 文档已对齐 | 预发 crontab **截图归档** |

---

### 5. 测试 · e2e-runner / testing-reality-checker（ECC）

| 层级 | 状态 |
|------|------|
| L1 单测 | ✅ 300 |
| L2 API smoke | ✅ `l3-api-checklist`、`e2e_flywheel` |
| L3 浏览器 | ◐ 自动截图 6/6，**缺人工签字** |
| integration 全量 | 未纳入本轮门禁 |

**证据缺口**：`npm audit` 未跑（本机无 npm）；integration 套件未作为发布门禁。

---

## 阻塞项对照（05-25 → 05-28）

| 05-25 阻塞 | 05-28 |
|------------|--------|
| unit 237 | ✅ **300** |
| e2e 需 SMOKE_TOKEN | ✅ checklist 内已覆盖 |
| 飞轮无 UI 签字 | ◐ 有截图，**无 PM 字** |
| Alembic 025–028 生产 | ❌ 仍待预发 |
| HTTPS 演示域 | ❌ |
| PM 7 项 | ❌ |
| integration/e2e 未入门禁 | ❌ 部分 API 已 smoke |

---

## 专家团一致行动项（按 Owner）

| # | 行动 | Owner | 截止建议 |
|---|------|-------|----------|
| 1 | 预发 Postgres + migrate + seed，`preflight --production` → 0 fail | 研发+运营 | 3 工作日 |
| 2 | 升 starlette / langchain-openai，复跑 venv `pip-audit` | 研发 | 2 工作日 |
| 3 | Redis + Celery worker/beat + 备份 cron 真跑 | 运营 | 3 工作日 |
| 4 | 真人 L3 六项 + 导出实点 + PM 签字表勾「对内演示」 | 产品 | 本轮发布前 |
| 5 | 配置 AI Key 后副驾复测并更新 signoff | 研发 | 有 Key 当日 |

---

## PM 签字（专家团建议）

| 项 | 建议 |
|----|------|
| 研发 Round 2 里程碑 | ☑ 认可 |
| 对内演示 | ☑ 有条件（Mock 口径书面告知） |
| 对外商用 | ☐ 维持 No-Go |
| 触发第三轮评审条件 | preflight 生产绿 + 真人 L3 + Celery 日志 + npm audit |

---

## 附件索引

| 文件 | 用途 |
|------|------|
| `docs/round2-dev-signoff.json` | 研发签字 |
| `docs/round2-ops-signoff.json` | 运营签字 |
| `docs/出海计/研发与运营-第二轮验收清单.md` | 执行清单 |
| `docs/出海计/漏洞修复计划-产品经理版.md` | 安全批次 |
| `docs/出海计/screenshots/round2/manifest.json` | L3 截图清单 |

---

**后续**：第三轮专家团评审见 [`专家团评审-第三轮-2026-05-29.md`](./专家团评审-第三轮-2026-05-29.md)（2026-05-29）。
