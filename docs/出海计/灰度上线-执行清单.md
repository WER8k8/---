# 灰度上线 · 执行清单（可勾选）

> **目标**：从「237 unit 全绿」推进到「预发/单租户可跑飞轮主链」  
> **不等于**：对外商用签字（见 [上线评审-严格门禁](./上线评审-严格门禁-2026-05-25.md) 若已创建）  
> **主站商用 MVP**（询盘 + 流量看板，AI 二期）：见 **[主站商用MVP-上线清单](./主站商用MVP-上线清单.md)** · `scripts/deploy-mvp-production.ps1`

---

## 阶段 A · 本机/预发（工程师）

| # | 动作 | 命令 / 路径 | 勾选 |
|---|------|-------------|------|
| A1 | 单元测试全绿 | `cd backend && python -m pytest tests/unit -q` | ☐ |
| A2 | 飞轮迁移 upgrade | `powershell -File scripts/run-flywheel-migrations.ps1`（**须 PostgreSQL**；SQLite 会在 pgvector 处失败） | ☐ |
| A3 | 迁移与表检查 | `powershell -File scripts/check-flywheel-migration.ps1` | ☐ |
| A4 | 启动后端 | `cd backend && uvicorn app.main:app --host 127.0.0.1 --port 8000` | ☐ |
| A5 | 取 JWT | `powershell -File scripts/get-smoke-token.ps1` | ☐ |
| A6 | E2E 飞轮脚本 | `powershell -File scripts/e2e-flywheel-validation.ps1`（需 A5） | ☐ |
| A7 | 路由挂载 | `python scripts/check_mounted_routes.py`（仓库根） | ☐ |

---

## 阶段 B · 生产库（运维）

| # | 动作 | 说明 | 勾选 |
|---|------|------|------|
| B1 | 备份生产库 | 迁移前全量备份 | ☐ |
| B2 | `alembic upgrade head` | 含 025–028；生产 `DATABASE_URL` | ☐ |
| B3 | 表存在性 | `ubrain_action_audits` 等 6 表 OK | ☐ |
| B4 | `.env.prod` | JWT/DB/Redis 强密钥；`DEBUG=false` | ☐ |

---

## 阶段 C · 验真（产品/运营）

| # | 项 | 勾选 |
|---|-----|------|
| C1 | HTTPS 演示独立域可访问 | ☐ |
| C2 | 副驾 D2 三根支柱有数据 | ☐ |
| C3 | D3 Brief ≥2 accio_actions | ☐ |
| C4 | D4 跑飞轮 → pipelines 有 job | ☐ |
| C5 | D6 同步反馈后 hints 更新 | ☐ |
| C6 | H5 真机提交询盘（合法手机号） | ☐ |

---

## 阶段 D · PM 书面（商用前）— **本轮跳过**

> 当前目标为 **全站技术验通 + 安全预检**，不做 PM 签字门禁。商用前再补。

- ⏭ 演示 HTTPS 域（技术验通仍建议自测 HTTPS，见 [全站验通与安全检查](./全站验通与安全检查.md)）  
- ⏭ 套餐金额与 Token 配额  
- ⏭ UBrain/出海参谋免责声明  

---

## 一键串（本机，后端已启动）

```powershell
cd "C:\Users\97907\Desktop\UJ\website CodeBuddy"
powershell -File scripts/run-flywheel-migrations.ps1
powershell -File scripts/get-smoke-token.ps1
powershell -File scripts/e2e-flywheel-validation.ps1
```
