# 专家团评审 · 第三轮（2026-05-29）

> **编排**：Agency 184 + ECC 38  
> **依据**：实时 `pytest`、`check_gates_g1_g7.py`、生产形态 preflight 模拟、`gates-check-latest.json`、`full-checklist-latest.json`、`security-audit-latest.md`、`sprint-task-report-latest.json`、`pm-deliverables/`、`pm-signoffs/l3-signoff.json`  
> **对照**：[`专家团评审-第二轮-2026-05-28.md`](./专家团评审-第二轮-2026-05-28.md) · [`上线评审-严格门禁-2026-05-25.md`](./上线评审-严格门禁-2026-05-25.md)

---

## 总判决

| 场景 | 结论 | 说明 |
|------|------|------|
| **代码合并 / 研发 Round 3** | **Go** | 单测 **337/337**；G1–G3/G5–G7 绿；冲刺任务 **97/97 done** |
| **对内 stakeholder 演示** | **Go（有条件）** | 须书面声明 Mock AI / demo 物流 / 支付未严验签 |
| **对外商用 / 独立 HTTPS 域** | **No-Go** | 生产 preflight **3 fail**；G4 红；Postgres/Celery/HTTPS 未就绪 |
| **灰度单租户** | **有条件 Go** | 见 [灰度上线-执行清单.md](./灰度上线-执行清单.md) 阶段 A→B |

**相较 2026-05-28 第二轮的进步**

| 指标 | Round 2 | Round 3 |
|------|---------|---------|
| 单测 | 300 | **337** |
| G1–G7 自动门禁 | 7/7（开发态） | **6/7**（G4 按生产口径显式标红） |
| 冲刺任务 | 70 done + 20 open | **97/97 done** |
| PM 交付物 | 未归档 | **PM-01～07 已写入 `docs/pm-deliverables/`** |
| L3 API 冒烟 | 7 项 | **8 项**（含询盘导出、SEO 报告导出） |
| pip-audit | 14 条 / 5 包 | **12 条 / 4 包**（langchain-openai 已不在列表） |

**第二轮触发条件完成度（仍未满足商用）**

| 条件 | 状态 |
|------|------|
| preflight 生产绿 | ❌ `ready=false`，3 fail |
| 真人 L3 + PM 签字 | ❌ `pm_signed: false` |
| Celery 真跑日志 | ❌ broker 未配 |
| npm audit | ❌ npmmirror 不支持 security API |

---

## 分角色意见

### 1. 工程质量 · code-reviewer（ECC）

**裁决：Go（研发里程碑） / CONDITIONAL（商用）**

| 优势 | 证据 |
|------|------|
| 单测基线 | `pytest tests/unit` **337/337**（2026-05-29 实跑） |
| 门槛脚本 | G1 Accio 四 gap 非 501；G2 主链 API 挂载；G6 超管 0 缺口 |
| 冲刺闭环 | `sprint-task-report-latest.json` **97/97 done**，`open_engineering: 0` |
| L3 扩展 | `inquiries_export`、`seo/report/export` 200 OK |
| B7 发布链 | `demo-acceptance-latest.json` pilot **10/10 success**（历史证据仍有效） |

**商用前必改**

1. **Postgres** + Alembic 025–028 在预发执行，`DATABASE_URL` 非 SQLite  
2. **G4**：至少一个真实 AI Key 且 `MVP_LAUNCH=0` 后复测副驾  
3. **starlette** 3 CVE 升级（HTTP 栈，见 security-audit）  
4. **passlib/bcrypt** trapped 告警——生产密码哈希须单独验证  
5. `run-preflight-production.ps1` 在 sprint 脚本中被 Python 误调导致 SyntaxError——CI 门禁需修正调用方式  

---

### 2. 安全 · security-reviewer（ECC）

| 场景 | 风险 |
|------|------|
| 内测演示 | **低～中** |
| 对外生产 | **高** |

**生产形态 preflight 模拟（2026-05-29）**

```
ready=false  score={pass:9, warn:3, fail:3}
FAIL database_url   生产环境不应使用 SQLite
FAIL payment_strict 生产须 PAYMENT_STRICT_VERIFY=1
FAIL ssl_provider   生产须 certbot/ssl_worker，禁用 mock
WARN logistics / frontend_url / crawl_webhook_secret
```

| pip-audit | Round 2 → Round 3 |
|-----------|-------------------|
| 总量 | 14 → **12** |
| 运行时关键 | **starlette** 3 CVE 仍在 |
| 已改善 | langchain-openai 不再出现在列表 |
| dev 工具 | pip / pytest / setuptools 可暂缓，生产镜像勿带旧 setuptools |

**运维缺口（延续）**：Redis 未启；Celery broker 未配；`backup.last_run: null`。

---

### 3. 产品 · product-manager + reality-checker（Agency）

**对内演示：Go（有条件）** — 工程自动化与 PM 交付物草稿齐全；演示前须口头 + 书面说明 Mock 口径。

**付费商用：No-Go** — 第二轮阻塞项大部分仍有效；PM 交付物为**草稿**（如 PM-01 标注「试点名单草稿」），不等于商用签字。

**PM 须亲自验证（不可交给脚本）**

1. 副驾：配置 Key 后简报/飞轮是否非 Mock  
2. 询盘导出：API 200 已验，**浏览器真点 + 文件内容**仍缺 PM 字  
3. HTTPS 演示域 + H5 真机（PM-04 仅为配置说明，未验收）  
4. `pm-signoffs/l3-signoff.json` → **`pm_signed: false`**  

**PM 交付物进展**：`docs/pm-deliverables/` 已落盘 PM-01～07，较第二轮「7 项未书面确认」有实质推进，但**不等于签字通过**。

---

### 4. 运维 · insulation-devops-engineer（灵码）

| 项 | Round 3 状态 | 评审 |
|----|--------------|------|
| 开发 preflight | dev 态 0 fail | ✅ 开发门禁维持 |
| 生产 preflight | 3 fail | ❌ **阻塞商用** |
| Postgres + migrate | 未执行 | ❌ 灰度阶段 B 首要项 |
| Redis + Celery | broker 未配 | ❌ G3 备注仍红 |
| 备份 cron | `last_run: null` | ❌ RPO 不可接受 |
| npm audit | npmmirror 404 | ⚠️ 换官方 registry 或离线 lockfile 审计 |

---

### 5. 测试 · e2e-runner / testing-reality-checker（ECC）

| 层级 | 状态 |
|------|------|
| L1 单测 | ✅ **337** |
| L2 API smoke | ✅ 8/8（含 export 两条） |
| L3 浏览器 | ◐ Round 2 截图 6/6 仍有效，**无 PM 签字** |
| G4 生产 AI | ❌ 无 Key / MVP_LAUNCH |
| integration 全量 | 未纳入发布门禁 |

**证据缺口**：npm audit 未有效执行；`flywheel_tests` 在 sprint-final-close 中为 fail。

---

## 阻塞项对照（05-28 → 05-29）

| 05-28 阻塞 / 待办 | 05-29 |
|-------------------|--------|
| unit 300 | ✅ **337** |
| preflight 生产绿 | ❌ 3 fail |
| 升 starlette | ❌ 仍在 audit 列表 |
| Redis + Celery 真跑 | ❌ |
| 真人 L3 + PM 签字 | ❌ API 绿，**pm_signed false** |
| PM 7 项书面 | ◐ **草稿已归档**，未签字 |
| npm audit | ❌ registry 不支持 |

---

## 专家团一致行动项（按 Owner）

| # | 行动 | Owner | 优先级 |
|---|------|-------|--------|
| 1 | 预发 Postgres + migrate + seed，生产 preflight → 0 fail | 研发+运营 | P0 |
| 2 | 升 starlette ≥0.49.1，复跑 venv pip-audit | 研发 | P0 |
| 3 | Redis + Celery worker/beat 真跑并留日志 | 运营 | P0 |
| 4 | 配置 AI Key，`MVP_LAUNCH=0`，G4 复绿 | 研发 | P1 |
| 5 | PM 真人 L3 六项 + 导出实点 + `l3-signoff.json` 签字 | 产品 | P1 |
| 6 | npm audit 改官方源或 CI 离线扫描 | 研发 | P2 |
| 7 | 修正 sprint 脚本对 `run-preflight-production.ps1` 的调用 | 研发 | P2 |

---

## PM 签字（专家团建议）

| 项 | 建议 |
|----|------|
| 研发 Round 3 里程碑 | ☑ **认可** |
| 对内演示 | ☑ 有条件（Mock 口径书面告知） |
| 对外商用 | ☐ **维持 No-Go** |
| 触发第四轮评审条件 | 生产 preflight 0 fail + G4 绿 + PM L3 签字 + Celery 日志 |

---

## 附件索引

| 文件 | 用途 |
|------|------|
| `docs/gates-check-latest.json` | G1–G7 最新（6/7） |
| `docs/full-checklist-latest.json` | 一键清单 fail=0 |
| `docs/production-preflight-latest.json` | 开发态 preflight |
| `docs/security-audit-latest.md` | pip-audit 12 条 |
| `docs/sprint-task-report-latest.json` | 97/97 冲刺闭环 |
| `docs/pm-deliverables/` | PM-01～07 草稿 |
| `docs/pm-signoffs/l3-signoff.json` | L3 工程冒烟 + PM 待签 |
| `docs/第三轮专家团评审-签字表.md` | 本轮签字表 |

---

**后续**：专家团行动项修补见 [`专家团评审-修补开发-2026-05-29.md`](./专家团评审-修补开发-2026-05-29.md)。
