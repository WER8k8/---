# 研发 + 运营 · 第二轮验收清单

> **日期**：2026-05-28  
> **背景**：P0 依赖安全升级已入仓；请研发、运营各跑一轮并**签字/截图**。  
> **原则**：在 **`backend\.venv`** 或独立 venv 中执行，勿用混了全局包的 Python。

---

## 分工一览

| 角色 | 负责 | 预计时间 |
|------|------|----------|
| **研发** | 依赖安装、单测、API 脚本、管理端 L3 点检 | 1～2 小时 |
| **运营** | Redis/Celery、爬虫 Worker、备份、生产预检对照 | 1～2 小时 |
| **产品** | 勾选本清单末尾「PM 签字」 | 15 分钟 |

---

## 一、研发（复制执行）

### 1. 环境

```powershell
cd "C:\Users\97907\Desktop\UJ\website CodeBuddy\backend"
python -m venv .venv
.\.venv\Scripts\Activate.ps1
$env:PYTHONPATH = (Get-Location).Path
$env:DATABASE_URL = "sqlite:///./youding_dev.db"
```

把旧电脑备份的 **`backend\.env`**、**`youding_dev.db`** 放到 `backend\`（若要保留数据）。

### 2. 安全依赖 + 审计（P0 复验）

```powershell
cd ..
powershell -ExecutionPolicy Bypass -File scripts\apply-security-upgrades.ps1
```

**通过标准**：脚本末尾 `OK`；打开 `docs\security-audit-latest.md`，漏洞数应 **明显低于 74**（当前基线约 **31**，以当次报告为准）。

### 3. 补齐开发库

```powershell
cd backend
python scripts\ensure_dev_sqlite.py
python scripts\seed_platforms_full.py
```

### 4. 启动本地（两个窗口别关）

**窗口 A — API 8001**

```powershell
cd backend
.\.venv\Scripts\Activate.ps1
$env:PYTHONPATH = (Get-Location).Path
$env:DATABASE_URL = "sqlite:///./youding_dev.db"
$env:JWT_SECRET_KEY = "dev-" + ("x" * 32)
$env:FOUNDER_DEBUG_TOKEN = "checklist-founder-dev"
$env:REDIS_ENABLED = "false"
python -m uvicorn app.main:app --host 127.0.0.1 --port 8001
```

**窗口 B — 管理端 5173**

```powershell
cd frontend\admin
npm install
npm run dev -- --host 0.0.0.0 --port 5173
```

登录：`admin` / `admin123`（若改过密码用新密码）。

### 5. 自动化门禁

```powershell
cd "C:\Users\97907\Desktop\UJ\website CodeBuddy"
$env:JWT_SECRET_KEY = "checklist-" + ("x" * 32)
$env:FOUNDER_DEBUG_TOKEN = "checklist-founder-dev"
$env:SMOKE_BASE = "http://127.0.0.1:8001"

python scripts\check_mounted_routes.py
python scripts\run_demo_acceptance.py
python scripts\run_production_preflight.py
cd backend
python -m pytest tests\unit -q --tb=no
cd ..
powershell -ExecutionPolicy Bypass -File scripts\run-full-checklist.ps1 -SkipUnitTests
```

**通过标准**：

- `pytest` → 299 passed  
- `docs\full-checklist-latest.json` → `"ok": true`  
- `docs\demo-acceptance-latest.json` → `"ok": true`

### 6. 浏览器 L3（研发必做，截图）

打开 `http://localhost:5173/login`，逐项打勾并截图存 `docs/出海计/screenshots/round2/`：

- [ ] 登录 / 登出  
- [ ] 询盘列表（unified 入口）  
- [ ] 发布台 / 平台试点  
- [ ] **副驾**：飞轮状态柱、生成简报、跑飞轮（有 Key 时看非 Mock）  
- [ ] **创始人诊断**（需 `FOUNDER_DEBUG_TOKEN` 或微信绑定）  
- [ ] 询盘导出  

对照：`docs\演示验收清单.md`、`docs\出海计\PM终测验收报告.md`。

### 7. 研发回填

在 `docs\round2-dev-signoff.json` 填写（见仓库模板）或飞书回复：

- 执行人、日期  
- pytest / full-checklist 是否绿  
- pip-audit 剩余条数  
- L3 未完成项编号  

---

## 二、运营（复制执行）

### 1. 本地后台进程（与研发联调同一天）

```powershell
# Redis（若已装）
redis-server
# 或 Docker: docker run -d -p 6379:6379 redis:7

cd "C:\Users\97907\Desktop\UJ\website CodeBuddy"
powershell -ExecutionPolicy Bypass -File scripts\start-celery-local.ps1
```

**通过标准**：Celery worker + beat 无报错；日志可见 GEO beat 注册（见 `docs\运维-cron.md`）。

### 2. 国际爬虫 Worker（按文档）

阅读并按 `docs\international-crawl-worker.md` 部署/模拟一次 Webhook（可用 curl 打本地 `CRAWL_WEBHOOK_SECRET`）。

### 3. 发布队列 / 定时

```powershell
$env:SMOKE_BASE = "http://127.0.0.1:8001"
powershell -ExecutionPolicy Bypass -File scripts\ops-publish-worker-cron.ps1
```

对照 `docs\运维-cron.md` 勾选已在测试机/预发配置的 cron。

### 4. 备份与换机

- [ ] 备份 `backend\.env`、`backend\youding_dev.db` 到网盘/U 盘  
- [ ] 记录备份路径与日期  

### 5. 生产 / 预发对照（尚未上云则做「模拟」）

```powershell
$env:JWT_SECRET_KEY = "<生产32位+随机>"
$env:PREFLIGHT_ENVIRONMENT = "production"
python scripts\run_production_preflight.py --production
```

**预期**：SQLite、缺 platforms 等会 **fail** — 属正常，上 Postgres + 迁移 + seed 后再跑应转绿。

运营回填：`docs\round2-ops-signoff.json`（模板见下）。

### 6. 运营关注项（非代码）

| 项 | 动作 |
|----|------|
| PM-04 演示域 / HTTPS | 域名 + 证书排期 |
| 真实 AI Key | `.env` 填 `AI_*_API_KEY`，通知研发复测副驾 |
| 生产 Postgres | 建库 + `run-flywheel-migrations.ps1` |
| 监控告警 | 健康检查 URL、磁盘、备份目录可写 |

---

## 三、双方交接点

```text
研发本地绿（pytest + full-checklist + L3 截图）
        ↓
运营起 Redis/Celery/爬虫 + 备份
        ↓
预发机：Postgres + .env.prod + 再跑 preflight --production
        ↓
产品 PM 签字 → 对外演示 / 商用 Go-No-Go
```

---

## 四、PM 签字（第二轮）

> **2026-05-28 自动化执行记录**：`docs/round2-dev-signoff.json`、`docs/round2-ops-signoff.json`；L3 截图见 `docs/出海计/screenshots/round2/`。

| 项 | 研发 | 运营 | PM |
|----|------|------|-----|
| 依赖审计较 P0 下降（venv **14** 条） | ☑ | — | ☐ |
| 单测 + 一键清单绿（**300** / fail=0） | ☑ | — | ☐ |
| L3 浏览器 6 项截图 | ☑ | — | ☐ |
| Celery/备份/cron 就绪 | — | ◐ 备份+cron；Celery 待预发 | ☐ |
| 预发/生产路径明确 | — | ◐ preflight 已跑（prod 预期红） | ☐ |
| **第二轮可对内演示** | ☑ 脚本+截图 | ◐ | ☐ 待 PM 勾（专家团见 [`专家团评审-第二轮-2026-05-28.md`](./专家团评审-第二轮-2026-05-28.md)） |

---

## 五、相关命令索引

| 脚本 | 谁跑 |
|------|------|
| `scripts\run-round2-dev.ps1` | 研发（venv 一条龙） |
| `scripts\round2-l3-screenshots.mjs` | 研发（L3 截图） |
| `scripts\apply-security-upgrades.ps1` | 研发 |
| `scripts\run-full-checklist.ps1` | 研发 |
| `scripts\run-round2-ops.ps1` | 运营 |
| `scripts\start-celery-local.ps1` | 运营 |
| `scripts\run_production_preflight.py --production` | 运营（预发） |
| `docs\出海计\漏洞修复计划-产品经理版.md` | 产品阅 |
