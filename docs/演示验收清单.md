# 演示验收清单（Sprint G）

> **阶段**：研发 Sprint A～F 已完成后的 **演示验收与签字**  
> **关联**：`docs/产品经理-开发文档.md` §九、`scripts/run_demo_acceptance.py`

---

## 一、一键验收

```powershell
cd backend
$env:JWT_SECRET_KEY="test-"+("x"*32)
python ..\scripts\run_demo_acceptance.py
```

产出：`docs/demo-acceptance-latest.json`、`docs/b7-pilot-acceptance-result.json`

**通过标准**：

| 步骤 | 说明 |
|------|------|
| `check_mounted_routes` | 关键 API 已挂载 |
| `b7_pilot_acceptance` | 5+5 平台 seed + 1 母版 → ≥10 条 `publish_task` + worker 处理 |

---

## 二、人工彩排（PM / 销售）

| # | 动作 | 入口 | 通过 |
|---|------|------|------|
| 1 | 超管登录 | `http://localhost:5173/login` | ⬜ |
| 2 | 统一发布台 | 管理端「统一发布台」/ `/publish/unified` | ⬜ |
| 3 | 选 5+5 平台发任务 | 母版保存 → 勾选平台 → 创建任务 | ⬜ |
| 4 | 租户站物流查询 | 租户前台物流页 + 运单号 | ⬜ |
| 5 | 七步脚本 ①②④⑥⑦ | 按总册 §2.3 | ⬜ |

---

## 三、API 抽查

| 接口 | 用途 |
|------|------|
| `GET /api/v1/platforms/pilot` | 5+5 试点平台（cn / global 分组） |
| `GET /api/v1/platforms` | 全量平台（含 `region`） |
| `POST /api/v1/content-masters/{id}/publish` | 母版 → 发布任务 |

---

## 四、PM 签字（仍阻塞商用）

- [ ] 5+5 试点名单（可与 seed 默认一致或调整）  
- [ ] 正式 40 平台表  
- [ ] 套餐金额与 Token/IP 配额  
- [ ] 1 个 HTTPS 演示独立域（DevOps）

**PM 签字**：________　**日期**：________
