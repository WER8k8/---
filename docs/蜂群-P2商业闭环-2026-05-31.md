# 蜂群冲刺 · P2 商业闭环（2026-05-31）

## 顺序交付

| 序号 | 包 | 内容 |
|------|-----|------|
| W10 | 真实支付 | `wechat_pay_v3.py` Native 下单 + v3 回调解密；`GET /payment/channels/status` |
| W11 | 到期提醒 | `tenant_renewal_notify_service` + lifecycle 脚本 `--notify-only` |
| W12 | 全电票 | `einvoice_provider.py` + `POST .../issue-via-provider` |

## W10 配置（生产）

```env
WECHAT_PAY_MCH_ID=
WECHAT_PAY_API_V3_KEY=
WECHAT_PAY_SERIAL_NO=
WECHAT_PAY_PRIVATE_KEY_PATH=./certs/apiclient_key.pem
WECHAT_PAY_NOTIFY_URL=https://api.youding.com/api/v1/payment/notify
PAYMENT_ALLOW_MOCK=0
PAYMENT_STRICT_VERIFY=1
```

## W11 Cron

```powershell
python backend/scripts/run_tenant_lifecycle.py
python backend/scripts/run_tenant_lifecycle.py --notify-only --within-days 7
```

需配置 `FEISHU_WEBHOOK_URL` 和/或 SMTP。

## W12 诺诺

```env
EINVOICE_PROVIDER=nuonuo
NUONUO_APP_KEY=
NUONUO_APP_SECRET=
NUONUO_SELLER_TAX_NUM=
```

## 验证

```powershell
cd backend
python -m pytest tests/unit/test_commercial_loop_p2.py -q
```
