# 蜂群 P9 商业闭环 — 2026-05-31

> 承接 P8：审计预览、微信探针、HTTP notify 外网验收脚本。

## 范围（W30–W32）

| ID | 交付 | 边界 |
|----|------|------|
| W30 | 改派审计分页预览 | `GET /inquiries/assignment-audit`；与 CSV 同源筛选 |
| W31 | 微信 Native 探针 | `POST /payment/ops/wechat/probe`（super_admin）；不落库 |
| W32 | HTTP notify 验收 | `scripts/staging_notify_http_check.py` + ngrok 配置提示 |

## 后端

### W30

- `list_assignment_audit_page()` — 分页包装 `query_assignment_audit_rows`
- `GET /api/v1/inquiries/assignment-audit?page=&page_size=&start_at=&end_at=&source_channel=`

### W31

- `probe_wechat_native()` — mock / live Native 预下单
- `POST /api/v1/payment/ops/wechat/probe`

### W32

- `staging_notify_http_check.py` — 创建 pending 订单，HTTP POST 双渠道 notify
- `--public-url` 写入网关 notify 配置提示

## 前端

| 页面 | 变更 |
|------|------|
| `/admin/inquiries` | 「审计预览」Drawer + 分页表 |
| `/admin/finance/payment-ops` | 「微信 Native 探针」 |

## 测试

```powershell
python -m pytest tests/unit/test_commercial_loop_p9.py -q
python scripts/staging_notify_http_check.py --base-url http://127.0.0.1:8001
python scripts/staging_notify_http_check.py --base-url http://127.0.0.1:8001 --public-url https://xxxx.ngrok-free.app
```

## 后续（P10）

- 审计预览：按操作人 / 改派目标用户筛选
- 支付运维：探针结果 QR 图 inline 展示
- 外网验收：自动读取 ngrok API 探测 public URL 可达性
