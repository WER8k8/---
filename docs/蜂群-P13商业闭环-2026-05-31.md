# 蜂群 P13 商业闭环 — 2026-05-31

> 承接 P12：严验签签名正向验收、运维审计 CSV、改派审计姓名列优先。

## 范围（W42–W44）

| ID | 交付 | 边界 |
|----|------|------|
| W42 | 签名 notify 正向验收 | HMAC（`PAYMENT_WEBHOOK_SECRET`）或支付宝 RSA2 沙箱签名 |
| W43 | 运维审计 CSV 导出 | `GET /payment/ops/audit/export` + 前端按钮 |
| W44 | 改派审计 CSV 姓名优先 | `actor_user_name` 列在 `actor_user_id` 之前；无姓名回退 ID |

## 后端

### W42

- `compute_webhook_hmac_signature()` — P0-07 同源
- `AlipayClient.sign_notify_payload()` — RSA2 沙箱签名
- `run_public_notify_e2e_check(..., try_signed=)` — 严验签两阶段：未签名拒绝 + 签名入账
- `POST /ops/staging/public-notify-check?try_signed=true`

### W43

- `query_payment_ops_audit_rows()` / `build_payment_ops_audit_csv()`
- `GET /api/v1/payment/ops/audit/export`

### W44

- `_CSV_HEADERS` 调整列序；`_csv_row_values()` 姓名优先

## 前端

| 页面 | 变更 |
|------|------|
| `/admin/finance/payment-ops` | 运维历史「导出 CSV」；公网 notify 带 `try_signed` |
| `/admin/inquiries` | CSV 列说明更新 |

## 测试

```powershell
python -m pytest tests/unit/test_commercial_loop_p13.py -q

$env:PAYMENT_STRICT_VERIFY='1'
$env:PAYMENT_WEBHOOK_SECRET='your-secret'
python scripts/staging_notify_http_check.py --base-url http://127.0.0.1:8001 --via-public --discover-ngrok --try-signed
```

## 商业闭环状态

**P0–P13 代码线已完成。** 后续为实机验收（ngrok、沙箱密钥、PM 签字），见 `docs/module-progress-latest.txt`。
