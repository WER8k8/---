# 蜂群 P7 商业闭环 — 2026-05-31

> 承接 P6：改派审计导出、支付运维页、微信严验签集成测试。

## 范围（W24–W26）

| ID | 交付 | 边界 |
|----|------|------|
| W24 | 改派审计 CSV 导出 | `GET /inquiries/assignment-audit/export`；仅 admin/super_admin；`assert_export_allowed` scope=tenant |
| W25 | 支付运维 API + 管理页 | `GET /payment/ops/status`、`POST /payment/ops/wechat/refresh-certs`（super_admin）；`/admin/finance/payment-ops` |
| W26 | 微信严验签集成测试 | mock 平台证书拉取 + 严格模式下 HTTP 403 拒绝无效签名 |

## 后端

### W24 审计导出

- **Service** `inquiry_assignment_audit_service.py`：`query_assignment_audit_rows`、`build_assignment_audit_csv`
- **Route** `GET /api/v1/inquiries/assignment-audit/export?source_channel=`

### W25 支付运维

- **Service** `payment_ops_service.py`：`get_payment_ops_status`、`refresh_wechat_platform_certs`
- **Cert 快照** `WeChatPlatformCertStore.cache_snapshot()`
- **Routes**
  - `GET /api/v1/payment/ops/status` — admin/super_admin
  - `POST /api/v1/payment/ops/wechat/refresh-certs` — super_admin

### W26 测试

- `test_commercial_loop_p7.py`：CSV、ops 路由、mock cert fetch 严验签、无效签名 403

## 前端

| 页面 | 路径 | 说明 |
|------|------|------|
| 支付运维 | `/admin/finance/payment-ops` | 渠道状态、证书缓存表、刷新按钮 |
| 询盘管理 | `/admin/inquiries` | 「导出改派审计」按钮 |

## 测试

```powershell
python -m pytest tests/unit/test_commercial_loop_p7.py -q
python -m pytest tests/unit/test_commercial_loop_p0.py ... test_commercial_loop_p7.py tests/unit/test_invoice_application_service.py -q
python scripts/smoke_commercial_loop.py --base-url http://127.0.0.1:8001
```

## 后续（P8）

- 改派审计按日期区间筛选（UI + API `start_at`/`end_at`）
- 支付运维：支付宝沙箱连通性探针一键触发
- 生产 staging：真实微信证书拉取 + notify 外网回调验收
