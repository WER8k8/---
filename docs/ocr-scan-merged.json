{
  "tool": "OpenCodeReview alibaba/open-code-review v1.12.5",
  "mode": "ocr scan LLM multi-batch",
  "provider_used": [
    "agnes"
  ],
  "models_used": [
    "agnes-2.5-flash",
    "agnes-3.0-flash"
  ],
  "runs": [
    {
      "file": "ocr-scan-critical-full.json",
      "exists": true,
      "status": "completed_with_errors",
      "llm": {
        "provider": "agnes",
        "model": "agnes-2.5-flash"
      },
      "summary": {
        "files_reviewed": 76,
        "comments": 86,
        "total_tokens": 835543,
        "input_tokens": 807307,
        "output_tokens": 28236,
        "cache_read_tokens": 575232,
        "elapsed": "13m11s",
        "budget_exceeded": true
      },
      "comment_count": 86,
      "warnings": [
        {
          "file": "backend/app/services/acquisition/onboarding.py",
          "message": "stopped in batch #0: used 789196 tokens + next-file estimate exceeds budget 800000",
          "type": "token_budget_reached"
        },
        {
          "file": "backend/app/services/acquisition/loss_report.py",
          "message": "main_task did not complete before stopping: reached the aggregate token budget before finishing",
          "type": "scan_subtask_error"
        },
        {
          "file": "backend/app/services/acquisition/nps_rescue.py",
          "message": "main_task did not complete before stopping: reached the aggregate token budget before finishing",
          "type": "scan_subtask_error"
        },
        {
          "file": "backend/app/api/v1/routes/acquisition.py",
          "message": "LLM completion error: context deadline exceeded",
          "type": "scan_subtask_error"
        }
      ],
      "session_id": "7e9bbc4c-98b6-4df0-95f0-1da87e04004e"
    },
    {
      "file": "ocr-scan-batch2.json",
      "exists": true,
      "status": "completed_with_errors",
      "llm": {
        "provider": "agnes",
        "model": "agnes-2.5-flash"
      },
      "summary": {
        "files_reviewed": 17,
        "comments": 66,
        "total_tokens": 565606,
        "input_tokens": 539680,
        "output_tokens": 25926,
        "cache_read_tokens": 293632,
        "elapsed": "5m39s",
        "budget_exceeded": true
      },
      "comment_count": 66,
      "warnings": [
        {
          "file": "backend/app/services/payment_pkg/payment_service_impl.py",
          "message": "stopped in batch #0: used 480799 tokens + next-file estimate exceeds budget 500000",
          "type": "token_budget_reached"
        },
        {
          "file": "backend/app/services/oauth_login.py",
          "message": "main_task did not complete before stopping: reached the aggregate token budget before finishing",
          "type": "scan_subtask_error"
        },
        {
          "file": "backend/app/services/hermes/harness_gateway.py",
          "message": "main_task did not complete before stopping: reached the aggregate token budget before finishing",
          "type": "scan_subtask_error"
        }
      ],
      "session_id": "9aa6c376-19f4-440b-8f4b-d7207f944361"
    },
    {
      "file": "ocr-scan-batch3.json",
      "exists": true,
      "status": "completed_with_errors",
      "llm": {
        "provider": "agnes",
        "model": "agnes-3.0-flash"
      },
      "summary": {
        "files_reviewed": 13,
        "comments": 17,
        "total_tokens": 700349,
        "input_tokens": 686432,
        "output_tokens": 13917,
        "cache_read_tokens": 282112,
        "elapsed": "8m1s",
        "budget_exceeded": true
      },
      "comment_count": 17,
      "warnings": [
        {
          "file": "backend/app/services/acquisition/experience_feed.py",
          "message": "main_task did not complete before stopping: reached the aggregate token budget before finishing",
          "type": "scan_subtask_error"
        },
        {
          "file": "backend/app/services/acquisition/payment_risk.py",
          "message": "stopped in batch #0: used 616193 tokens + next-file estimate exceeds budget 600000",
          "type": "token_budget_reached"
        },
        {
          "file": "backend/app/services/acquisition/onboarding.py",
          "message": "main_task did not complete before stopping: reached the aggregate token budget before finishing",
          "type": "scan_subtask_error"
        },
        {
          "file": "backend/app/services/acquisition/ops_card_pg.py",
          "message": "main_task did not complete before stopping: reached the aggregate token budget before finishing",
          "type": "scan_subtask_error"
        },
        {
          "file": "backend/app/api/v1/routes/acquisition.py",
          "message": "main_task did not complete before stopping: reached the aggregate token budget before finishing",
          "type": "scan_subtask_error"
        }
      ],
      "session_id": "8f6d36d7-62f7-4597-929d-b9aa07413c85"
    },
    {
      "file": "ocr-scan-batch4.json",
      "exists": true,
      "status": "success",
      "llm": {
        "provider": "agnes",
        "model": "agnes-2.5-flash"
      },
      "summary": {
        "files_reviewed": 2,
        "comments": 12,
        "total_tokens": 38593,
        "input_tokens": 33105,
        "output_tokens": 5488,
        "cache_read_tokens": 8192,
        "elapsed": "1m1s"
      },
      "comment_count": 12,
      "warnings": [],
      "session_id": "eb239c67-fa91-4240-8a66-d2db4e72f709"
    },
    {
      "file": "ocr-scan-batch5-payment.json",
      "exists": true,
      "status": "success",
      "llm": {
        "provider": "agnes",
        "model": "agnes-2.5-flash"
      },
      "summary": {
        "files_reviewed": 2,
        "comments": 13,
        "total_tokens": 84731,
        "input_tokens": 78721,
        "output_tokens": 6010,
        "cache_read_tokens": 52480,
        "elapsed": "4m34s"
      },
      "comment_count": 13,
      "warnings": [],
      "session_id": "d7b9bb0d-9ac4-4745-9618-d99793a018b9"
    },
    {
      "file": "ocr-scan-batch6-acq-rest.json",
      "exists": true,
      "status": "success",
      "llm": {
        "provider": "agnes",
        "model": "agnes-2.5-flash"
      },
      "summary": {
        "files_reviewed": 10,
        "comments": 47,
        "total_tokens": 199919,
        "input_tokens": 181353,
        "output_tokens": 18566,
        "cache_read_tokens": 63744,
        "elapsed": "2m48s"
      },
      "comment_count": 47,
      "warnings": [],
      "session_id": "7b746850-37cd-4fa4-a797-3b496284877e"
    },
    {
      "file": "ocr-scan-batch7-acq-route.json",
      "exists": true,
      "status": "success",
      "llm": {
        "provider": "agnes",
        "model": "agnes-2.5-flash"
      },
      "summary": {
        "files_reviewed": 1,
        "comments": 10,
        "total_tokens": 170400,
        "input_tokens": 166167,
        "output_tokens": 4233,
        "cache_read_tokens": 92672,
        "elapsed": "2m11s"
      },
      "comment_count": 10,
      "warnings": [],
      "session_id": "a8181451-c52c-4f51-b485-6e4e0b7a64b2"
    }
  ],
  "comment_count_raw": 251,
  "comment_count_unique": 251,
  "by_severity": {
    "critical": 8,
    "high": 60,
    "medium": 87,
    "low": 81,
    "unknown": 15
  },
  "by_category": {
    "security": 47,
    "bug": 89,
    "other": 21,
    "maintainability": 58,
    "performance": 17,
    "documentation": 9,
    "style": 3,
    "test": 1,
    "unknown": 6
  },
  "by_file": {
    "backend/app/services/acquisition/experience_feed.py": 17,
    "backend/app/api/v1/routes/acquisition.py": 15,
    "backend/app/services/oauth_login.py": 12,
    "backend/app/api/v1/routes/tenants.py": 10,
    "backend/app/services/acquisition/nps_rescue.py": 10,
    "backend/app/services/acquisition/ops_card_pg.py": 10,
    "backend/app/api/v1/routes/payment.py": 9,
    "backend/app/core/config.py": 9,
    "backend/app/api/v1/routes/auth.py": 8,
    "backend/app/core/login_bruteforce.py": 8,
    "backend/app/services/acquisition/__init__.py": 8,
    "backend/app/services/acquisition/suppression_list.py": 8,
    "backend/app/services/adapters/tradeai/__init__.py": 8,
    "backend/app/services/hermes/harness_gateway.py": 8,
    "backend/app/services/acquisition/dispatch_service.py": 7,
    "backend/app/services/hermes/task_control_supervisor.py": 7,
    "backend/app/services/payment_pkg/payment_service_impl.py": 7,
    "backend/app/core/admin_auth.py": 6,
    "backend/app/core/database.py": 6,
    "backend/app/services/payment_service.py": 6,
    "backend/app/services/acquisition/onboarding.py": 6,
    "backend/app/services/acquisition/tender_engine.py": 6,
    "backend/app/services/acquisition/baseline_bench.py": 5,
    "backend/app/services/acquisition/billing_explain.py": 5,
    "backend/app/services/acquisition/growth_ops.py": 5,
    "backend/app/services/acquisition/knowledge_queue.py": 5,
    "backend/app/services/acquisition/payment_risk.py": 5,
    "backend/app/services/goodjob/customer_pool_projection.py": 5,
    "backend/app/services/acquisition/intent_classifier.py": 5,
    "backend/app/services/acquisition/fulfillment_nodes.py": 4,
    "backend/app/services/acquisition/loss_report.py": 4,
    "backend/app/services/acquisition/template_weights.py": 4,
    "backend/app/services/acquisition/sales_collision.py": 3,
    "backend/app/services/acquisition/sample_flow.py": 3,
    "backend/app/services/goodjob/trade_document_bridge.py": 3,
    "backend/app/services/acquisition/quote_guard.py": 2,
    "backend/app/services/adapters/__init__.py": 1,
    "backend/app/services/goodjob/__init__.py": 1
  },
  "incomplete_warnings": [
    "backend/app/services/acquisition/onboarding.py · token_budget_reached · stopped in batch #0: used 789196 tokens + next-file estimate exceeds budget 800000",
    "backend/app/services/acquisition/loss_report.py · scan_subtask_error · main_task did not complete before stopping: reached the aggregate token budget before finishing",
    "backend/app/services/acquisition/nps_rescue.py · scan_subtask_error · main_task did not complete before stopping: reached the aggregate token budget before finishing",
    "backend/app/api/v1/routes/acquisition.py · scan_subtask_error · LLM completion error: context deadline exceeded",
    "backend/app/services/payment_pkg/payment_service_impl.py · token_budget_reached · stopped in batch #0: used 480799 tokens + next-file estimate exceeds budget 500000",
    "backend/app/services/oauth_login.py · scan_subtask_error · main_task did not complete before stopping: reached the aggregate token budget before finishing",
    "backend/app/services/hermes/harness_gateway.py · scan_subtask_error · main_task did not complete before stopping: reached the aggregate token budget before finishing",
    "backend/app/services/acquisition/experience_feed.py · scan_subtask_error · main_task did not complete before stopping: reached the aggregate token budget before finishing",
    "backend/app/services/acquisition/payment_risk.py · token_budget_reached · stopped in batch #0: used 616193 tokens + next-file estimate exceeds budget 600000",
    "backend/app/services/acquisition/onboarding.py · scan_subtask_error · main_task did not complete before stopping: reached the aggregate token budget before finishing",
    "backend/app/services/acquisition/ops_card_pg.py · scan_subtask_error · main_task did not complete before stopping: reached the aggregate token budget before finishing",
    "backend/app/api/v1/routes/acquisition.py · scan_subtask_error · main_task did not complete before stopping: reached the aggregate token budget before finishing"
  ],
  "project_summaries": [
    "## 来自 ocr-scan-critical-full.json\n\n### Top Issues\n\n1.  **Silent Error Swallowing Across Acquisition Services**\n    Broad `except Exception: pass` or silent `except Exception:` blocks appear in `dispatch_service.py`, `nps_rescue.py`, `__init__.py`, `billing_explain.py`, and `baseline_bench.py`. These hide configuration failures, missing dependencies (e.g., `ops_card_pg`), and data integrity errors, making production diagnosis nearly impossible.\n\n2.  **Inconsistent Time Semantics in Bruteforce Protection**\n    `login_bruteforce.py` mixes `time.time()` (wall-clock) and `time.monotonic()` (clock-speed). This causes Redis-backed and memory-backed locks to expire at different real-world moments, leading to unpredictable lock durations and potential race conditions during failover.\n\n3.  **IP Extraction Vulnerability via X-Forwarded-For**\n    `client_ip_from_request` trusts the first IP in `X-Forwarded-For` without validating proxy chain legitimacy. Attackers can spoof client IPs (e.g., `8.8.8.8`), bypassing IP-based bruteforce locks and identity restrictions.\n\n4.  **Cross-Tenant Data Leak in Billing/Acquisition Queries**\n    `billing_explain.py` queries acquisition events without filtering by `tenant_id`/`tid`. This exposes all tenants' activity to any user, constituting a critical security and compliance violation in multi-tenancy contexts.\n\n5.  **Broken Read-Write Split After Engine Rebind**\n    `database.py`'s `rebind_engine()` recreates the main engine but leaves `read_engine` stale. Subsequent read operations will hit the old, potentially disconnected or outdated database instance, breaking data consistency.\n\n6.  **Shared Mutable State in Multi-Threaded Acquisition Modules**\n    `growth_ops.py`, `knowledge_queue.py`, and `__init__.py` rely on module-level singletons (`content_attr_store`, `knowledge_queue_store`, `buyer_store`) without synchronization. In ASGI environments with multiple workers/threads, this leads to data corruption, race conditions, and lost updates.\n\n7.  **Missing Tenant Isolation in Knowledge Queue**\n    `knowledge_queue.py` accepts `tenant_id` parameters in `list()` and `report()` but ignores them, returning all items. This is a latent data leak if multi-tenancy is ever enabled.\n\n8.  **Incomplete Production Key Validation**\n    `config.py`'s `_init_production_key_validation()` checks only a subset of secrets (JWT, Redis, DB) while missing AI provider keys (OpenAI, Anthropic). Compromised env vars for these missing keys go undetected.\n\n### Module Hotspots\n\n*   **`backend/app/services/acquisition/`**: High density of issues across 8+ files. Dominated by silent error handling, missing tenant filters, race conditions on shared state, and business logic gaps (e.g., `loss_report.py`, `dispatch_service.py`, `nps_rescue.py`).\n*   **`backend/app/core/database.py`**: Critical concurrency and lifecycle bugs (stale engines, unsafe stack assumptions, logger usage before definition).\n*   **`backend/app/core/login_bruteforce.py`**: Security-relevant time inconsistency and IP spoofing vulnerabilities.\n*   **`backend/app/core/config.py`**: Structural maintainability issues and incomplete security validation.\n\n### Cross-Cutting Concerns\n\n*   **Bare/ Silent Exception Handling**: Found in `database.py`, `login_bruteforce.py` (Lua fallbacks), `baseline_bench.py`, `billing_explain.py`, `dispatch_service.py`, `nps_rescue.py`, and `__init__.py`. The pattern is consistent: broad catches with no logging or re-raising, obscuring root causes.\n*   **Missing Tenant Context Propagation**: `billing_explain.py` and `knowledge_queue.py` fail to enforce tenant boundaries in queries or method signatures, risking data leakage.\n*   **Inconsistent Input Validation**: `intent_classifier.py` has inconsistent keyword padding ('t/t' vs 'tt '), `experience_feed.py` has inconsistent currency fallbacks, and `login_bruteforce.py` doesn't validate empty usernames in key generation.\n*   **Mutable Default Arguments / Side Effects**: `experience_feed.py` warns about potential mutable default list risks; `__init__.py` mutates `buyer_grade_reason` during read operations (`score_display`), causing persistent side effects.\n\n### Quick Wins\n\n1.  **Add Logging to Silent Catches**: In `billing_explain.py` (lines 88, 107) and `dispatch_service.py` (lines 36, 105), replace silent `except` blocks with `logger.warning` or `logger.exception` to surface failures.\n2.  **Standardize Time Sources**: Refactor `login_bruteforce.py` to use `_now_mono()` (`time.monotonic()`) exclusively for all lock expiration calculations to ensure consistent semantics across Redis and memory stores.\n3.  **Validate X-Forwarded-For Chain**: Update `client_ip_from_request` in `login_bruteforce.py` to validate that forwarded IPs belong to trusted proxy ranges before trusting them.\n4.  **Fix Tenant Filter in Billing**: Add `WHERE tid = :tenant_id` to the acquisition events query in `billing_explain.py`.\n5.  **Update Read Engine on Rebind**: In `database.py`, ensure `rebind_engine()` also updates `read_engine` to prevent stale database connections.\n6.  **Remove Dead Code**: Delete the redundant `if key[1]:` check in `acquisition/__init__.py` (line 140) which is unreachable.\n7.  **Validate Config File Existence**: In `config.py`, add a check to ensure files in `_env_file` actually exist before passing them to pydantic-settings, preventing silent ignore of misconfigured paths.",
    "## 来自 ocr-scan-batch2.json\n\n### Top Issues\n\n1.  **Payment Webhook Security & Integrity Failures** (`backend/app/api/v1/routes/payment.py`)\n    *   The Stripe webhook handler accepts `checkout.session.completed` events without verifying the actual `payment_status`, allowing potential spoofing.\n    *   A broad `try/except Exception` swallows signature verification failures and metadata errors, returning 500s that trigger aggressive Stripe retries instead of appropriate 403/400 responses.\n    *   Balance payment logic lacks compensation guarantees: if provisioning fails after wallet debit, the refund path itself may fail, leaving the wallet debited without service provisioned.\n\n2.  **Authentication Bypass & Token Revocation Failure** (`backend/app/core/admin_auth.py`, `backend/app/api/v1/routes/auth.py`)\n    *   `jwt.decode` is called with `verify_exp=False`, meaning revoked/expired tokens bypass blacklist checks indefinitely if leaked.\n    *   Dev auto-bind logic allows any `dev_`-prefixed OAuth identity to bind to admin accounts without password/2FA; leakage of this flag into production is a critical risk.\n    *   Bare `except` blocks in `_get_token_jti` and auth routes swallow `JWTDecodeError` and other authentication errors, making it impossible to distinguish between valid tokens without JTI and malformed tokens.\n\n3.  **Tenant Data Integrity & Validation Gaps** (`backend/app/api/v1/routes/tenants.py`)\n    *   Domain verification is faked: GET/POST endpoints hardcode `verifyStatus: \"verified\"` and `sslStatus: \"active\"` without actual DNS/SSL checks, misleading the frontend.\n    *   Tenant settings updates accept arbitrary JSON without schema validation, allowing clients to inject malicious or corrupt configuration blobs.\n    *   IP resolution trusts the first entry in `X-Forwarded-For` without validation, vulnerable to header injection in non-trusted proxy environments.\n    *   Fallback query when no active `UserTenant` link exists returns the first tenant in the DB (`db.query(Tenant).first()`), breaking multi-tenant isolation.\n\n4.  **Hermes Task Supervisor Concurrency & State Bugs** (`backend/app/services/hermes/task_control_supervisor.py`)\n    *   Recursive DAG validation (`dfs`) can hit Python’s recursion limit on deep graphs, causing unhandled crashes instead of clean validation failures.\n    *   Compensation logic leaves dependent nodes (completed after a skipped node) in `done` status without triggering their `compensation_action`, resulting in inconsistent system state.\n    *   Broad `except Exception` blocks silently reset `budget_used` to 0.0 on parse errors, disabling budget gates and allowing unbounded spending.\n    *   Race condition in `advance_plan`: between reading `running_now` and dispatching tasks, another process may dispatch additional tasks, violating batch isolation.\n\n5.  **TradeAI Adapter Global State & Isolation Risks** (`backend/app/services/adapters/tradeai/__init__.py`)\n    *   Module-level mutable global `_tenant_orchestrators` is accessed without thread synchronization, causing race conditions in multi-threaded worker environments.\n    *   Import-time execution (`_loaded = _load_vendor_isolated()`) creates a race condition if the module is imported concurrently by multiple workers.\n    *   `SECRET_KEY` fallback logic is broken: if `SECRET_KEY` is already set in the parent environment, the dedicated `TRADEAI_SECRET_KEY` is ignored, potentially weakening isolation.\n    *   Bare `except Exception` in `_load_vendor_isolated` masks errors during module restore in the `finally` block, potentially leaving the application in a partially corrupted state.\n\n6.  **Auth Route Race Conditions & Undefined Variables** (`backend/app/api/v1/routes/auth.py`)\n    *   Undefined variable `count` in the rate-limit fallback path causes an `UnboundLocalError` whenever Redis is unavailable, breaking authentication entirely during outages.\n    *   In-memory email code storage (`_email_code_store`, `_email_code_ttl_store`) uses mutable globals without thread safety, leading to data corruption under concurrent requests.\n    *   Check-then-act race condition in email verification: concurrent requests with the same valid code can bypass the `used=True` check, allowing multiple logins.\n\n7.  **PII Leakage in Job Projections** (`backend/app/services/goodjob/customer_pool_projection.py`)\n    *   Projection payloads contain raw PII (email, phone, name) without masking, which will be stored in task queues and potentially logged by worker infrastructure, violating data protection requirements.\n\n8.  **OAuth Implementation Flaws** (`backend/app/services/oauth_login.py`)\n    *   `verify_wechat_signature` incorrectly reads `FEISHU_VERIFICATION_TOKEN` instead of a WeChat-specific token, causing signature verification to always fail or use wrong credentials.\n    *   QQ OAuth token exchange uses GET with `client_secret` in query parameters, leaking secrets in server logs and browser history; should use POST with body credentials.\n    *   WeChat provider ID generation prioritizes `unionid` over `openid`; if `unionid` becomes unavailable, the same user gets a different provider ID, causing duplicate accounts.\n\n---\n\n### Module Hotspots\n\n*   **`backend/app/api/v1/routes/payment.py`**: High severity issues集中在 webhook security, idempotency, and error handling. 9 distinct comments covering security, concurrency, and correctness.\n*   **`backend/app/core/admin_auth.py`**: Critical security flaws in JWT verification and token revocation. 6 comments highlighting authentication bypass risks.\n*   **`backend/app/services/adapters/tradeai/__init__.py`**: Concurrency and isolation vulnerabilities in a core adapter module. 7 comments focusing on thread safety and error handling.\n*   **`backend/app/api/v1/routes/auth.py`**: Race conditions and undefined variables in authentication flows. 6 comments highlighting availability and correctness issues.\n*   **`backend/app/api/v1/routes/tenants.py`**: Data integrity and validation gaps in tenant management. 7 comments covering security, validation, and multi-tenant isolation.\n*   **`backend/app/services/hermes/task_control_supervisor.py`**: Concurrency bugs and state management issues in task orchestration. 7 comments focusing on race conditions and error swallowing.\n\n---\n\n### Cross-Cutting Concerns\n\n*   **Bare `except Exception` Swallowing Critical Errors**\n    *   Patterns found in: `payment.py`, `admin_auth.py`, `tradeai/__init__.py`, `auth.py`, `tenants.py`, `task_control_supervisor.py`\n    *   Consistently masks configuration errors, data integrity issues, and security failures, making debugging difficult and hiding real problems.\n\n*   **Missing Thread Synchronization for Shared State**\n    *   Affected: `tradeai/__init__.py` (_tenant_orchestrators), `auth.py` (_email_code_store), `tenants.py` (_register_rate), `admin_auth.py` (Redis cache)\n    *   Multiple modules use mutable globals or check-then-act patterns without locks, leading to race conditions in multi-worker deployments.\n\n*   **Inconsistent Error Handling & Response Codes**\n    *   Affected: `payment.py`, `auth.py`, `task_control_supervisor.py`\n    *   Signature failures return 500 instead of 403; validation errors are swallowed; API response models are inconsistently declared, breaking OpenAPI schemas.\n\n*   **Insufficient Input Validation**\n    *   Affected: `tenants.py` (tenant settings, XFF headers), `customer_pool_projection.py` (tenant_id whitespace), `trade_document_bridge.py` (step_number)\n    *   Missing schema validation, trust of proxied headers without verification, and lack of boundary checks on numeric inputs.\n\n*   **Idempotency Key Collisions**\n    *   Affected: `customer_pool_projection.py`, `trade_document_bridge.py`\n    *   Both modules truncate idempotency keys to 128 characters using simple slicing, risking collisions between distinct payloads.\n\n---\n\n### Quick Wins\n\n1.  **Fix `verify_wechat_signature` token mismatch** (`backend/app/services/oauth_login.py`): Change to use WeChat-specific verification token instead of `FEISHU_VERIFICATION_TOKEN`.\n2.  **Define `count` in auth rate-limit fallback** (`backend/app/api/v1/routes/auth.py`): Ensure `count` is initialized before the fallback path to prevent `UnboundLocalError` when Redis is unavailable.\n3.  **Add thread lock to `_register_rate`** (`backend/app/api/v1/routes/tenants.py`): Wrap the read-modify-write sequence in a lock to prevent race conditions during concurrent registrations.\n4.  **Validate `step_number` in trade document bridge** (`backend/app/services/goodjob/trade_document_bridge.py`): Add check for `step_number < 1` and raise `ValueError` with clear message.\n5.  **Sanitize raw input in harness gateway** (`backend/app/services/hermes/harness_gateway.py`): Replace `raw_text` in event payload with sanitized/redacted version to prevent PII leakage in logs.\n6.  **Standardize Stripe webhook error handling** (`backend/app/api/v1/routes/payment.py`): Return 403 for signature failures, 400 for missing metadata, and only 500 for unexpected internal errors to stop aggressive retry loops.\n7.  **Add tenant_id whitespace stripping** (`backend/app/services/goodjob/customer_pool_projection.py`): Apply `.strip()` to tenant_id and reject if empty after stripping to prevent isolation bypasses.\n8.  **Fix OAuth token exchange method** (`backend/app/services/oauth_login.py`): Change QQ token exchange from GET with query params to POST with credentials in request body.",
    "## 来自 ocr-scan-batch3.json\n\n### Top Issues\n\n1. **Silent Error Swallowing & Data Loss in `nps_rescue.py`**\n   - Critical bugs in `backend/app/services/acquisition/nps_rescue.py`:\n     - Negative NPS scores are coerced to `0` (detractor) instead of being handled gracefully or rejected.\n     - A missing parenthesis in a conditional expression (`sum(...) or getattr(...)`) breaks the logic flow, likely causing incorrect card enumeration or state updates.\n     - `except Exception: pass` blocks silently discard errors when loading `_by_inquiry` and card data, making it indistinguishable between \"no data\" and \"failed to load.\"\n   - **Impact:** High. Leads to incorrect rescue state, silent failures, and inability to debug production issues.\n\n2. **Broken Ternary Logic & Data Overwrite in `routes/acquisition.py`**\n   - `backend/app/api/v1/routes/acquisition.py`:\n     - The expression `if (out.get(\"ok\") and body.inquiry_id)` is parsed incorrectly due to operator precedence, and `body.inquiry_id` does not exist on `TenderAdvanceRequest`, rendering the logic broken.\n     - `card.risk_flags = list(body.risk_flags or [])` overwrites existing risk flags instead of merging, causing data loss (e.g., losing sanctions watch flags).\n     - Duplicate route registration for `@router.get(\"/ops/reconcile\")` means the first definition is unreachable.\n   - **Impact:** High. API endpoints behave incorrectly, potential security/compliance data loss, and maintenance confusion due to dead code.\n\n3. **Schema Drift & Fragile Dataclass Construction in `ops_card_pg.py`**\n   - `backend/app/services/acquisition/ops_card_pg.py`:\n     - Using `**` unpacking from JSON/DB rows into dataclasses (`OpsCardLogistics`, etc.) will raise `TypeError` if the schema drifts (old rows have new fields).\n     - Redundant `CREATE TABLE IF NOT EXISTS` on every save adds unnecessary DB round-trips.\n     - The update wrapper silently swallows exceptions from `save_ops_card`, contradicting the module's \"fail honestly\" principle.\n   - **Impact:** Medium-High. System fragility against schema changes, performance degradation, and silent persistence failures.\n\n4. **Ambiguous Tenant Filtering in `nps_rescue.py`**\n   - `backend/app/services/acquisition/nps_rescue.py`:\n     - The condition `getattr(c, \"tenant_id\", \"\") in (\"\", tenant_id)` treats unassigned cards (empty tenant) as valid matches for *any* specific tenant ID.\n   - **Impact:** High. Potential cross-tenant data leakage or incorrect aggregation in multi-tenant environments.\n\n5. **Mutable Global State in `onboarding.py`**\n   - `backend/app/services/acquisition/onboarding.py`:\n     - `STEPS` is a module-level mutable list of mutable dicts. Importing this allows external code to mutate shared state, affecting all other consumers.\n   - **Impact:** Medium. Subtle bugs and side effects across the application if any module modifies `STEPS`.\n\n### Module Hotspots\n\n*   **`backend/app/services/acquisition/`**: This module has the highest density of critical issues, including silent error handling, broken logic in `nps_rescue.py`, fragile persistence in `ops_card_pg.py`, and mutable global state in `onboarding.py`.\n*   **`backend/app/api/v1/routes/acquisition.py`**: Contains critical API logic errors, including unreachable routes, broken ternaries, and data loss due to overwriting collections.\n\n### Cross-Cutting Concerns\n\n*   **Silent Error Handling (`except: pass`)**: Appears in `nps_rescue.py` and `ops_card_pg.py`. This anti-pattern hides failures, making production debugging extremely difficult and leading to silent data corruption or missing state.\n*   **Unvalidated/Dangerous Data Coercion**: \n    - `nps_rescue.py`: Coerces negative NPS to 0; crashes on non-numeric onboarding view strings.\n    - `ops_card_pg.py`: Blindly unpacks DB/JSON data into dataclasses without schema validation.\n*   **Inconsistent State Mutation**: `routes/acquisition.py` overwrites `risk_flags` instead of merging; `experience_feed.py` includes cards in both \"won\" and \"lost\" categories if `loss_reasons` is not cleared on win.\n\n### Quick Wins\n\n1.  **Remove `except: pass` blocks**: In `nps_rescue.py` and `ops_card_pg.py`, replace with specific exception logging or re-raising to preserve diagnostic information.\n2.  **Fix Tenant Filtering**: In `nps_rescue.py`, exclude empty tenant IDs from the match tuple for specific tenants (e.g., `if tenant_id and getattr(c, \"tenant_id\", \"\") == tenant_id`).\n3.  **Correct API Logic**: In `routes/acquisition.py`, remove the duplicate `/ops/reconcile` route and fix the broken ternary for `inquiry_id` (which should likely be `body.get(\"inquiry_id\")` or similar, depending on the model).\n4.  **Prevent Data Loss**: In `routes/acquisition.py`, change `card.risk_flags = ...` to a merge operation (e.g., `card.risk_flags = list(set(card.risk_flags or []) | set(body.risk_flags or []))`).\n5.  **Immutability**: In `onboarding.py`, convert `STEPS` to a frozen dataclass structure or a tuple of tuples to prevent accidental mutation.",
    "## 来自 ocr-scan-batch4.json\n\n### Top Issues\n\n1.  **WeChat Signature Verification Uses Feishu Token** (`backend/app/services/oauth_login.py`)\n    `verify_wechat_signature` references `settings.FEISHU_VERIFICATION_TOKEN` instead of a WeChat-specific setting. This is likely a copy-paste error that would cause WeChat signature verification to fail or accept invalid signatures if tokens overlap.\n\n2.  **QQ Response Parser Regex Greedy Over-match** (`backend/app/services/oauth_login.py`)\n    The regex `^\\s*\\w+\\s*\\(\\s*(.+)\\s*\\)\\s*;?\\s*$` with `re.DOTALL` uses a greedy `.+` that can over-match when JSON bodies contain balanced parentheses (e.g., nested values like `\"(val)\"`). This could corrupt response parsing.\n\n3.  **Short UUID Generation Risking Event ID Collisions** (`backend/app/services/hermes/harness_gateway.py`, lines 46-47)\n    `event_id` is generated by truncating a UUID to 12 hex characters (48 bits). In high-throughput, multi-worker deployments, collision probability becomes non-negligible (~0.03% at 1M events), potentially causing event deduplication failures or data corruption.\n\n4.  **PII Leakage via OpenID Prefix Logging** (`backend/app/services/oauth_login.py`, lines 297, 327, 368, ~413)\n    Logs emit `openid[:8]`, leaking the first 8 characters of users' persistent, globally unique OpenIDs (QQ and WeChat). While not a full secret, this constitutes PII exposure and violates privacy best practices.\n\n5.  **Dev Mode OAuth Code Exposure in URLs** (`backend/app/services/oauth_login.py`)\n    The `_dev_authorize_url` function exposes the OAuth `code` in the redirect URL query string. This code may be logged in server access logs or browser history, creating a security risk even in non-production environments.\n\n6.  **Async Docstring Misleading Sync Callers** (`backend/app/services/hermes/harness_gateway.py`)\n    `process_inbound_webhook` is documented as a \"synchronous entry-point wrapper\" but is defined as an async function. Calling it without `await` or `asyncio.run()` from a sync context will raise a `TypeError`.\n\n### Module Hotspots\n\n-   **`backend/app/services/oauth_login.py`**: Contains 6 comments covering critical bugs (signature verification, regex parsing), security issues (PII logging, dev mode exposure), dead code, and style problems. This is the highest-density hotspot.\n-   **`backend/app/services/hermes/harness_gateway.py`**: Contains 6 comments focusing on type safety, PII exposure risks, event ID generation quality, and documentation accuracy.\n\n### Cross-Cutting Concerns\n\n-   **PII/Privacy Exposure**: Both files exhibit patterns where sensitive user data (OpenID prefixes, raw user input in `IntentEvent`) is logged or exposed without adequate masking. Representative paths: `oauth_login.py` (lines 297, 327, 368, ~413) and `harness_gateway.py` (lines 56-60).\n-   **Inconsistent/Error-Prone OAuth Implementation**: The OAuth login service has multiple implementation flaws: incorrect token reference for WeChat, fragile regex parsing for QQ, and insecure dev-mode URL handling. Path: `backend/app/services/oauth_login.py`.\n-   **Type and Validation Gaps**: `harness_gateway.py` shows insufficient validation of nested payload types and silent coercion of non-string intent values to strings, risking downstream failures. Paths: `harness_gateway.py` (lines 46-47, 56-60, 92-94, 95).\n\n### Quick Wins\n\n-   **Fix WeChat Token Reference** (`oauth_login.py`): Replace `settings.FEISHU_VERIFICATION_TOKEN` with the correct WeChat verification token setting.\n-   **Fix QQ Regex Greediness** (`oauth_login.py`): Change the regex to use a non-greedy quantifier (`.+?`) or a more precise pattern that avoids over-matching on nested parentheses.\n-   **Remove/Mask OpenID Prefixes in Logs** (`oauth_login.py`): Stop logging `openid[:8]` or replace with a non-identifying hash/token.\n-   **Generate Full UUIDs for Event IDs** (`harness_gateway.py`, line 46-47): Use the full UUID hex string instead of truncating to 12 characters to eliminate collision risk.\n-   **Correct Async Docstring** (`harness_gateway.py`): Update the docstring for `process_inbound_webhook` to accurately reflect its async nature.\n-   **Add Nested Payload Validation** (`harness_gateway.py`, lines 92-94): Extend `isinstance` checks to validate nested dictionary values or add schema validation to catch type mismatches earlier.",
    "## 来自 ocr-scan-batch5-payment.json\n\n### Top Issues\n\n1.  **Duplicate Subscriptions and Revenue via TOCTOU Races**\n    *Root Cause*: Both `process_wechat_notify`/`process_alipay_notify` and `_mark_paid_and_provision` perform check-then-act operations without database locks.\n    *Impact*: Concurrent webhook retries from payment gateways can pass the `status=pending` guard multiple times, leading to duplicate benefit provisioning, double revenue recording, and corrupted subscription states.\n    *Paths*: `backend/app/services/payment_service.py`, `backend/app/services/payment_pkg/payment_service_impl.py`\n\n2.  **Webhook Signature Confusion Attack**\n    *Root Cause*: HMAC-SHA256 payload format uses a colon separator (`f\"{order_no}:{amount}\"`).\n    *Impact*: If `order_no` contains a colon (e.g., `A:B`), an attacker could craft a payload where the intended amount shifts into the order_no field, potentially bypassing signature verification or altering the interpreted amount.\n    *Path*: `backend/app/services/payment_service.py`\n\n3.  **Indefinite Order Pending State (Silent Failure)**\n    *Root Cause*: The Alipay notify handler logs an amount mismatch but returns `None` instead of success or an explicit error code.\n    *Impact*: The payment gateway interprets the lack of success response as a failure and may not retry appropriately, or the order remains stuck in a `pending` state indefinitely because the system neither acknowledges nor explicitly rejects the notification.\n    *Path*: `backend/app/services/payment_service.py`\n\n4.  **Cascading Failure in Compensation Scheduling**\n    *Root Cause*: `_schedule_compensation` references an undefined variable `data`.\n    *Impact*: Any attempt to schedule compensation raises a `NameError`, which is caught by an exception handler that likely fails due to the same missing context, resulting in silent loss of compensation logic and potential data inconsistency.\n    *Path*: `backend/app/services/payment_service.py`\n\n5.  **Subscription-Order Transactional Inconsistency**\n    *Root Cause*: `create_native_payment` creates/updates a subscription record *before* creating the payment order.\n    *Impact*: If the payment order creation fails (e.g., DB error), the subscription is left in an inconsistent state (created but no payment order exists), requiring complex manual rollback logic that may not cover all edge cases.\n    *Path*: `backend/app/services/payment_service.py`\n\n### Module Hotspots\n\n*   **`backend/app/services/payment_pkg/payment_service_impl.py`**: High density of critical issues including race conditions, manual transaction management errors, and input validation gaps.\n*   **`backend/app/services/payment_service.py`**: Central hub for security vulnerabilities (webhook verification) and critical bugs (undefined variables, silent failures).\n\n### Cross-Cutting Concerns\n\n*   **Inconsistent Error Handling in Payment Flows**:\n    Multiple methods (`create_native_payment`, `create_egress_addon_native`, `create_token_pack_native`) manually call `self.db.rollback()` in error branches. If the caller’s outer transaction expects to commit, this creates conflicting transaction states. Additionally, broad `except Exception` blocks (e.g., in `_record_revenue`) may mask `PendingRollbackError` or other session-level issues.\n    *Paths*: `backend/app/services/payment_pkg/payment_service_impl.py`\n\n*   **Lack of Atomicity in Critical Operations**:\n    Both the old (`payment_service.py`) and new (`payment_pkg/payment_service_impl.py`) implementations fail to use database-level locking or atomic updates for state transitions (pending -> paid), making them vulnerable to race conditions under concurrent webhook delivery.\n    *Paths*: `backend/app/services/payment_service.py`, `backend/app/services/payment_pkg/payment_service_impl.py`\n\n*   **Inconsistent Input/Channel Validation**:\n    Some methods enforce a whitelist for `pay_channel` (`wechat`, `alipay`), while others default silently to `\"wechat\"` without validation, creating injection vectors or unexpected behavior.\n    *Paths*: `backend/app/services/payment_pkg/payment_service_impl.py`\n\n*   **Secret Exposure Risk**:\n    The `PAYMENT_WEBHOOK_SECRET` is used in HMAC computation. There are concerns about it being logged or included in stack traces, especially given the inconsistent error handling that might expose internal state.\n    *Path*: `backend/app/services/payment_pkg/payment_service_impl.py`\n\n### Quick Wins\n\n1.  **Remove Redundant Imports**: Eliminate inline imports of `hashlib`, `hmac`, and `os` in `verify_notify` and other methods, as they are already imported at the module level. This improves code clarity.\n    *Path*: `backend/app/services/payment_service.py`\n\n2.  **Fix Alipay Handler Return Value**: Change the Alipay notify handler to return a success response (as per Alipay docs) even on amount mismatch, or explicitly return an error code that triggers gateway retry, rather than returning `None` which leaves orders pending.\n    *Path*: `backend/app/services/payment_service.py`\n\n3.  **Repair Undefined Variable**: Define or pass the `data` variable correctly in `_schedule_compensation` to prevent `NameError`.\n    *Path*: `backend/app/services/payment_service.py`\n\n4.  **Standardize Channel Validation**: Enforce a consistent whitelist check for `pay_channel` at all entry points, removing silent defaults.\n    *Path*: `backend/app/services/payment_pkg/payment_service_impl.py`\n\n5.  **Secure Webhook Signature Format**: Replace the colon-separated HMAC payload with a more robust format (e.g., JSON serialization or length-prefixed fields) to prevent signature confusion attacks.\n    *Path*: `backend/app/services/payment_service.py`",
    "## 来自 ocr-scan-batch6-acq-rest.json\n\n### Top Issues\n\n1.  **Data Integrity & Concurrency Races**\n    Critical race conditions in `tender_engine.py` (check-then-act on stage transitions) and `sales_collision.py` (non-atomic claim/handoff). In `ops_card_pg.py`, concurrent cache restores risk overwriting newer in-memory state with stale persistence data due to missing version checks.\n    *   *Paths:* `backend/app/services/acquisition/tender_engine.py`, `backend/app/services/acquisition/sales_collision.py`, `backend/app/services/acquisition/ops_card_pg.py`\n\n2.  **Tenant Isolation Violations**\n    `ops_card_pg.py` loads cards without filtering by `tenant_id`, risking cross-tenant data leakage if `inquiry_id` collides. `onboarding.py` silently falls back to a hardcoded `\"demo\"` tenant when arguments are missing, potentially filtering cards against the wrong tenant context in production.\n    *   *Paths:* `backend/app/services/acquisition/ops_card_pg.py`, `backend/app/services/acquisition/onboarding.py`\n\n3.  **Silent Error Swallowing (Debugging Black Holes)**\n    Widespread use of bare `except Exception: pass` blocks prevents debugging of store failures, key errors, and type mismatches. This appears in `onboarding.py`, `payment_risk.py`, `template_weights.py`, and `ops_card_pg.py`. `experience_feed.py` also truncates stack traces to 500 chars, losing critical diagnostic context.\n    *   *Paths:* `backend/app/services/acquisition/onboarding.py`, `backend/app/services/acquisition/payment_risk.py`, `backend/app/services/acquisition/template_weights.py`\n\n4.  **State Corruption via Mutable Globals**\n    Several modules expose or mutate module-level singletons without synchronization: `STEPS` list in `onboarding.py`, `_approved` dict in `template_weights.py`, and `suppression_store` in `suppression_list.py`. These are vulnerable to concurrent corruption and make unit testing difficult due to lack of isolation/cleanup.\n    *   *Paths:* `backend/app/services/acquisition/onboarding.py`, `backend/app/services/acquisition/template_weights.py`, `backend/app/services/acquisition/suppression_list.py`\n\n5.  **Logic Errors in Core State Machines**\n    `sample_flow.py` contains invalid transition paths (self-loops bypassing validation) and references undefined status values (`unbilled`, `billed`) not present in `SAMPLE_STATUSES`. `tender_engine.py` allows arbitrary stage jumps to `won`/`lost` from initial stages, bypassing qualification gates.\n    *   *Paths:* `backend/app/services/acquisition/sample_flow.py`, `backend/app/services/acquisition/tender_engine.py`\n\n6.  **Incomplete Data Validation**\n    `quote_guard.py` fails to enforce timezone awareness on naive datetime inputs, leading to runtime errors. `tender_engine.py` accepts negative or unvalidated amounts. `suppression_list.py` uses permissive email validation (`'@'` check only) and lacks input bounds for `tenant_id`.\n    *   *Paths:* `backend/app/services/acquisition/quote_guard.py`, `backend/app/services/acquisition/tender_engine.py`, `backend/app/services/acquisition/suppression_list.py`\n\n7.  **Crash Risks on Empty Data**\n    `template_weights.py` calls `max()` on an empty sequence when there are zero loss records, causing an unhandled `ValueError`. The ternary default only affects the assignment, not the `max()` call itself.\n    *   *Paths:* `backend/app/services/acquisition/template_weights.py`\n\n### Module Hotspots\n\n*   **`backend/app/services/acquisition/`**\n    This directory contains all reviewed files and exhibits high density of concurrency issues, silent error handling, and state management flaws. Specific concentration in:\n    *   `onboarding.py`: Tenant fallback and mutable global risks.\n    *   `tender_engine.py`: Race conditions and logic bypasses.\n    *   `template_weights.py`: Empty sequence crashes and unprotected globals.\n    *   `suppression_list.py`: Thread-safety and persistence gaps.\n\n### Cross-Cutting Concerns\n\n*   **Unsafe Exception Handling**: Consistent pattern of `except Exception: pass` or overly broad catches in `onboarding.py`, `payment_risk.py`, `template_weights.py`, and `ops_card_pg.py`.\n*   **Thread-Safety Neglect**: Multiple modules (`tender_engine.py`, `suppression_list.py`, `template_weights.py`, `sales_collision.py`) operate on shared mutable state without locks, posing significant risks in async or multi-worker deployments.\n*   **Missing Tenant Context Enforcement**: Aside from the explicit bug in `ops_card_pg.py`, `onboarding.py` has implicit fallbacks that compromise tenant isolation.\n*   **Inconsistent Audit/History Schemas**: `sales_collision.py` returns inconsistently shaped audit records (`claimed_new` vs. `handoff_history`), making downstream consumption fragile.\n\n### Quick Wins\n\n1.  **Fix `template_weights.py` Empty Sequence Crash**: Add a guard clause before `max(loss_reason_counts.items(), ...)` to return a default or empty dict when the list is empty.\n2.  **Replace Bare `except` Blocks**: Add logging or re-raise specific exceptions in `onboarding.py` (line 56-64), `payment_risk.py` (lines 82-83), and `template_weights.py` (lines 49-58).\n3.  **Add Tenant Filter to `ops_card_pg.py`**: Ensure `load_ops_card` includes `WHERE tenant_id = :tenant_id` to prevent cross-tenant data exposure.\n4.  **Simplify `payment_risk.py` Logic**: Replace the complex conditional `auto_pi_allowed = False if auto_pi else True` with `not auto_pi`.\n5.  **Validate `tender_engine.py` Stage Transitions**: Restrict `won`/`lost` assignments to only come from valid preceding stages (e.g., `negotiation`) rather than allowing arbitrary jumps.\n6.  **Add Locking or Atomic Operations**: Implement simple locking in `suppression_list.py` and `sales_collision.py` for check-then-act sequences."
  ],
  "comments": [
    {
      "path": "backend/app/api/v1/routes/auth.py",
      "start_line": 101,
      "end_line": 104,
      "category": "security",
      "severity": "critical",
      "content": "Line 101: `except (ConnectionError, TimeoutError, Exception)` — catching bare `Exception` here is problematic. It will also catch `HTTPException` raised by the `raise HTTPException(429, ...)` on line 99 if Redis returns count > limit, causing the rate limit to silently fail open (allowing the request through). The rate limit check should happen BEFORE the except block, or the except clause should be narrowed to only the Redis operations. As written, if `incr()` returns a count > 3, the exception is caught and the code falls through to the unguarded in-memory path which then crashes on `count` being undefined.",
      "existing_code": "        except (ConnectionError, TimeoutError, Exception):\n            import logging\n            logger = logging.getLogger(__name__)\n            logger.warning(\"Redis 限流故障，降级到内存计数: %s\", email)",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/auth.py",
      "start_line": 106,
      "end_line": 110,
      "category": "bug",
      "severity": "critical",
      "content": "Undefined variable `count` in rate-limit fallback path causes UnboundLocalError on every non-Redis request. When Redis is unavailable or fails, the code falls through to lines 106-110 which reference `count` (defined only inside the `if _use_redis_rate_limit:` block at line 95). This makes the email code sending endpoint crash with a 500 error whenever Redis is not configured or fails. Fix: use a local variable like `email_count = len(_email_code_store[email])` for the in-memory check.",
      "existing_code": "    now = time.time()\n    window = _email_code_store[email]\n    _email_code_store[email] = [t for t in window if now - t < _EMAIL_CODE_WINDOW]\n    if count > _EMAIL_CODE_LIMIT:\n        raise HTTPException(429, \"验证码发送过于频繁，请 1 小时后再试\")",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/auth.py",
      "start_line": 106,
      "end_line": 111,
      "category": "bug",
      "severity": "critical",
      "content": "In `_check_email_code_rate`, when Redis is unavailable and the fallback path executes, the `count` variable from line 95 is out of scope, causing an UnboundLocalError. Additionally, the in-memory path only checks the count after filtering the window but uses `count` (undefined) instead of the length of the filtered list. This means email code sending always fails with a 500 error in non-Redis environments.",
      "existing_code": "    now = time.time()\n    window = _email_code_store[email]\n    _email_code_store[email] = [t for t in window if now - t < _EMAIL_CODE_WINDOW]\n    if count > _EMAIL_CODE_LIMIT:\n        raise HTTPException(429, \"验证码发送过于频繁，请 1 小时后再试\")\n    _email_code_store[email].append(now)",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/payment.py",
      "start_line": 537,
      "end_line": 548,
      "category": "security",
      "severity": "critical",
      "content": "BUG-06 Stripe checkout.session.completed is accepted without verifying the session's actual payment status. The webhook handler trusts `event.get('data', {})` to contain a dict, but never inspects `session.get('payment_status')` or `session.get('status')`. A Stripe session can complete in states other than paid (e.g. `payment_pending`, `complete` with no charge captured). Processing such an event will call `_mark_paid_and_provision`, mark the order as paid, and provision credits even though payment was never collected. Fix: assert `session.get('payment_status') == 'paid'` (or equivalently `session.get('status') == 'complete'` with verified charge) before provisioning. Add an explicit comment that this guard matches Stripe's idempotency policy.",
      "existing_code": "        if event_type != 'checkout.session.completed':\n            # 非支付完成事件（如 checkout.session.expired）确认收到即可\n            return success_response(data={'ignored': True, 'event_type': event_type}, message='Webhook 处理成功')\n\n        session = event.get('data') or {}\n        if not isinstance(session, dict):\n            return error_response(400, '事件数据格式异常')\n        metadata = session.get('metadata') ",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/payment.py",
      "start_line": 611,
      "end_line": 626,
      "category": "security",
      "severity": "critical",
      "content": "The balance payment code calls `pay_order_with_balance` then `_mark_paid_and_provision` sequentially. If `_mark_paid_and_provision` raises (or the compensation path `_refund_balance_after_failed_pay` raises), the wallet may have already been debited without crediting the user. Wrap the compound sequence in a single database/workflow transaction where possible, or ensure the compensation function is idempotent and logged with sufficient context to allow manual recovery.",
      "existing_code": "        result = pay_order_with_balance(user_id=str(current_user.id), order_id=str(order.id), amount=amount, currency=getattr(order, 'currency', 'CNY'), tenant_id=str(order.tenant_id))\n        if not result or not getattr(result, 'success', False):\n            return error_response(400, (getattr(result, 'error', None) if result else None) or '余额支付失败')\n        # 复用回调一致的发放链路：原子置 paid + 收入 + 权益\n     ",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/core/admin_auth.py",
      "start_line": 202,
      "end_line": 206,
      "category": "security",
      "severity": "critical",
      "content": "Security: jwt.decode is called with verify_exp=False, which means revoked sessions using expired tokens will bypass the blacklist check. If a token is leaked and then the user logs out (revoking the session), an attacker can continue using the expired token indefinitely since the expiry check is disabled. The comment '防止 token 泄露后被吊销的会话仍可使用' contradicts this - it claims to prevent leaked tokens from being used, but verify_exp=False actually enables exactly that for expired tokens. Either remove verify_exp=False and handle expired token cases differently, or document why this is intentional and accept the security trade-off.",
      "existing_code": "        payload = jwt.decode(\n            token, settings.JWT_SECRET_KEY,\n            algorithms=[settings.JWT_ALGORITHM],\n            options={\"verify_exp\": False},\n        )",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/oauth_login.py",
      "start_line": 0,
      "end_line": 0,
      "category": "security",
      "severity": "critical",
      "content": "The comment references lines 393-402 but verify_wechat_signature is actually around line 387-396. The actual issue is correct - using FEISHU_VERIFICATION_TOKEN for WeChat signature verification. This is a critical bug that causes the function to always fail or verify with the wrong credential.",
      "existing_code": "def verify_wechat_signature(signature: str, timestamp: str, nonce: str) -> bool:\n    token = settings.FEISHU_VERIFICATION_TOKEN.strip()",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/oauth_login.py",
      "start_line": 667,
      "end_line": 675,
      "category": "security",
      "severity": "critical",
      "content": "verify_wechat_signature reads settings.FEISHU_VERIFICATION_TOKEN instead of a WeChat-specific signature token. This means the function will either always return False (wrong token) or use the wrong credential entirely. For Feishu events, a separate function should use FEISHU_VERIFICATION_TOKEN. For WeChat, the wechat signature uses a different algorithm (simple sort-and-sha1) but still requires a WeChat-configured token, not Feishu's.",
      "existing_code": "def verify_wechat_signature(signature: str, timestamp: str, nonce: str) -> bool:\n    \"\"\"验证微信服务器推送的签名（用于事件回调）。\"\"\"\n    token = settings.FEISHU_VERIFICATION_TOKEN.strip()\n    if not token:\n        return False\n\n    parts = sorted([token, timestamp, nonce])\n    expected = hashlib.sha1(\"\".join(parts).encode(\"utf-8\")).hexdigest()\n    return signature == expected",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/acquisition.py",
      "start_line": 620,
      "end_line": 623,
      "category": "bug",
      "severity": "high",
      "content": "NONE SAFETY: `_ops_card_payload` is called after operations that might return None (e.g., `ops_card_store.update_sample`, `ops_card_store.record_touch`). If these return None, calling `.to_dict()`, `.summary_lines()`, etc. will raise AttributeError.",
      "existing_code": "card = ops_card_store.update_sample(inquiry_id, **kwargs)\n    out = _ops_card_payload(card)\n    out[\"sample\"] = sample_view(card.sample)\n    return out",
      "scan": "ocr-scan-batch7-acq-route.json"
    },
    {
      "path": "backend/app/api/v1/routes/acquisition.py",
      "start_line": 1218,
      "end_line": 1233,
      "category": "bug",
      "severity": "high",
      "content": "Unreachable logic due to duplicate route registration and silent error swallowing: `@router.get(\"/ops/reconcile\")` is defined twice. The first definition (lines 1218-1233) includes a `get_read_session` fallback but is overridden by the second definition (line 1286), making the first one dead code. Furthermore, inside the code, the `except Exception: pass` silently swallows errors from the read-session attempt and falls through to calling `billing_reconcile` with `session=None`, causing opaque failures. Remove the duplicate or merge the fallback logic, and ensure errors are logged or handled explicitly.",
      "existing_code": "@router.get(\"/ops/reconcile\")\ndef acquisition_billing_reconcile(\n    tenant_id: str = \"demo\",\n    window_hours: int = 24,\n    current_user: User = Depends(get_current_user),\n    db: Session = Depends(get_db),\n):\n    \"\"\"E-5 meter vs ledger 对账（只读）。优先只读会话。\"\"\"\n    session = _resolve_db(db)\n    if session is None:\n        try:\n            with get_read_session() as rs:\n                return billing_re",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/api/v1/routes/acquisition.py",
      "start_line": 1218,
      "end_line": 1233,
      "category": "bug",
      "severity": "high",
      "content": "CRITICAL: Duplicate route definition for `/ops/reconcile`. Lines 1218-1233 and 1286-1294 both define the same GET endpoint. FastAPI will register the second one, making the first one unreachable. This creates inconsistent behavior: the first version uses read-session fallback logic, while the second directly calls billing_reconcile without fallback.",
      "existing_code": "@router.get(\"/ops/reconcile\")\ndef acquisition_billing_reconcile(\n    tenant_id: str = \"demo\",\n    window_hours: int = 24,\n    current_user: User = Depends(get_current_user),\n    db: Session = Depends(get_db),\n):\n    \"\"\"E-5 meter vs ledger 对账（只读）。优先只读会话。\"\"\"\n    session = _resolve_db(db)\n    if session is None:\n        try:\n            with get_read_session() as rs:\n                return billing_re",
      "scan": "ocr-scan-batch7-acq-route.json"
    },
    {
      "path": "backend/app/api/v1/routes/acquisition.py",
      "start_line": 1472,
      "end_line": 1479,
      "category": "security",
      "severity": "high",
      "content": "MISSING TENANT ISOLATION: Multiple endpoints accept `tenant_id` from request body or query params without validating it belongs to the current user. For example, `/reply-ingest`, `/dispatch`, `/buyers/upsert`, `/suppression/add` all trust user-supplied tenant_id. An authenticated user could potentially access or modify another tenant's data.",
      "existing_code": "@router.post(\"/reply-ingest\")\ndef reply_ingest(\n    body: ReplyIngestRequest,\n    current_user: User = Depends(get_current_user),\n    db: Session = Depends(get_db),\n):\n    \"\"\"客户回复进线（E-3：限流）。\"\"\"\n    _rate_limit_or_raise(body.tenant_id or \"demo\", action=\"reply_ingest\")",
      "scan": "ocr-scan-batch7-acq-route.json"
    },
    {
      "path": "backend/app/api/v1/routes/auth.py",
      "start_line": 351,
      "end_line": 366,
      "category": "security",
      "severity": "high",
      "content": "Race condition in email verification: the check-then-act pattern between querying for an unused verification code (with `.first()`) and then setting `verification.used = True` is not atomic across concurrent requests. Two simultaneous login attempts with the same valid code could both pass the query, and both would succeed in logging in. While `.with_for_update()` is present (line 304), this requires database-level row locking support (e.g., PostgreSQL with transaction isolation). With SQLite or misconfigured transactions, this race persists. Consider using an atomic UPDATE ... RETURNING or SELECT ... FOR UPDATE with proper transaction semantics.",
      "existing_code": "    verification = (\n        db.query(EmailVerification)\n        .filter(\n            EmailVerification.email == request.email,\n            EmailVerification.code == input_code_hash,\n            EmailVerification.used == False,\n            EmailVerification.expires_at > datetime.now(timezone.utc),\n        )\n        .with_for_update()\n        .first()\n    )\n    if not verification:\n        return e",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/auth.py",
      "start_line": 570,
      "end_line": 580,
      "category": "security",
      "severity": "high",
      "content": "Dev auto-bind bypasses authentication controls by allowing any `dev_`-prefixed OAuth identity to directly bind to the admin account without any password verification or 2FA. If `OAUTH_DEV_BYPASS=True` leaks into production (or if `is_production` incorrectly reports False), this creates an unrestricted privileged access path. The check at line 575 (`not settings.is_production`) is the only defense, but `is_production` is an application-level setting that can be misconfigured. Consider adding a separate, explicit feature flag that is harder to accidentally enable in production, and logging an alert when dev auto-bind is triggered.",
      "existing_code": "    admin_link = db.query(User).filter(User.username == \"admin\").first()\n    if (\n        admin_link\n        and admin_link.is_active\n        and provider_id.startswith(\"dev_\")\n        and not settings.is_production\n        and (\n            settings.OAUTH_DEV_BYPASS\n            or settings.ENVIRONMENT == \"development\"\n        )\n    ):",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/payment.py",
      "start_line": 561,
      "end_line": 563,
      "category": "security",
      "severity": "high",
      "content": "The Stripe webhook handler wraps the entire request body, signature verification, dedup, dispatch, and provisioning in a single broad `try / except Exception as e` returning 500. Stripe will retry any 5xx response aggressively. Signature failures should return 403, missing metadata should return 400, and known-ignored events should return 200 — none of these currently do. This turns every malformed/stale webhook delivery into a retry storm that also inflates error monitoring alerts. Fix: narrow the catch to unexpected internal errors only, and return explicit 2xx/4xx codes for expected cases before entering the try block (or restructure with early returns). Log errors instead of swallowing them inside the generic handler.",
      "existing_code": "    except Exception as e:\n        log.exception('[Payment] Stripe Webhook 处理失败: %s', e)\n        return error_response(500, f'Webhook 处理失败: {e}')",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/tenants.py",
      "start_line": 33,
      "end_line": 37,
      "category": "security",
      "severity": "high",
      "content": "The module-level `_register_rate` dict is mutated without any lock or synchronization. In multi-worker (gunicorn) or threaded deployments, concurrent registration requests from the same IP will race on the read-modify-write sequence (lines 33-37), causing the rate limit to be ineffective — multiple registrations can slip through the check before either worker appends its timestamp. Fix: move rate-limit state into Redis or use a process-safe lock (e.g., `threading.Lock`) if single-process is guaranteed.",
      "existing_code": "now = time.time()\n    _register_rate[ip] = [t for t in _register_rate[ip] if now - t < _REGISTER_WINDOW]\n    if len(_register_rate[ip]) >= _REGISTER_LIMIT:\n        raise HTTPException(429, \"注册过于频繁，请 1 小时后再试\")\n    _register_rate[ip].append(now)",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/tenants.py",
      "start_line": 285,
      "end_line": 287,
      "category": "security",
      "severity": "high",
      "content": "The `update_current_tenant_settings` endpoint (line 285) accepts `body.settings` and merges it directly into the tenant's JSON settings blob via `existing.update(parsed)`. There is no schema validation on the contents of `parsed` — a malicious or erroneous client can write arbitrary keys into the settings dict, potentially overwriting critical fields like `brand`, `onboarding`, or `white_label`. The endpoint should validate that only known-permitted keys are merged, or at minimum deeply-validate the incoming structure before mutating the tenant record.",
      "existing_code": "parsed = json.loads(body.settings) if isinstance(body.settings, str) else body.settings\n        existing = _safe_json_loads(tenant.settings)\n        existing.update(parsed)",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/tenants.py",
      "start_line": 1065,
      "end_line": 1066,
      "category": "security",
      "severity": "high",
      "content": "In `get_tenant_domains` (line 1066) and `add_tenant_domain` (line 1090), when the current user has no active `UserTenant` link (`ut` is None), the code falls back to `db.query(Tenant).first()` — returning the first tenant in the database regardless of which user is logged in. This is an authorization bypass: any authenticated user without an active tenant association can read or modify the domain list of an arbitrary tenant (the first one in DB order). Fix: return 403 when `ut` is None rather than falling back to an arbitrary tenant.",
      "existing_code": "ut = db.query(UserTenant).filter(UserTenant.user_id == current_user.id, UserTenant.is_active.is_(True)).first()\n    tenant = db.query(Tenant).filter(Tenant.id == ut.tenant_id).first() if ut else db.query(Tenant).first()",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/tenants.py",
      "start_line": 1346,
      "end_line": 1347,
      "category": "bug",
      "severity": "high",
      "content": "In `_create_register_entities` (lines 1346-1347), the tenant domain is generated using Python string `.replace()` with raw regex patterns instead of `re.sub()`:\n\n    tenant_domain = body.company_name.lower().replace(r\"\\s+\", \"-\").replace(r\"[^a-z0-9-]\", \"\")\n\nThe `.replace()` calls treat the pattern strings as literal text, so `r\"\\s+\"` is replaced literally (not whitespace), and `r\"[^a-z0-9-]\"` is also a literal string. This means non-alphanumeric characters are never actually stripped — e.g., a company name like `My Company!` would produce `my-company!` instead of `my-company`. Fix: use `re.sub(r\"\\s+\", \"-\", ...)` and `re.sub(r\"[^a-z0-9-]\", \"\", ...)`. Additionally, this means duplicate domain collisions are possible since two companies with similar names could produce identical slugs.",
      "existing_code": "tenant_domain = body.company_name.lower().replace(\n        r\"\\s+\", \"-\").replace(r\"[^a-z0-9-]\", \"\")[:50]",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/tenants.py",
      "start_line": 1528,
      "end_line": 1531,
      "category": "bug",
      "severity": "high",
      "content": "In `register_tenant` (lines 1528-1531), entities are created with `db.flush()` inside `_create_register_entities` but only committed after `_apply_register_extras` completes. If `_apply_register_extras` raises an exception (e.g., from `bind_guest_media_to_tenant`, `ReferralService.apply_referral`, or `provision_tenant_onboarding`), the partially-written rows (user, tenant, subscription, user_tenant) remain in the database as orphaned records because there is no rollback. Fix: wrap the entire creation sequence in a try/except with `db.rollback()` on failure, or use a transaction block.",
      "existing_code": "now = datetime.now(timezone.utc)\n    user, tenant, trial_end = _create_register_entities(db, body, plan, now)\n    guest_bound, onboarding = _apply_register_extras(db, body, tenant, user, plan, trial_end, verification)\n    db.commit()",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/core/admin_auth.py",
      "start_line": 208,
      "end_line": 209,
      "category": "security",
      "severity": "high",
      "content": "Error handling: The bare except in _get_token_jti swallows ALL exceptions including JWTDecodeError, InvalidTokenError, and other authentication-related errors. This makes it impossible to distinguish between a valid token without JTI, a malformed token, a signature verification failure, or a Redis unavailability issue. During security incidents, this obscurity prevents effective investigation. At minimum, catch specific JWT exceptions and log them. Consider whether unauthorized tokens should be treated differently than tokens without JTI.",
      "existing_code": "    except Exception:\n        return None",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/core/database.py",
      "start_line": 113,
      "end_line": 113,
      "category": "security",
      "severity": "high",
      "content": "Bare except clause swallows KeyboardInterrupt and SystemExit, preventing proper shutdown signals from reaching the application. This makes it impossible to stop the service cleanly. Should catch Exception at minimum and re-raise system exceptions.",
      "existing_code": "except Exception as e:",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/database.py",
      "start_line": 126,
      "end_line": 128,
      "category": "bug",
      "severity": "high",
      "content": "rebind_engine() recreates the main engine but leaves read_engine stale. After rebind, any sessions using RoutingSession will reference the old read_engine for read operations, breaking the read-write split. Should also update read_engine when rebind is called.",
      "existing_code": "def rebind_engine(database_url: str | None = None) -> None:\n    \"\"\"按新 URL 重建 engine/SessionLocal（预检、脚本切换 SQLite/Postgres 用）。\"\"\"\n    global engine, SessionLocal, UUID_TYPE",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/database.py",
      "start_line": 184,
      "end_line": 185,
      "category": "bug",
      "severity": "high",
      "content": "Logger is used before it's defined in mount_rls_pilot_if_enabled(). The function references logger on line 189 but logger is only defined on line 191. This will cause NameError at runtime when the function is called.",
      "existing_code": "def mount_rls_pilot_if_enabled() -> None:\n    \"\"\"按配置门控挂载 RLS 试点表（默认关，生产零回归）。",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/login_bruteforce.py",
      "start_line": 0,
      "end_line": 0,
      "category": "bug",
      "severity": "high",
      "content": "`_redis_check` uses `time.time()` while `_memory_check` uses `_now_mono()` (which is `time.monotonic()`). This means Redis and in-memory stores have inconsistent lock semantics: a lock acquired in memory at monotonic time T+100 will expire at T+100+LOCK_SECONDS, but the same lock stored in Redis at wall-clock time W+100 expires at W+100+LOCK_SECONDS. If the system clock is adjusted (NTP sync, manual change), Redis locks can expire early or late relative to memory locks, defeating the anti-bruteforce guarantee during fallback transitions. Both paths should use the same time source. Since the Lua script already receives `ARGV[1]` as `now`, unify by passing `time.monotonic()` everywhere and using it consistently in Redis as well.",
      "existing_code": "def _redis_check(r: Any, client_ip: str,\n                 username_or_email: str) -> Optional[Tuple[int, str]]:\n    now = time.time()\n    ik = _identity_key(client_ip, username_or_email)",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/login_bruteforce.py",
      "start_line": 120,
      "end_line": 127,
      "category": "security",
      "severity": "high",
      "content": "Bare `except Exception` in `_redis_client()` is far too broad. It catches `KeyboardInterrupt` via `Exception` is actually fine (KiB is not a subclass of Exception), but it still swallows import failures, config errors, and unexpected library bugs that should surface. More critically, the comment says '导入失败极少' but any exception here returns None, silently forcing the entire login brute-force protection into memory-only mode across all requests until the module is restarted or the issue is resolved. Use a narrower exception type or let it propagate so the service operator notices the Redis outage.",
      "existing_code": "    try:\n        from app.core.refresh_token_blacklist import \\\n            get_refresh_blacklist_redis\n\n        return get_refresh_blacklist_redis()\n    except Exception as exc:  # pragma: no cover - 导入失败极少\n        log.warning(\"login_bruteforce: 无法获取 Redis 客户端: %s\", exc)\n        return None",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/login_bruteforce.py",
      "start_line": 160,
      "end_line": 160,
      "category": "bug",
      "severity": "high",
      "content": "`_redis_record_failure` also mixes `time.time()` with the Lua script while `_memory_record_failure` uses `time.monotonic()`. This same inconsistency means identity locks recorded via Redis and via memory will expire at different real-world moments. Fix by using `_now_mono()` (i.e., `time.monotonic()`) in `_redis_record_failure` and `_redis_check`.",
      "existing_code": "-    now = time.time()",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/login_bruteforce.py",
      "start_line": 372,
      "end_line": 375,
      "category": "security",
      "severity": "high",
      "content": "`client_ip_from_request` returns the first IP from X-Forwarded-For without validating that it is a legitimate IP address. An attacker can send `X-Forwarded-For: 8.8.8.8, 127.0.0.1` and if the direct connection comes from a trusted proxy, the function returns `8.8.8.8` as the client IP. This bypasses IP-based rate limiting because the attacker's real IP is never stored. Add validation: parse the extracted IP with `ipaddress.ip_address()` and reject malformed entries, or at minimum verify the extracted IP is not a private/reserved address when claiming it came from a real client.",
      "existing_code": "    if _is_trusted_proxy(direct_ip):\n        fwd = request.headers.get(\"x-forwarded-for\") or request.headers.get(\"X-Forwarded-For\")\n        if fwd:\n            return fwd.split(\",\")[0].strip()",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/__init__.py",
      "start_line": 912,
      "end_line": 916,
      "category": "bug",
      "severity": "high",
      "content": "The bare `except Exception: pass` on lines 912-916 silently swallows any import failure from the persistence patch. This means if `ops_card_pg` is missing, misconfigured, or raises any exception during initialization, the system will continue running without the Postgres backend — but the developer has no visibility into this. At minimum, log the exception. Also, this swallows exceptions during module import time, which could hide critical setup errors.",
      "existing_code": "try:\n    from app.services.acquisition.ops_card_pg import patch_store_persistence\n    patch_store_persistence(ops_card_store)\nexcept Exception:\n    pass",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/baseline_bench.py",
      "start_line": 36,
      "end_line": 37,
      "category": "bug",
      "severity": "high",
      "content": "The bare `except Exception` at lines 32-34 and 42-44 silently swallows errors without logging or preserving the original traceback. This makes it impossible to diagnose why baseline operations failed. Consider using `except Exception as e:` and logging the exception, or re-raising with `raise ... from e` to preserve context.",
      "existing_code": "        except Exception:\n            errors += 1",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/billing_explain.py",
      "start_line": 119,
      "end_line": 125,
      "category": "security",
      "severity": "high",
      "content": "The acquisition events query filters by task_type pattern but does NOT filter by tenant_id/tid. This means it returns acquisition events from ALL tenants, not just the requested tenant. This is a security and correctness issue - users could see other tenants' acquisition activity. Fix: add `.filter(EvolutionTaskRecord.tenant_id == tid)` to the query on line 102.",
      "existing_code": "        acq = (\n            session.query(EvolutionTaskRecord)\n            .filter(EvolutionTaskRecord.task_type.like(\"acquisition.%\"))\n            .order_by(EvolutionTaskRecord.created_at.desc())\n            .limit(limit)\n            .all()\n        )",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/dispatch_service.py",
      "start_line": 41,
      "end_line": 47,
      "category": "security",
      "severity": "high",
      "content": "The except block on lines 36-41 silently swallows ALL exceptions from resolve_tenant_uuid without logging, making it impossible to diagnose tenant resolution failures. This could hide critical security issues like invalid tenant IDs being accepted. Consider logging the exception at least at DEBUG level, or re-raising with context if the failure is unrecoverable.",
      "existing_code": "        try:\n            from app.services.acquisition.repo import resolve_tenant_uuid\n            tid = resolve_tenant_uuid(session, tenant_id)\n            if tid:\n                resolved_tenant = tid\n        except Exception:\n            pass",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/dispatch_service.py",
      "start_line": 95,
      "end_line": 96,
      "category": "bug",
      "severity": "high",
      "content": "Line 95: `node_tasks[0]` indexing will raise IndexError if `parse_graph_to_tasks` returns an empty list. The conditional only guards `getattr` but doesn't check if node_tasks is non-empty before indexing.",
      "existing_code": "        node_tasks = parse_graph_to_tasks(session, resolved_tenant, graph)\n        plan_task_id = str(getattr(node_tasks[0], \"parent_task_id\", \"\")) if node_tasks else \"\"",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 55,
      "end_line": 59,
      "category": "bug",
      "severity": "high",
      "content": "Lines 55 and 75: Both `except Exception` blocks use `# noqa: BLE001` to suppress the bare-except linting warning. While catching `Exception` (not bare `except:`) is better than swallowing `KeyboardInterrupt` and `SystemExit`, these are very broad catches. The first block (line 55) catches any exception from `EvolutionEngine` instantiation or `record_task_execution` call — including programming errors, import failures, etc. — and silently falls through to the fallback. If the fallback also fails, the original exception from the primary path is lost (only `exc2` is logged on line 76). Consider logging the primary exception at a higher level or including it in the returned reason for better observability.",
      "existing_code": "    except Exception as exc:  # noqa: BLE001\n        logger.debug(\"EvolutionEngine 写入失败，尝试 terminal_hook: %s\", exc)\n\n    # 2) terminal_hook\n    try:",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 55,
      "end_line": 77,
      "category": "bug",
      "severity": "high",
      "content": "Lines 34-56 and 59-77: The fallback chain silently discards the primary exception. If `EvolutionEngine` raises an exception (e.g., database constraint violation, connection error), it is logged at DEBUG level and the code falls through to `terminal_hook`. If `terminal_hook` also fails, only `exc2` is captured in the returned reason — the original `Exc` is completely lost. This makes debugging production failures nearly impossible when both paths fail. At minimum, log both exceptions at WARNING or ERROR level, and include the primary exception in the returned reason dict so operators can see the full failure chain.",
      "existing_code": "    except Exception as exc:  # noqa: BLE001\n        logger.debug(\"EvolutionEngine 写入失败，尝试 terminal_hook: %s\", exc)\n\n    # 2) terminal_hook\n    try:\n        from app.services.trace.terminal_hook import record_terminal_state\n        out = record_terminal_state(\n            db,\n            task_type=task_type,\n            executor_type=\"workflow\",\n            executor_id=executor_id,\n            suc",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/fulfillment_nodes.py",
      "start_line": 68,
      "end_line": 69,
      "category": "bug",
      "severity": "high",
      "content": "When deposit is paid, the code unconditionally overwrites status to \"done\", even if the deposit node was already marked \"overdue\". This loses overdue information and will suppress overdue reminders. Fix: skip the payment-led update when status is \"overdue\" or \"skipped\", e.g. `if status not in (\"done\", \"skipped\", \"overdue\"):`.",
      "existing_code": "            elif key == \"deposit\" and deposit_paid:\n                status = \"done\" if not status or status in (\"pending\", \"active\") else status",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/growth_ops.py",
      "start_line": 173,
      "end_line": 174,
      "category": "other",
      "severity": "high",
      "content": "Module-level mutable singletons (content_attr_store and ip_slot_catalog) have no thread safety. Concurrent requests from different tenants can interleave mutations on shared dicts/lists, causing data corruption or inconsistent reads. No locks are used around dict operations or list iterations.",
      "existing_code": "content_attr_store = ContentAttributionStore()\nip_slot_catalog = IpSlotCatalog()",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/knowledge_queue.py",
      "start_line": 180,
      "end_line": 180,
      "category": "security",
      "severity": "high",
      "content": "The `tenant_id` parameter in `list()` and `report()` is unused. The method signatures imply multi-tenancy support, but there is no filtering or isolation by tenant. If this is intended to support multiple tenants, the implementation is incomplete and leaks state. If not, the parameter should be removed to avoid misleading API contracts.",
      "existing_code": "def list(self, *, tenant_id: str = \"\", category: str = \"\", only_pending: bool = False) -> list[dict[str, Any]]:",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/knowledge_queue.py",
      "start_line": 211,
      "end_line": 212,
      "category": "security",
      "severity": "high",
      "content": "The `report()` method returns all items via `self.list(tenant_id=tenant_id)` which ignores the `tenant_id` parameter. If multi-tenancy is intended, this is a data leak. If not, the parameter should be removed.",
      "existing_code": "def report(self, *, tenant_id: str = \"\") -> dict[str, Any]:\n        items = self.list(tenant_id=tenant_id)",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/knowledge_queue.py",
      "start_line": 237,
      "end_line": 237,
      "category": "other",
      "severity": "high",
      "content": "The module-level singleton `knowledge_queue_store` holds mutable state (the `_items` dict). In a multi-tenant, multi-threaded environment (common for SaaS applications), concurrent calls to `mark_done`, `mark_pending`, or `list` can cause race conditions. The check-then-act pattern in `mark_done` (reading `it.done`, then setting it) is not atomic. Use a lock or thread-safe data structure to protect shared state.",
      "existing_code": "knowledge_queue_store = KnowledgeQueueStore()",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/loss_report.py",
      "start_line": 26,
      "end_line": 27,
      "category": "bug",
      "severity": "high",
      "content": "Inquiry-level substring match in _normalize_reason is too broad. Any reason containing '无回复' (e.g. '客户说无回复意向', '暂无回复计划') is collapsed into the canonical '无回复' bucket, distorting both counts and hints. If the intent is to normalize only exact known values, use an exact equality check. If substring matching is intentional, document that behavior explicitly and consider whether it leads to misleading reports.",
      "existing_code": "    if \"无回复\" in r or r == \"原因未知\":\n        return \"无回复\"",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/nps_rescue.py",
      "start_line": 41,
      "end_line": 41,
      "category": "bug",
      "severity": "high",
      "content": "Bug: the `or` in `getattr(c, \"stage\", \"\") == \"won\" or getattr(c, \"won_at\", \"\")` is not parenthesized, so the whole `sum(1 for ...)` expression actually becomes:\n\n    (sum(1 for c in cards if getattr(c, \"stage\", \"\") == \"won\")) or getattr(c, \"won_at\", \"\")\n\ni.e. Python's generator is consumed first, then the result is `or`-ed with the *last* iteration's `won_at` attribute. This means `won_n` will hold the `won_at` string of the final card instead of the count when the count is 0. The intended semantics is almost certainly that a card counts as \"won\" if either field is truthy:\n\n    won_n = sum(1 for c in cards if getattr(c, \"stage\", \"\") == \"won\" or bool(getattr(c, \"won_at\", \"\")))",
      "existing_code": "            won_n = sum(1 for c in cards if getattr(c, \"stage\", \"\") == \"won\" or getattr(c, \"won_at\", \"\"))",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/services/acquisition/nps_rescue.py",
      "start_line": 45,
      "end_line": 47,
      "category": "bug",
      "severity": "high",
      "content": "The won_n count logic uses `or` which means a card with both stage=='won' AND won_at truthy will still only be counted once (correct). However, the condition `getattr(c, 'stage', '') == 'won' or getattr(c, 'won_at', '')` could double-count if there's a bug in data where both are present but distinct. More importantly, the won_n value from ops_store can be overridden by win_loss_view data (line 46-47: `won_n = won_n or int(...)`), which means if ops_store has valid won_n > 0, the win_loss_view count is ignored entirely. This is potentially surprising behavior.",
      "existing_code": "    if win_loss_view:\n        won_n = won_n or int(win_loss_view.get(\"won_count\") or 0)\n        lost_n = lost_n or int(win_loss_view.get(\"lost_count\") or 0)",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/nps_rescue.py",
      "start_line": 79,
      "end_line": 79,
      "category": "security",
      "severity": "high",
      "content": "Bug: when `nps_score` is a negative number (e.g. -3, which a caller could pass in despite the `Optional[int]` type hint), `s = max(0, min(10, int(nps_score)))` coerces it to 0, which is then misinterpreted as a collected NPS score of 0 (detractor bucket). The code's own docstring principle is \"无数据不编 NPS 分数\" — clamping negative input to 0 fabricates a score from invalid data. Consider returning the uncollected block when the value is outside [0, 10], or raising a `ValueError` to make the contract explicit.",
      "existing_code": "        s = max(0, min(10, int(nps_score)))",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/services/acquisition/ops_card_pg.py",
      "start_line": 111,
      "end_line": 114,
      "category": "security",
      "severity": "high",
      "content": "No tenant isolation: The table stores tenant_id but load_ops_card doesn't filter by tenant. If inquiry_id collides across tenants, the wrong card could be loaded.",
      "existing_code": "        row = session.execute(\n            text(\"SELECT payload FROM acquisition_ops_cards WHERE inquiry_id = :iid\"),\n            {\"iid\": inquiry_id},\n        ).scalar()",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/ops_card_pg.py",
      "start_line": 174,
      "end_line": 174,
      "category": "bug",
      "severity": "high",
      "content": "When payment is an empty dict, the ternary creates a bare OpsCardPayment() instance, but when payment is a non-empty dict, the inner dict-comprehension {k: payment.get(k, getattr(OpsCardPayment(), k)) for k in payment} only iterates over the keys present in the serialized payment. Any OpsCardPayment field that was omitted (e.g., because it defaulted to None/empty and was stripped during serialization) will be set to its default via getattr, which is correct. However, the real defect is that getattr(OpsCardPayment(), k) creates a new throwaway OpsCardPayment instance for every key lookup — if OpsCardPayment is a dataclass with non-trivial __init__ defaults, this is expensive and, more importantly, if any default itself depends on other fields or raises (e.g., a field with a factory that returns a mutable object shared across instances), the default will be the shared factory result, not a fresh copy. Use a single reference instance for defaults.",
      "existing_code": "        card.payment = OpsCardPayment(**{k: payment.get(k, getattr(OpsCardPayment(), k)) for k in payment} if payment else OpsCardPayment())",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/services/acquisition/payment_risk.py",
      "start_line": 82,
      "end_line": 83,
      "category": "other",
      "severity": "high",
      "content": "Lines 82-83: The bare `except Exception: pass` silently swallows all errors from `ops_store` operations (AttributeError, TypeError, etc.). This makes debugging impossible when the ops_store API changes or fails. Consider logging the exception or re-raising with context.",
      "existing_code": "        except Exception:\n            pass",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/quote_guard.py",
      "start_line": 21,
      "end_line": 30,
      "category": "bug",
      "severity": "high",
      "content": "The `now` parameter accepts `Optional[datetime]` but is never asserted to be timezone-aware. When callers pass a naive datetime, `valid_until = start + timedelta(days=days)` produces a timezone-aware `valid_until` (since `start` comes from `_parse_ts`, presumably aware), and the comparison `now >= valid_until` will raise `TypeError: can't compare offset-naive and offset-aware datetimes`. Callers should ensure `now` is UTC-aware, or the function should validate/normalize it.",
      "existing_code": "def quote_validity_view(\n    *,\n    quote_at: str = \"\",\n    valid_days: Optional[int] = None,\n    fx_locked: bool = False,\n    fx_note: str = \"\",\n    now: Optional[datetime] = None,\n) -> dict[str, Any]:\n    \"\"\"报价有效期视图（大白话）。\"\"\"\n    now = now or datetime.now(timezone.utc)",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/sales_collision.py",
      "start_line": 0,
      "end_line": 0,
      "category": "bug",
      "severity": "high",
      "content": "Race condition: The get/check/set sequence in claim_inquiry is not atomic. If two users call claim_inquiry concurrently for the same inquiry_id, both may see `card` as having no owner (or different owners) before either calls set_owner, leading to silent overwrites without proper collision handling. This is a data integrity issue for a collision-tracking system whose entire purpose is to detect and prevent exactly this scenario. Consider whether ops_store methods need to be called within a transaction or with distributed locking.",
      "existing_code": "    card = ops_store.get_by_inquiry(inquiry_id)\n    if card is None:\n        card = ops_store.materialize(\n            tenant_id=tenant_id or \"demo\",\n            inquiry_id=inquiry_id,\n            owner_user_id=user_id,\n        )\n        return {\n            \"ok\": True,\n            \"code\": \"claimed_new\",\n            ...\n        }\n\n    current = (card.owner_user_id or \"\").strip()\n    if not current",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/sample_flow.py",
      "start_line": 94,
      "end_line": 100,
      "category": "bug",
      "severity": "high",
      "content": "The fee_status field is referenced in fee_hole logic but never defined in SAMPLE_STATUSES or _TRANSITIONS. The SAMPLE_STATUSES tuple only contains shipment statuses ('none' through 'rejected'), while fee_hole checks for 'unbilled' and 'billed' which are not valid sample statuses. This suggests the fee tracking state machine is incomplete or inconsistently modeled - the code assumes fee_status values exist but they're not part of the defined state transitions.",
      "existing_code": "    fee_hole = False\n    if payload:\n        if payload.get(\"status\") in (\"shipped\", \"delivered\") and payload.get(\"fee_status\") in (\n            \"unbilled\",\n            \"billed\",\n        ) and (payload.get(\"fee_amount\") or 0) > 0:\n            fee_hole = True",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/suppression_list.py",
      "start_line": 30,
      "end_line": 36,
      "category": "security",
      "severity": "high",
      "content": "Thread safety issue: The module-level `suppression_store` singleton is shared mutable state accessed by `add`, `remove`, and `check_outreach` without any locking. In multi-threaded or async deployments, concurrent calls can cause check-then-act races — e.g., two threads both calling `add` for the same email simultaneously could both pass the `if key in self._by_key` check and insert duplicate entries, or a `remove` could interleave with a `check_outreach` causing a suppressed contact to slip through. This directly violates the module's stated policy that suppression is additive and must prevent re-sending.",
      "existing_code": "class SuppressionStore:\n    def __init__(self) -> None:\n        self._by_key: dict[tuple[str, str], SuppressionEntry] = {}\n\n    @staticmethod\n    def _norm(email: str) -> str:\n        return (email or \"\").strip().lower()",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/suppression_list.py",
      "start_line": 165,
      "end_line": 166,
      "category": "security",
      "severity": "high",
      "content": "No persistence guarantee: The comment states '生产可换 DB 表' (production can swap to DB), but the current in-memory implementation loses ALL suppression data on process restart. This violates the stated policy that '抑制只增不减' (suppression is additive and never decreases). If this file is used in production without immediate DB replacement, any server restart will wipe all suppression entries, allowing previously unsubscribed/complained contacts to receive unsolicited emails. A persistence layer or at minimum a restart-safe storage mechanism should be added before production use.",
      "existing_code": "# 全局单例（生产可换 DB 表）\nsuppression_store = SuppressionStore()",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/template_weights.py",
      "start_line": 154,
      "end_line": 154,
      "category": "bug",
      "severity": "high",
      "content": "Line 137: `max(loss_reason_counts.items(), key=lambda x: x[1])` will raise `ValueError: max() arg is an empty sequence` when there are zero loss records. The ternary conditional expression only provides a default for the result assignment but does not prevent the `max()` call from executing when `loss_reason_counts` is empty.",
      "existing_code": "    top_loss = max(loss_reason_counts.items(), key=lambda x: x[1])[0] if loss_reason_counts else \"\"",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/template_weights.py",
      "start_line": 175,
      "end_line": 176,
      "category": "other",
      "severity": "high",
      "content": "Lines 153-154: The module-level `_approved` dict is shared mutable state mutated by `approve_weight()` without any synchronization. In multi-threaded or multi-worker deployments, concurrent calls can corrupt the approval records.",
      "existing_code": "# 内存中的「已批准权重」（生产应落 PG；当前先只读建议 + 可选批准记录）\n_approved: dict[str, dict[str, Any]] = {}",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/adapters/tradeai/__init__.py",
      "start_line": 191,
      "end_line": 202,
      "category": "other",
      "severity": "high",
      "content": "The `_tenant_orchestrators` dictionary is a module-level mutable global that is accessed and mutated without any thread synchronization. In a multi-threaded web service (Hermes supervisor pattern), concurrent calls to `tenant_orchestrator()` could race on the check-then-act pattern at lines 178-182: one thread might check `tenant_id not in _tenant_orchestrators` while another is in the middle of creating the orchestrator, potentially causing duplicate creation or, worse, a KeyError if `sys.modules` manipulation in `_load_vendor_isolated` races with this path.",
      "existing_code": "_tenant_orchestrators: Dict[str, Any] = {}\n\n\ndef tenant_orchestrator(tenant_id: str) -> Any:\n    \"\"\"按租户返回隔离的 AgentOrchestrator 实例（其原生为全局单例，总纲 §5.2.3-④ 改造项）。\"\"\"\n    if not tenant_id:\n        raise ValueError(\"tenant_id 必填（冻结原则 §1.2-3 租户隔离优先）\")\n    if tenant_id not in _tenant_orchestrators:\n        orch_cls = getattr(importlib.import_module(_EXPORTS[\"AgentOrchestrator\"]), \"AgentOrchestrator\")\n      ",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/goodjob/customer_pool_projection.py",
      "start_line": 69,
      "end_line": 78,
      "category": "security",
      "severity": "high",
      "content": "The projection payload contains raw PII fields (email, phone, name, message_excerpt) without any masking or sensitivity handling. These values will be stored in task queues, potentially logged by the worker infrastructure, and may violate data protection requirements. Consider masking sensitive fields (e.g., partial email masking, phone number truncation) or documenting the PII handling expectations with downstream consumers.",
      "existing_code": "        \"customer\": {\n            \"name\": _attr_str(row, \"name\"),\n            \"email\": _attr_str(row, \"email\"),\n            \"phone\": _attr_str(row, \"phone\"),\n            \"product\": _attr_str(row, \"product\"),\n            \"message_excerpt\": _excerpt(getattr(row, \"message\", \"\")),\n            \"status\": _attr_str(row, \"status\"),\n            \"source_channel\": _attr_str(row, \"source_channel\"),\n          ",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/goodjob/trade_document_bridge.py",
      "start_line": 53,
      "end_line": 55,
      "category": "bug",
      "severity": "high",
      "content": "The idempotency key truncation at 128 chars can cause collisions. Two different payloads with different tenant_id, inquiry_id, doc_type, or content hashes could produce identical keys if the differing segments fall after position 128 in the concatenated string. Consider using a hash prefix (e.g., first 8-16 chars of sha256) instead of raw string truncation to minimize collision risk.",
      "existing_code": "    return (\n        f\"gj-doc:{tenant_id}:{inquiry_id}:{doc_type}:{_content_hash(items, options)}\"\n    )[:_KEY_MAX_LEN]",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/hermes/task_control_supervisor.py",
      "start_line": 51,
      "end_line": 62,
      "category": "bug",
      "severity": "high",
      "content": "The recursive `dfs` in `_validate_dag_topology` can hit Python's default recursion limit (typically 1000) on DAGs with deep chains approaching the MAX_DAG_NODES=100 boundary. A recursion error here would surface as an unhandled exception rather than a clean validation failure. Consider rewriting as an iterative DFS or increasing the recursion limit with a explicit guard.",
      "existing_code": "    def dfs(u: str) -> None:\n        visited[u] = 1\n        for v in graph_adj.get(u, []):\n            if visited.get(v) == 1:\n                raise ValueError(f\"DAG Governor: 检测到拓扑死循环环路: '{u}' -> '{v}'\")\n            if visited.get(v) == 0:\n                dfs(v)\n        visited[u] = 2\n\n    for n in nodes:\n        if visited[n.id] == 0:\n            dfs(n.id)",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/hermes/task_control_supervisor.py",
      "start_line": 282,
      "end_line": 285,
      "category": "bug",
      "severity": "high",
      "content": "If any child task has a non-numeric `budget_used` value, the `float()` conversion raises `ValueError` which is caught by the broad `except Exception`, silently resetting `budget_used` to 0.0. This disables the budget gate for the entire plan and could allow unbounded spending. Additionally, the error is not logged, making diagnosis impossible.",
      "existing_code": "    try:\n        budget_used = sum(float(t.budget_used or 0) for t in child_tasks)\n    except Exception:  # noqa: BLE001\n        budget_used = 0.0",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/hermes/task_control_supervisor.py",
      "start_line": 428,
      "end_line": 429,
      "category": "bug",
      "severity": "high",
      "content": "When `on_fail=abort` triggers `compensate_plan`, only nodes with `compensation_action` in their input get status set to `cancelled`. Nodes that completed successfully *after* a skipped node (i.e., dependents of the skipped node) are left as `done` without their `compensation_action` being executed or status reset. This violates Saga rollback semantics — the plan is marked failed but downstream completed work is not compensated.",
      "existing_code": "        # 2. If node was 'done' or 'executing', and has a compensation action, execute it.\n        if task.status in [\"done\", \"executing\"] and comp_action:",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/oauth_login.py",
      "start_line": 400,
      "end_line": 410,
      "category": "security",
      "severity": "high",
      "content": "The QQ OAuth token exchange uses a GET request with client_secret as a query parameter. Per OAuth 2.0 best practices and the QQ OpenAPI documentation, the token endpoint should receive credentials via POST with the body, not GET with query params. GET leaks the client_secret in server access logs, browser history, and Referer headers. Consider changing to client.post() with form data.",
      "existing_code": "        tr = client.get(\n            \"https://graph.qq.com/oauth2.0/token\",\n            params={\n                \"grant_type\": \"authorization_code\",\n                \"client_id\": app_id,\n                \"client_secret\": key,\n                \"code\": code,\n                \"redirect_uri\": _redirect_uri(),\n                \"fmt\": \"json\",\n            },\n        )",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/oauth_login.py",
      "start_line": 667,
      "end_line": 669,
      "category": "bug",
      "severity": "high",
      "content": "Bug: `verify_wechat_signature` reads `settings.FEISHU_VERIFICATION_TOKEN` instead of a WeChat-specific token setting (e.g. `WECHAT_OPEN_VERIFICATION_TOKEN` or similar). This is almost certainly a copy-paste from the Feishu signature verification pattern. If no Feishu verification token is configured, WeChat event callback signature verification will always fail, breaking WeChat webhook processing. Either add a dedicated WeChat verification token config and reference it here, or remove this function if WeChat server events are not supported.",
      "existing_code": "def verify_wechat_signature(signature: str, timestamp: str, nonce: str) -> bool:\n    \"\"\"验证微信服务器推送的签名（用于事件回调）。\"\"\"\n    token = settings.FEISHU_VERIFICATION_TOKEN.strip()",
      "scan": "ocr-scan-batch4.json"
    },
    {
      "path": "backend/app/services/payment_pkg/payment_service_impl.py",
      "start_line": 145,
      "end_line": 146,
      "category": "security",
      "severity": "high",
      "content": "Secret exposure risk: `PAYMENT_WEBHOOK_SECRET` is used to compute an HMAC digest. Ensure this value is never logged, returned in responses, or included in stack traces. Confirm that `verify_notify` is only called from trusted webhook endpoints and that the signature comparison path does not inadvertently log `sig` or `secret`. Consider rotating the secret on a regular schedule and storing it in a secrets manager rather than a plain env var.",
      "existing_code": "        secret = (os.getenv(\"PAYMENT_WEBHOOK_SECRET\") or \"\").strip()\n        sig = (signature or \"\").strip()",
      "scan": "ocr-scan-batch5-payment.json"
    },
    {
      "path": "backend/app/services/payment_pkg/payment_service_impl.py",
      "start_line": 368,
      "end_line": 372,
      "category": "bug",
      "severity": "high",
      "content": "Silent failure in `_record_revenue`: while the method re-raises after rollback, the broad `except Exception` may mask transaction state issues (e.g., `PendingRollbackError`). If the revenue entry is partially written before the exception, the rollback leaves the session in a dirty state. Consider using a separate transaction or explicit savepoint to isolate revenue ledgering from the main payment flow, ensuring idempotency checks are not bypassed by a failed rollback.",
      "existing_code": "        except Exception:\n            # BUG-09 修复：财务台账写入失败时回滚并重新抛出，不静默吞异常\n            self.db.rollback()\n            logger.exception(\"FinanceLedgerEntry 写入失败 order=%s\", order.order_no)\n            raise",
      "scan": "ocr-scan-batch5-payment.json"
    },
    {
      "path": "backend/app/services/payment_pkg/payment_service_impl.py",
      "start_line": 431,
      "end_line": 441,
      "category": "bug",
      "severity": "high",
      "content": "TOCTOU race: `_mark_paid_and_provision` uses `SELECT-UPDATE` then proceeds to async provisioning. Under concurrent webhook delivery (channel retries), two requests can both read `status=pending`, pass the `updated == 0` guard, and proceed to provision — causing duplicate benefit issuance. The rollback-to-pending path only protects the sender of the second request, but the first request may have already provisioned. Recommend using a DB-level atomic update with a returning/affected-row check plus a distributed lock or idempotency key, and rejecting the second webhook with a definitive \"already processed\" status rather than retryable failure.",
      "existing_code": "        updated = (\n            self.db.query(PaymentOrder)\n            .filter(PaymentOrder.order_no == order_no, PaymentOrder.status == \"pending\")\n            .update({PaymentOrder.status: \"paid\", PaymentOrder.paid_at: datetime.now(timezone.utc)})\n        )\n        self.db.commit()\n        if updated == 0:\n            return None\n        order = self.db.query(PaymentOrder).filter(\n            Pa",
      "scan": "ocr-scan-batch5-payment.json"
    },
    {
      "path": "backend/app/services/payment_service.py",
      "start_line": 0,
      "end_line": 0,
      "category": "bug",
      "severity": "high",
      "content": "Bug: `_schedule_compensation` references an undefined variable `data` at line 360, which will raise a `NameError` whenever compensation scheduling is attempted, causing the exception handler to fail. Additionally, `sync_b2b_from_payment_notify(self.db, data or {})` at the end of the method is dead/unreachable code because the method either returns after the try/except block or raises an exception before reaching it. The call should either be moved inside the `try` block alongside provisioning calls (if intended) or removed entirely.",
      "existing_code": "        except Exception as exc:\n            logger.error(\n                \"WeChat provision failed, scheduled compensation | order_no=%s err=%s\",\n                order_no,\n                exc_info=True,\n            )\n            self._schedule_compensation(order, source=\"wechat_notify\")\n        return order\n\n    # ------------------------------------------------------------------\n    # 处理支付宝回调\n  ",
      "scan": "ocr-scan-batch5-payment.json"
    },
    {
      "path": "backend/app/services/payment_service.py",
      "start_line": 154,
      "end_line": 159,
      "category": "security",
      "severity": "high",
      "content": "Security: Weak webhook signature verification. The HMAC-SHA256 payload format `f\"{order_no}:{amount}\"` uses a colon separator that could be ambiguous if field values contain colons (e.g., order_no = 'A:B' vs 'A:B:100'). This could allow signature confusion attacks. Additionally, there's no timestamp or nonce to prevent replay attacks. The `PAYMENT_WEBHOOK_SECRET` fallback for non-strict mode also allows unverified callbacks in non-production environments without clear security boundaries.",
      "existing_code": "        payload = f\"{order_no}:{amount}\"\n        expected = hmac.new(\n            secret.encode(\"utf-8\"),\n            payload.encode(\"utf-8\"),\n            hashlib.sha256,\n        ).hexdigest()",
      "scan": "ocr-scan-batch5-payment.json"
    },
    {
      "path": "backend/app/services/payment_service.py",
      "start_line": 587,
      "end_line": 593,
      "category": "other",
      "severity": "high",
      "content": "Race condition: `process_wechat_notify` and `process_alipay_notify` perform check-then-act without database locking. Concurrent webhook deliveries (common with payment gateways retrying failed notifications) could cause duplicate provisioning, revenue recording, and subscription updates. Consider using SELECT FOR UPDATE or a unique constraint on order_no with idempotency checks at the database level.",
      "existing_code": "        order = self.db.query(PaymentOrder).filter(\n            PaymentOrder.order_no == order_no\n        ).first()\n        if not order or order.status != \"pending\":\n            return None\n\n        order.status = \"paid\"",
      "scan": "ocr-scan-batch5-payment.json"
    },
    {
      "path": "backend/app/api/v1/routes/acquisition.py",
      "start_line": 339,
      "end_line": 343,
      "category": "bug",
      "severity": "medium",
      "content": "INCONSISTENT ERROR HANDLING: `intent_preview` catches exceptions and raises HTTPException with 400, but other endpoints like `ops_card_touch`, `ops_card_loss`, `ops_card_win` don't handle potential exceptions from store operations. If `record_touch` or similar fails, it will raise unhandled exceptions.",
      "existing_code": "try:\n        import asyncio\n        graph, source = await ps.decompose(ev, db=None)  # type: ignore[arg-type]\n    except Exception as exc:  # noqa: BLE001\n        raise HTTPException(status_code=400, detail=f\"拆解失败: {exc}\") from exc",
      "scan": "ocr-scan-batch7-acq-route.json"
    },
    {
      "path": "backend/app/api/v1/routes/acquisition.py",
      "start_line": 573,
      "end_line": 576,
      "category": "bug",
      "severity": "medium",
      "content": "Missing `tenant_id` when creating a new OpsCard: when the card does not exist, a bare `OpsCard(inquiry_id=inquiry_id)` is constructed without a `tenant_id`. If the `OpsCard` model or downstream logic (e.g., `ops_card_store.update`, `_ops_card_payload`) relies on `tenant_id` being set, this will produce a card with `tenant_id=None` or a default, causing incorrect tenant scoping in lists/stats and potential data leakage across tenants. The same issue exists in `ops_card_quote_validity` and `ops_card_leadtime` (lines ~1100-1115).",
      "existing_code": "    card = ops_card_store.get_by_inquiry(inquiry_id)\n    if card is None:\n        from app.services.acquisition import OpsCard as _OC\n        card = _OC(inquiry_id=inquiry_id)",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/api/v1/routes/acquisition.py",
      "start_line": 600,
      "end_line": 600,
      "category": "bug",
      "severity": "medium",
      "content": "NULL POINTER RISK: In `ops_card_sample`, `card.sample` is accessed at line 600 without null check. If `card` exists but `card.sample` is None, this will raise AttributeError.",
      "existing_code": "current_status = (card.sample.status if card and card.sample else \"none\") or \"none\"",
      "scan": "ocr-scan-batch7-acq-route.json"
    },
    {
      "path": "backend/app/api/v1/routes/acquisition.py",
      "start_line": 1181,
      "end_line": 1185,
      "category": "bug",
      "severity": "medium",
      "content": "DEAD CODE: Lines 1182-1184 contain a condition check that does nothing (just `pass`). The comment says '同步跟单卡备注' but no code follows. This is unreachable logic.",
      "existing_code": "out = tender_engine.advance(body.tender_id, body.to_stage, note=body.note)\n    # 同步跟单卡备注\n    if out.get(\"ok\") and body.inquiry_id if hasattr(body, \"inquiry_id\") else False:\n        pass\n    return out",
      "scan": "ocr-scan-batch7-acq-route.json"
    },
    {
      "path": "backend/app/api/v1/routes/acquisition.py",
      "start_line": 1183,
      "end_line": 1184,
      "category": "bug",
      "severity": "medium",
      "content": "Broken ternary expression and reference to a non-existent attribute. `TenderAdvanceRequest` does not have an `inquiry_id` field, so `hasattr(body, \"inquiry_id\")` is always `False`. Due to Python operator precedence, the expression parses as `if (out.get(\"ok\") and body.inquiry_id) if (hasattr(body, \"inquiry_id\")) else False`, which always evaluates to `False`. The entire `if` block is dead code (body is `pass`), so it should be removed. If the intent was to sync a tender note to the linked ops card, the logic needs to be implemented properly with the correct field.",
      "existing_code": "    if out.get(\"ok\") and body.inquiry_id if hasattr(body, \"inquiry_id\") else False:\n        pass",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/api/v1/routes/acquisition.py",
      "start_line": 1252,
      "end_line": 1255,
      "category": "bug",
      "severity": "medium",
      "content": "Data loss: `card.risk_flags = list(body.risk_flags or [])` overwrites all previously accumulated risk flags on the card with only the flags from the current request. If the card had prior flags (e.g., `sanctions_watch` added by the sanctions screening endpoint) and the current request omits them, those flags are silently lost. Consider merging instead of overwriting: `card.risk_flags = list(set(card.risk_flags) | set(body.risk_flags or []))`.",
      "existing_code": "    card = ops_card_store.get_by_inquiry(body.inquiry_id.strip())\n    if card is not None:\n        card.risk_flags = list(body.risk_flags or [])\n        card = ops_card_store.update(card)",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/api/v1/routes/auth.py",
      "start_line": 65,
      "end_line": 66,
      "category": "other",
      "severity": "medium",
      "content": "The module-level mutable globals `_email_code_store` (dict[str, list[float]]) and `_email_code_ttl_store` (dict[str, float]) retain state across requests and are not thread-safe. Under multi-worker or async concurrency, these dicts can be corrupted or cause race conditions on the in-memory rate-limit fallback path. Consider using a thread-safe structure or ensuring the application runs single-process.",
      "existing_code": "_email_code_store: dict[str, list[float]] = defaultdict(list)\n_email_code_ttl_store: dict[str, float] = {}",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/auth.py",
      "start_line": 279,
      "end_line": 281,
      "category": "maintainability",
      "severity": "medium",
      "content": "Broad exception handling catches `Exception` alongside `jwt.InvalidTokenError`, which swallows system exceptions like `KeyboardInterrupt` and `SystemExit` that should propagate. The tuple `(jwt.InvalidTokenError, Exception)` is redundant since `Exception` covers all standard exceptions — this should just be `except Exception`. At minimum, it should not be a tuple with `Exception` as it gives a false impression of specificity while still being over-broad.",
      "existing_code": "        except (jwt.InvalidTokenError, Exception) as exc:\n            logger = logging.getLogger(__name__)\n            logger.warning(\"JWT 处理失败（可能是无效令牌）: %s\", exc)",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/payment.py",
      "start_line": 0,
      "end_line": 0,
      "category": "security",
      "severity": "medium",
      "content": "Several ops endpoints use `dependencies=[]` on the decorator, which explicitly disables any default dependencies (such as security/auth dependencies defined on the router). This is likely intentional for audit/export endpoints, but verify that these endpoints still enforce admin role checks via `_payment_ops_allowed`. If `dependencies=[]` accidentally strips authentication middleware, any unauthenticated caller could access them. Confirm the router's default dependencies are truly unwanted for these routes.",
      "existing_code": "@router.get('/ops/staging/public-reachability', ..., dependencies=[])\n@router.post('/ops/staging/public-notify-check', ..., dependencies=[])\n@router.get('/ops/audit/export', ..., dependencies=[])",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/payment.py",
      "start_line": 498,
      "end_line": 499,
      "category": "performance",
      "severity": "medium",
      "content": "Stripe session creation constructs success_url and cancel_url via f-string interpolation of `settings.SITE_URL`. If `SITE_URL` is unset or malformed, the resulting redirect URLs will be invalid and the user will be dropped after payment. Validate `settings.SITE_URL` at startup (e.g. in lifespan/onStartup) rather than failing at request time. Alternatively, fall back to a well-known endpoint or return a clear config error before invoking Stripe.",
      "existing_code": "            success_url=f'{settings.SITE_URL}/payment/success?session_id={{CHECKOUT_SESSION_ID}}',\n            cancel_url=f'{settings.SITE_URL}/payment/cancel',",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/tenants.py",
      "start_line": 389,
      "end_line": 399,
      "category": "performance",
      "severity": "medium",
      "content": "The async route `generate_tenant_site_content` (line 342) awaits `run_ai_site_builder_v1` which likely performs synchronous HTTP calls to AI services and/or database queries. If that function is a regular `def` (not `async def`), calling it with `await` will raise a `TypeError` because regular functions are not awaitable. If it is `async def` but contains blocking I/O (e.g., `requests.get`, synchronous SQLAlchemy queries), it will block the asyncio event loop, degrading concurrency for all other tenants. Confirm the signature of `run_ai_site_builder_v1` and whether it uses async-safe I/O throughout.",
      "existing_code": "result = await run_ai_site_builder_v1(\n            db,\n            tenant_id=tenant_id or \"anonymous\",\n            product_name=product_name,\n            company_name=company_name,\n            auto_save=bool(body.auto_save and tenant),\n            use_ai=use_ai,\n            persist_fn=persist_tenant_site_content if tenant else None,\n            product_images=body.product_images or None,\n         ",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/tenants.py",
      "start_line": 1078,
      "end_line": 1078,
      "category": "bug",
      "severity": "medium",
      "content": "The `/domains` GET endpoint (line 1078) and `/domains` POST endpoint (lines 1103-1106) mark all custom domains with hardcoded `verifyStatus: \"verified\"` and `sslStatus: \"active\"` without any actual verification. This misleads the frontend into treating unverified domains as fully operational, which could cause broken links or SSL errors for customers who haven't completed DNS/SSL setup. Fix: remove the false status indicators or set them to `pending`/`unverified` until actual verification is performed.",
      "existing_code": "domain_list = [{\"id\": d, \"domain\": d, \"verifyStatus\": \"verified\", \"sslStatus\": \"active\"} for d in custom_domains]",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/tenants.py",
      "start_line": 1439,
      "end_line": 1448,
      "category": "bug",
      "severity": "medium",
      "content": "In `_run_register_autopilot` (lines 1439-1448), a bare `except Exception: pass` silently swallows any failure from `build_and_persist_product_profile`. This means if the product profile build fails (e.g., due to an API error, network issue, or model misconfiguration), the error is completely invisible — no log, no alert, no user-facing signal. The caller has no way to know the autopilot step failed. Fix: at minimum log the exception (`logging.exception`) or re-raise with context.",
      "existing_code": "try:\n        build_and_persist_product_profile(\n            db,\n            tenant,\n            product_hint=product_for_autopilot,\n            location_hint=body.location_hint,\n            run_web_research=True,\n        )\n    except Exception:\n        pass",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/core/admin_auth.py",
      "start_line": 50,
      "end_line": 58,
      "category": "performance",
      "severity": "medium",
      "content": "Concurrency/Atomicity: get_user_permission_codes performs a non-atomic check-then-set pattern on Redis cache. Between the redis_client.get() and set_cache() calls, another request could update the database and invalidate the cache, leading to stale permissions being served. Additionally, there's no TTL set on cached permissions, so they persist indefinitely until explicitly invalidated. If invalidate_user_permission_cache is not called after role/permission changes (e.g., during admin operations), users may retain elevated privileges.",
      "existing_code": "    if redis_client:\n        cached = redis_client.get(cache_key)\n        if cached:\n            import json\n            return json.loads(cached)\n\n    codes = _load_permission_codes(user, db)\n    if redis_client:\n        set_cache(cache_key, codes)",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/core/admin_auth.py",
      "start_line": 221,
      "end_line": 233,
      "category": "performance",
      "severity": "medium",
      "content": "Performance: build_menu_tree calls _load_permission_codes for every recursive call, causing repeated database queries. For a menu tree with N nodes, this results in O(N) permission code lookups when only one is needed. Move the permission loading outside the recursive function or pass codes as a parameter. Also, the menu SQL query does not filter by permission at the database level - it loads all visible menus and filters in Python, which is inefficient and potentially leaks data in multi-tenant scenarios if the filtering logic is incorrect.",
      "existing_code": "    # 获取用户权限码\n    codes = _load_permission_codes(user, db)\n    has_all = \"*:*\" in codes\n    # 查询菜单\n    menus = (\n        db.query(AdminMenu)\n        .filter(\n            AdminMenu.parent_id == parent_id,\n            AdminMenu.visible == True,\n        )\n        .order_by(AdminMenu.sort_order)\n        .all()\n    )",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/core/database.py",
      "start_line": 41,
      "end_line": 46,
      "category": "bug",
      "severity": "medium",
      "content": "RoutingSession.get_bind() checks self._flushing which is an internal SQLAlchemy attribute that may not be reliable across versions. Also, if a query initiates a flush internally (e.g., lazy loading relationship), _flushing may not be set at the time get_bind is called, causing reads to incorrectly route to read_engine during write operations.",
      "existing_code": "class RoutingSession(sqlalchemy.orm.Session):\n    def get_bind(self, mapper=None, clause=None, **kwargs):\n        # 如果正在 flush（写操作），或在查询对象中标明需要写库，则路由到主库\n        if self._flushing or getattr(self, \"info\", {}).get(\"write\", False):\n            return engine\n        return read_engine",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/database.py",
      "start_line": 55,
      "end_line": 61,
      "category": "bug",
      "severity": "medium",
      "content": "Mutable list created with setdefault() on connection.info is stored as shared state. If before_cursor_execute is called before after_cursor_execute completes (e.g., nested transactions, recursive queries), the pop(-1) will mismatch with push order, causing IndexError or incorrect timing measurements. The list-based stack approach is fragile for nested operations.",
      "existing_code": "@event.listens_for(engine, \"before_cursor_execute\")\ndef before_cursor_execute(conn, cursor, statement, parameters, context, executemany):\n    conn.info.setdefault(\"query_start_time\", []).append(time.time())\n\n@event.listens_for(engine, \"after_cursor_execute\")\ndef after_cursor_execute(conn, cursor, statement, parameters, context, executemany):\n    start_time = conn.info[\"query_start_time\"].pop(-1)",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/login_bruteforce.py",
      "start_line": 0,
      "end_line": 0,
      "category": "performance",
      "severity": "medium",
      "content": "`_cleanup_stale_entries` only runs every 300 seconds and uses a cutoff of 1 hour. Entries with `lock_until=0` and `fails=0` (successfully logged-in identities) are never removed because `lock_until < now` is true but `lock_until < cutoff` is also true (0 < any positive cutoff), so they WILL be removed. Wait — actually `lock_until=0` means `0 < now` is true and `0 < cutoff` is true, so they ARE removed. This is correct. However, entries with `lock_until > 0` but `lock_until < cutoff` (expired locks older than 1 hour) are removed, which is fine. The real issue: if `_cleanup_stale_entries` is never called (e.g., if `_memory_record_failure` is never invoked after startup), the stores grow unboundedly. There is no background task or periodic trigger — cleanup only happens on demand when a failure is recorded. If an attacker only succeeds (never fails), no cleanup ever runs.",
      "existing_code": "def _cleanup_stale_entries(now: float) -> None:\n    global _last_cleanup\n    if now - _last_cleanup < _CLEANUP_INTERVAL:\n        return\n    _last_cleanup = now\n    cutoff = now - 3600.0  # 1 hour of inactivity\n    with _memory_lock:\n        for store in (_identity_store, _ip_store):\n            stale = [k for k, e in store.items()\n                     if e.lock_until < now and e.lock_until < cutof",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/login_bruteforce.py",
      "start_line": 198,
      "end_line": 204,
      "category": "bug",
      "severity": "medium",
      "content": "The Lua script uses `cjson.encode` and `cjson.decode` which are Redis-lua builtins provided by the `redis.server.exec` environment in standard Redis. However, if this service uses a Redis alternative (e.g., KeyDB, Dragonfly, or a mocked Redis in tests) that does not support `cjson`, this will fail silently or raise. Additionally, the script mutates state in two separate `eval` calls within a pipeline — if the first `eval` (identity key) succeeds but the second (IP key) fails, there is no partial-commit rollback: the identity counter is incremented but the IP counter is not, producing an inconsistent state where the identity is locked but the IP is not (or vice versa on retry). Consider using a single Lua script that handles both keys, or accept the asymmetry with a clear comment.",
      "existing_code": "    try:\n        pipe = r.pipeline(transaction=True)\n        pipe.eval(_LUA_RECORD, 1, _redis_key_id(ik),\n                  *args_id)  # type: ignore[arg-type]\n        pipe.eval(_LUA_RECORD, 1, _redis_key_ip(client_ip),\n                  *args_ip)  # type: ignore[arg-type]\n        pipe.execute()",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/login_bruteforce.py",
      "start_line": 242,
      "end_line": 250,
      "category": "other",
      "severity": "medium",
      "content": "In `_memory_check`, the IP check runs before the identity check, but if the IP is locked, the function returns immediately without checking whether the specific identity is already locked. This is correct behavior (IP lock should block all identities from that IP), but the reverse is not true: if the identity is locked but the IP is not, the function returns the identity lock status. This asymmetry is actually fine for security (identity lock is stricter), but document it to avoid confusion. More importantly, `_memory_record_failure` updates both stores under the same lock, but `_memory_check` acquires and releases the lock separately for IP and identity — this is a check-then-act race where the state could change between the two acquisitions. The window is small but real: an attacker could exploit the gap to exceed thresholds slightly.",
      "existing_code": "    with _memory_lock:\n        ip_e = _ip_store.get(client_ip)\n        if ip_e and ip_e.lock_until > t:\n            return 429, \"请求过于频繁，请稍后再试\"\n\n        ik = _identity_key(client_ip, username_or_email)\n        e = _identity_store.get(ik)\n        if e and e.lock_until > t:\n            return 429, \"请求过于频繁，请稍后再试\"",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/__init__.py",
      "start_line": 106,
      "end_line": 109,
      "category": "bug",
      "severity": "medium",
      "content": "When buyer.email is empty, key becomes (tenant_id, '') which has truthy second element '', causing `if key[1]:` to be True and enter the conflict branch — but the code then calls `.lower()` on an already-lowercased empty string which is fine, however the logic is confusing and could mask bugs. More importantly, the code creates a `self._by_email` entry with empty email as key (line 141), which pollutes the index with unresolvable lookups. Consider guarding the index update with a non-empty email check.",
      "existing_code": "        key = (buyer.tenant_id, (buyer.email or \"\").lower())\n        if key[1]:\n            existing_id = self._by_email.get(key)\n            if existing_id and existing_id != buyer.buyer_id:",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/__init__.py",
      "start_line": 515,
      "end_line": 518,
      "category": "bug",
      "severity": "medium",
      "content": "The `add_note` method silently creates a new OpsCard when inquiry_id doesn't exist (lines 516-518). This masks data integrity issues — callers expecting an existing card will get a phantom card with no tenant or buyer context. Same pattern appears in `set_owner`, `record_touch`, `record_loss`, `update_sample`, `update_fulfillment_node`, `set_research_level`, `update_payment`, `update_logistics`. These methods should either raise an exception or return None to signal the card wasn't found.",
      "existing_code": "    def add_note(self, inquiry_id: str, author: str, body: str, pinned: bool = False) -> OpsCard:\n        card = self._by_inquiry.get(inquiry_id)\n        if card is None:\n            card = OpsCard(inquiry_id=inquiry_id)",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/__init__.py",
      "start_line": 628,
      "end_line": 630,
      "category": "bug",
      "severity": "medium",
      "content": "The sample status auto-transition on line 629 only checks for 'shipped', 'delivered', 'confirmed', 'preparing' but not 'requested'. If a sample goes through 'requested -> fee_paid' it should probably also transition. This is a business-logic gap — the state machine may leave samples stuck in 'requested' even after fee collection.",
      "existing_code": "        # 收款成功可自动推进样品阶段\n        if kwargs.get(\"fee_status\") == \"paid\" and s.status in (\"shipped\", \"delivered\", \"confirmed\", \"preparing\"):\n            s.status = \"fee_collected\"",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/__init__.py",
      "start_line": 907,
      "end_line": 909,
      "category": "security",
      "severity": "medium",
      "content": "The module-level singleton stores (`buyer_store`, `ops_card_store`, `playbook_store`) are mutable shared state with no synchronization. In a multi-threaded ASGI server (like uvicorn with multiple workers), concurrent requests can race on dict mutations. While Python's GIL protects dict operations from corruption, logical races (check-then-act) are possible — especially in `BuyerMasterStore.upsert()` where two threads could both see `existing_id` as None and both insert, creating duplicate identities.",
      "existing_code": "buyer_store = BuyerMasterStore()\nops_card_store = OpsCardStore()\nplaybook_store = PlaybookStore()",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/baseline_bench.py",
      "start_line": 44,
      "end_line": 45,
      "category": "bug",
      "severity": "medium",
      "content": "The bare `except Exception` at lines 42-44 silently swallows errors during idempotency checks without logging. This hides failures that should be reported in the baseline results.",
      "existing_code": "        except Exception:\n            idem_err += 1",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/baseline_bench.py",
      "start_line": 49,
      "end_line": 49,
      "category": "bug",
      "severity": "medium",
      "content": "Lines 52-53: The P95 index calculation `int(len(lat_ms) * 0.95) - 1` can produce negative indices for small lists. For example, with len=1, this gives index 0 (correct), but with len=2 and the formula, it could produce -1 which wraps to the last element. Consider using `max(0, int(len * 0.95) - 1)` or a different percentile calculation approach.",
      "existing_code": "    p95 = lat_ms[int(len(lat_ms) * 0.95) - 1] if len(lat_ms) >= 2 else (lat_ms[0] if lat_ms else 0)",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/billing_explain.py",
      "start_line": 114,
      "end_line": 115,
      "category": "maintainability",
      "severity": "medium",
      "content": "The `except Exception:` on line 88-89 silently swallows all exceptions from the meter query block without logging. If MeterEvent model doesn't exist or the query fails, the error is invisible. At minimum, add a warning log like the token_ledger block does (line 73).",
      "existing_code": "    except Exception:\n        pass",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/billing_explain.py",
      "start_line": 114,
      "end_line": 115,
      "category": "maintainability",
      "severity": "medium",
      "content": "The `except Exception:` on lines 107-108 silently swallows all exceptions from the acquisition query block without logging. If EvolutionTaskRecord model doesn't exist or the query fails, the error is invisible. Add a warning log for consistency with the token_ledger block.",
      "existing_code": "    except Exception:\n        pass",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/billing_explain.py",
      "start_line": 134,
      "end_line": 134,
      "category": "bug",
      "severity": "medium",
      "content": "The `success` attribute access on line 109 uses `getattr(r, 'success', True)` which defaults to True when the attribute doesn't exist. This means if the model schema changes or the field is missing, failed tasks will be displayed as '成功' (success), which is misleading. Consider defaulting to False or handling missing attributes explicitly.",
      "existing_code": "            \"plain\": f\"获客动作 · {_reason_plain(t)} · {'成功' if getattr(r, 'success', True) else '失败/诚实降级'}\",",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/dispatch_service.py",
      "start_line": 110,
      "end_line": 113,
      "category": "maintainability",
      "severity": "medium",
      "content": "The broad except on lines 105-112 catches all exceptions from parse_graph_to_tasks but only records a truncated message. The original traceback is lost (no `raise ... from exc`), making postmortems difficult. Also, no logger.exception call preserves the full traceback for debugging.",
      "existing_code": "    except Exception as exc:  # noqa: BLE001\n        logger.exception(\"dispatch_acquisition 落库失败\")\n        result[\"dispatch_error\"] = str(exc)[:200]\n        result[\"persistence_note\"] = \"任务图落库失败\"",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 93,
      "end_line": 106,
      "category": "bug",
      "severity": "medium",
      "content": "The variable `currency` is passed into the function but only used inside `record_ops_win`. However, in line 103-104, when `amount` is truthy and `currency` is falsy/empty, the fallback uses `'USD'`. This is inconsistent because the parameter default is `'USD'` — but if a caller explicitly passes `currency=''`, it falls back to `'USD'` via the `or 'USD'` expression, which silently overrides an explicit empty value. More importantly, `currency` is not passed to `record_acquisition_event`, so any non-default currency is silently discarded. Consider whether currency should be included in the detail string or persisted separately.",
      "existing_code": "def record_ops_win(\n    db: Any,\n    *,\n    tenant_id: str,\n    inquiry_id: str,\n    amount: float = 0,\n    currency: str = \"USD\",\n    note: str = \"\",\n    won_reasons: list[str] | None = None,\n) -> dict[str, Any]:\n    \"\"\"P2-2 成交入经验环（Win）。\"\"\"\n    parts = []\n    if amount:\n        parts.append(f\"amount={amount}{currency or 'USD'}\")",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 105,
      "end_line": 106,
      "category": "bug",
      "severity": "medium",
      "content": "Lines 105-106: When `amount` is truthy but `currency` is an empty string `''`, the expression `currency or 'USD'` correctly falls back to `'USD'`. However, `currency` is never passed through to `record_acquisition_event`, so any non-default currency information is silently dropped from the evolution record. If multi-currency support is intended, `currency` should be included in the `metadata` dict or the `detail` string explicitly. As written, the detail line `f\"amount={amount}{currency or 'USD'}\"` does include the currency in the text, but structurally it is buried in a free-text `detail` field rather than being stored as a proper field.",
      "existing_code": "    if amount:\n        parts.append(f\"amount={amount}{currency or 'USD'}\")",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 125,
      "end_line": 127,
      "category": "security",
      "severity": "medium",
      "content": "Line 125: `from app.services.acquisition.loss_report import REASON_HINTS` is imported inside the function body on every call. While this avoids circular import issues, it means the import happens repeatedly. More importantly, both `experience_feed.py` and `loss_report.py` appear to be in the same package (`app.services.acquisition`). If `loss_report.py` imports anything from `experience_feed.py` at module level, this creates a circular dependency that could fail at runtime. Verify that `loss_report.py` does not transitively import from `experience_feed.py`.",
      "existing_code": "    from app.services.acquisition.loss_report import REASON_HINTS\n\n    items = list(cards or [])",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 129,
      "end_line": 129,
      "category": "bug",
      "severity": "medium",
      "content": "The `lost` filter includes any card with a truthy `loss_reasons` attribute regardless of its `stage`. If a card reaches `stage == \"won\"` but still carries a non-empty `loss_reasons` list (e.g., reasons were recorded early and never cleared), it will appear in both the `won` and `lost` lists, inflating `lost_count` and skewing the `loss_reasons` distribution. Exclude cards already in the `won` set:",
      "existing_code": "    lost = [c for c in items if getattr(c, \"stage\", \"\") == \"lost\" or getattr(c, \"loss_reasons\", None)]",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/services/acquisition/fulfillment_nodes.py",
      "start_line": 108,
      "end_line": 108,
      "category": "bug",
      "severity": "medium",
      "content": "The balance_status guard uses `not in (\"paid\",)` which silently allows None (missing balance_status attribute) into the reminder path as non-paid. Because `\"\" or \"\"` earlier normalizes missing balance_status to `\"\"`, this is reachable. The comparison should explicitly handle None/missing rather than relying on normalization.",
      "existing_code": "                or (key == \"balance\" and deposit_paid and balance_status not in (\"paid\",))",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/fulfillment_nodes.py",
      "start_line": 109,
      "end_line": 109,
      "category": "bug",
      "severity": "medium",
      "content": "Missing `stage` in reminder key check: the earlier loop considers `stage` for quote done, but the reminder logic checks `key in (\"pi\", \"deposit\")` and then inside has a pi branch. If stage is something like `\"won\"` the PI may be done while deposit reminder still fires. Consider whether `stage` should gate the deposit reminder condition.",
      "existing_code": "                or (key == \"pi\" and not pi_no and stage not in (\"new\", \"lost\"))",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/growth_ops.py",
      "start_line": 0,
      "end_line": 0,
      "category": "security",
      "severity": "medium",
      "content": "Id generation uses hex[:10] from uuid4, producing only 40 bits of entropy. If these IDs are exposed in URLs or APIs, an attacker could enumerate all content_ids and slot_ids by brute-force guessing.",
      "existing_code": "cid = content_id or f\"cnt-{uuid4().hex[:10]}\"\nand:\nslot_id: str = field(default_factory=lambda: f\"ip-{uuid4().hex[:8]}\")",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/growth_ops.py",
      "start_line": 60,
      "end_line": 63,
      "category": "bug",
      "severity": "medium",
      "content": "link_inquiry calls upsert_content before any validation of the inquiry_id, which means a phantom ContentTouch entry is created even when linking fails (e.g., if content_id doesn't exist or inquiry_id is invalid). The early return on missing ids happens after the mutation.",
      "existing_code": "def link_inquiry(self, *, content_id: str, inquiry_id: str, tenant_id: str = \"\") -> dict[str, Any]:\n    if not content_id or not inquiry_id:\n        return {\"linked\": False, \"reason\": \"content_id/inquiry_id required\"}\n    item = self.upsert_content(content_id=content_id, tenant_id=tenant_id)",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/intent_classifier.py",
      "start_line": 19,
      "end_line": 19,
      "category": "bug",
      "severity": "medium",
      "content": "The 'competitor' keyword in reject_competitor can cause false positives in neutral or positive contexts where the word appears without rejection intent (e.g., 'We also work with competitor brands'). This keyword is broad and should either be qualified with negative terms or removed in favor of more specific language.",
      "existing_code": "        [\"别家\", \"已订\", \"已采购\", \"不要了\", \"不需要\", \"competitor\", \"already ordered\", \"not interested\"],",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/intent_classifier.py",
      "start_line": 96,
      "end_line": 96,
      "category": "bug",
      "severity": "medium",
      "content": "When multiple intents have the same keyword hit count, only the first matched intent (in rule order) is selected. This means rule ordering silently determines outcomes without documentation. For example, if both `generic_interest` and `request_quote` match equally, `generic_interest` wins simply because it appears first. Consider adding tie-breaking logic or logging the ambiguity.",
      "existing_code": "        elif len(hits) == best_hits and hits and best is None:",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/knowledge_queue.py",
      "start_line": 193,
      "end_line": 196,
      "category": "maintainability",
      "severity": "medium",
      "content": "Error response shapes are inconsistent: `not_found` returns `{ok: False, ...}` while `already_done` returns `{ok: True, ...}` with the item. This forces clients to check both `ok` and `code` to distinguish between missing and already-processed items. Consider a uniform error shape or explicit success wrapping for all cases.",
      "existing_code": "if it is None:\n            return {\"ok\": False, \"code\": \"not_found\", \"message\": f\"无此知识条目 {item_id}\"}\n        if it.done:\n            return {\"ok\": True, \"code\": \"already_done\", \"message\": f\"「{it.title}」已读过\", \"item\": it.__dict__}",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/loss_report.py",
      "start_line": 54,
      "end_line": 64,
      "category": "bug",
      "severity": "medium",
      "content": "When total_lost is 0 (empty cards or no lost-stage items), counter is empty so sum(counter.values()) is 0, but `or 1` forces total_marks to 1. As a result, distribution will be [] (no items), top_reason='', but total_reason_marks=1 and every returned consumer sees a non-zero denominator with no backing data. This silently masks the zero-state and could mislead downstream consumers that trust total_reason_marks as an accuracy indicator.",
      "existing_code": "    total_marks = sum(counter.values()) or 1\n    distribution = [\n        {\n            \"reason\": reason,\n            \"count\": count,\n            \"percent\": round(count * 100.0 / total_marks, 1),\n            \"hint\": REASON_HINTS.get(reason, \"结合个案复盘话术与报价结构\"),\n        }\n        for reason, count in counter.most_common()\n    ]\n    top = distribution[0][\"reason\"] if distribution else \"\"",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/nps_rescue.py",
      "start_line": 0,
      "end_line": 0,
      "category": "bug",
      "severity": "medium",
      "content": "The should_notify logic uses `bucket == 'detractor'` which depends on the potentially truncated score. If nps_score is float (e.g., 6.9), truncation to 6 creates a detractor bucket, but rounding would give 7 (passive). This could cause unexpected notifications. The same issue affects the plain text description which uses the truncated value.",
      "existing_code": "        should_notify: bool = usage_low or (nps_block.get(\"collected\") and nps_block.get(\"bucket\") == \"detractor\"),",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/nps_rescue.py",
      "start_line": 34,
      "end_line": 44,
      "category": "maintainability",
      "severity": "medium",
      "content": "The broad `except Exception: pass` at lines 33-42 silently swallows all errors from ops_store processing (e.g., AttributeError, TypeError, configuration failures). This makes it impossible to diagnose why card/touch/won/lost counts might unexpectedly be zero. Consider logging the exception or at least narrowing the catch to specific expected exceptions.",
      "existing_code": "    if ops_store is not None:\n        try:\n            cards = list(getattr(ops_store, \"_by_inquiry\", {}).values())\n            if tenant_id:\n                cards = [c for c in cards if getattr(c, \"tenant_id\", \"\") in (\"\", tenant_id)]\n            card_n = len(cards)\n            touch_n = sum(1 for c in cards if getattr(c, \"last_touch_at\", \"\"))\n            won_n = sum(1 for c in cards if getattr(c,",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/nps_rescue.py",
      "start_line": 37,
      "end_line": 38,
      "category": "bug",
      "severity": "medium",
      "content": "Ambiguous tenant filtering when `tenant_id` is empty or specific: the condition `if not tenant_id or getattr(c, \"tenant_id\", \"\") in (\"\", tenant_id)` includes empty string in the match tuple, treating unassigned/empty-tenant cards as valid matches for any specific tenant. This can leak cross-tenant or unassigned cards. If including unassigned is intentional, document it; otherwise remove `\"\"` from the tuple to match only explicit tenant IDs.",
      "existing_code": "            if tenant_id:\n                cards = [c for c in cards if getattr(c, \"tenant_id\", \"\") in (\"\", tenant_id)]",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/services/acquisition/nps_rescue.py",
      "start_line": 43,
      "end_line": 44,
      "category": "maintainability",
      "severity": "medium",
      "content": "Silent `except Exception: pass` swallows every error from reading `_by_inquiry` and card enumeration, leaving counters and onboarding state at defaults with zero diagnostic information. This makes it impossible to distinguish \"no data\" from \"data failed to load\". At minimum log the exception or attach it to the returned structure so operators can detect silent data-load failures.",
      "existing_code": "        except Exception:\n            pass",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/services/acquisition/nps_rescue.py",
      "start_line": 79,
      "end_line": 79,
      "category": "bug",
      "severity": "medium",
      "content": "The NPS score normalization uses `int()` which truncates rather than rounds non-integer scores. A score of 8.9 would become 8 instead of 9, potentially misclassifying a promoter as a passive. Also, values outside 0-10 are clamped silently without warning, which may hide data quality issues. Consider using round() and logging out-of-range values.",
      "existing_code": "        s = max(0, min(10, int(nps_score)))",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/onboarding.py",
      "start_line": 77,
      "end_line": 87,
      "category": "bug",
      "severity": "medium",
      "content": "The bare `except Exception:` with `pass` on line 56-64 silently swallows all errors during ops_store inspection — including KeyError, AttributeError, TypeError, or even KeyboardInterrupt if the interpreter is misconfigured. This makes debugging invalid store shapes impossible and hides real failures. At minimum use `except Exception as e:` and log the error. If the intent is truly best-effort, catch only the expected exceptions or use a narrower try block.",
      "existing_code": "        try:\n            cards = [\n                c\n                for c in getattr(ops_store, \"_by_inquiry\", {}).values()\n                if not tenant_id or getattr(c, \"tenant_id\", \"\") in (\"\", tenant_id)\n            ]\n            has_card = len(cards) > 0\n            has_inquiry = any(getattr(c, \"last_summary\", \"\") or getattr(c, \"buyer_display\", \"\") for c in cards)\n            has_touch = any(",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/ops_card_pg.py",
      "start_line": 0,
      "end_line": 0,
      "category": "performance",
      "severity": "medium",
      "content": "Re-running DDL on every write: save_ops_card executes CREATE TABLE IF NOT EXISTS DDL before each upsert. This adds unnecessary database round-trips and latency on every save operation. The table should only be created once during initialization, not on every write.",
      "existing_code": "    try:\n        from sqlalchemy import text\n        session.execute(text(DDL))\n        payload = card.to_dict()",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/ops_card_pg.py",
      "start_line": 45,
      "end_line": 46,
      "category": "maintainability",
      "severity": "medium",
      "content": "Generic exception handling: The bare except Exception blocks catch all exceptions but only log warnings, making it difficult to diagnose serialization failures, constraint violations, or connection issues.",
      "existing_code": "    except Exception as exc:  # noqa: BLE001\n        logger.warning(\"ensure ops_cards table: %s\", exc)",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/ops_card_pg.py",
      "start_line": 176,
      "end_line": 176,
      "category": "bug",
      "severity": "medium",
      "content": "Fragile dataclass construction from serialized dicts: passing raw keys from JSON/PG rows directly into `OpsCardLogistics`, `OpsCardSkuLine`, etc., via `**` unpacking will raise `TypeError` if stored rows contain fields that no longer exist on the dataclass (schema drift). This error is silently swallowed, causing data loss. Filter keys against the dataclass's actual `dataclasses.fields` before constructing.",
      "existing_code": "        card.logistics = OpsCardLogistics(**logistics) if logistics else OpsCardLogistics()",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/services/acquisition/ops_card_pg.py",
      "start_line": 209,
      "end_line": 211,
      "category": "other",
      "severity": "medium",
      "content": "restore_ops_card silently discards newer in-memory state: If a card is evicted from cache and restored from PG, there's no version check to ensure we're not overwriting newer in-memory changes with stale PG data.",
      "existing_code": "        store._by_inquiry[inquiry_id] = card\n        store._by_id[card.card_id] = card\n        return card",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/ops_card_pg.py",
      "start_line": 224,
      "end_line": 230,
      "category": "other",
      "severity": "medium",
      "content": "The patch_store_persistence function wraps orig_update and tries to save the returned value, but orig_update might return None or a different object type, causing save_ops_card to fail silently.",
      "existing_code": "    def update(card):\n        out = orig_update(card)\n        try:\n            save_ops_card(out)\n        except Exception:\n            pass\n        return out",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/ops_card_pg.py",
      "start_line": 226,
      "end_line": 230,
      "category": "maintainability",
      "severity": "medium",
      "content": "The update wrapper silently swallows all exceptions from save_ops_card with a bare 'pass', contradicting the module's stated principle of 'failing honestly' (no pretending to have persisted). save_ops_card already logs and returns a structured result dict; discarding it makes persistence failures invisible at the call site and complicates debugging of data loss. At minimum, log the failure so operators can detect that the in-memory update succeeded but the PG write did not.",
      "existing_code": "        try:\n            save_ops_card(out)\n        except Exception:\n            pass\n        return out",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/services/acquisition/payment_risk.py",
      "start_line": 71,
      "end_line": 71,
      "category": "bug",
      "severity": "medium",
      "content": "Line 71: String matching is case-sensitive for 'payment' (`r.lower()`) but case-insensitive for '付款' (uses `in` operator). If loss_reasons contains mixed-case 'Payment', it would be missed. Also, '付款' is a Chinese substring check that could match unintended strings (e.g., '不付款说明').",
      "existing_code": "                if any(\"付款\" in r or \"payment\" in r.lower() for r in loss):",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/sales_collision.py",
      "start_line": 0,
      "end_line": 0,
      "category": "bug",
      "severity": "medium",
      "content": "Inconsistent audit record shape: claimed_new returns a dict with keys {type, from, to, at, note}, while claimed_empty and handed_off return entries from card.handoff_history which may have a different structure. Consumers of the audit field cannot rely on a consistent schema across code paths. The claimed_new audit entry uses `type` key but others may not. Consider normalizing all audit records to the same shape.",
      "existing_code": "            \"audit\": [{\n                \"type\": \"claim_new\",\n                \"from\": \"\",\n                \"to\": user_id,\n                \"at\": _now(),\n                \"note\": note or \"新建认领\",\n            }],\n        }\n\n    current = (card.owner_user_id or \"\").strip()\n    if not current:\n        card = ops_store.set_owner(inquiry_id, user_id, note=note or \"认领无主询盘\")\n        return {\n            \"ok\": ",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/sales_collision.py",
      "start_line": 109,
      "end_line": 112,
      "category": "maintainability",
      "severity": "medium",
      "content": "collision_report reads ops_store._by_inquiry (private attribute) directly instead of using a public API method. This couples the function to internal implementation details of ops_store. If the storage layer changes its internal structure (e.g., renames the dict, moves to a database query), this function will silently return empty results rather than raising an error. Consider adding a public method to ops_store for retrieving all cards or collision history.",
      "existing_code": "def collision_report(ops_store: Any, tenant_id: str = \"\") -> dict[str, Any]:\n    \"\"\"团队撞单/交接汇总（只读）。\"\"\"\n    items = []\n    for card in getattr(ops_store, \"_by_inquiry\", {}).values():",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/sample_flow.py",
      "start_line": 64,
      "end_line": 69,
      "category": "maintainability",
      "severity": "medium",
      "content": "can_transition returns True for self-loops (from_status == to_status), which bypasses the _TRANSITIONS validation. This may allow unintended state changes where a sample transitions from status X to the same status X without being a valid transition. Consider whether this behavior is intentional or if it should reject same-status transitions.",
      "existing_code": "def can_transition(from_status: str, to_status: str) -> bool:\n    if from_status == to_status:\n        return True\n    if to_status not in SAMPLE_STATUSES:\n        return False\n    return to_status in _TRANSITIONS.get(from_status, set())",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/suppression_list.py",
      "start_line": 38,
      "end_line": 49,
      "category": "security",
      "severity": "medium",
      "content": "Permissive email validation: The check `not email_n or '@' not in email_n` only verifies the presence of an '@' character. This accepts malformed inputs like '@', 'a@b', or 'user@localhost', which are not valid RFC 5322 emails. While this is a suppression list and not a sending endpoint, invalid entries could clutter the store or be used to bypass suppression checks (e.g., adding 'user@localhost' to suppress 'USER@localhost.COM'). Consider using a proper email validation library or at least requiring a non-empty local part and domain with a TLD.",
      "existing_code": "    def add(\n        self,\n        *,\n        email: str,\n        tenant_id: str = \"demo\",\n        reason: str = \"unsubscribe\",\n        source: str = \"\",\n        created_by: str = \"system\",\n    ) -> dict[str, Any]:\n        email_n = self._norm(email)\n        if not email_n or \"@\" not in email_n:\n            return {\"ok\": False, \"code\": \"invalid_email\", \"message\": \"邮箱格式不合法\"}",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/suppression_list.py",
      "start_line": 165,
      "end_line": 166,
      "category": "maintainability",
      "severity": "medium",
      "content": "Global mutable singleton limits testability: The module-level `suppression_store` is a global mutable object. Tests cannot isolate state between test cases without clearing or reinitializing this singleton. There is no cleanup method or context manager to reset the store. This makes unit testing difficult and risks test interference in multi-test scenarios. Consider adding a `reset()` class method or using dependency injection.",
      "existing_code": "# 全局单例（生产可换 DB 表）\nsuppression_store = SuppressionStore()",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/template_weights.py",
      "start_line": 71,
      "end_line": 72,
      "category": "other",
      "severity": "medium",
      "content": "Lines 49-58: The broad `except Exception` on ops_store iteration silently swallows all errors including potential data corruption or interface contract violations. Consider narrowing to specific exceptions or at minimum logging the error with more context.",
      "existing_code": "        except Exception as exc:  # noqa: BLE001\n            logger.debug(\"ops_store 统计失败: %s\", exc)",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/template_weights.py",
      "start_line": 78,
      "end_line": 78,
      "category": "security",
      "severity": "medium",
      "content": "Lines 64-65: The `from sqlalchemy import or_` import is inside the try block but `or_` is never used in the query. More critically, if SQLAlchemy is not installed, this ImportError would bypass the `except Exception` and escape as an unhandled import failure.",
      "existing_code": "            from sqlalchemy import or_",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/adapters/tradeai/__init__.py",
      "start_line": 65,
      "end_line": 74,
      "category": "security",
      "severity": "medium",
      "content": "The SECRET_KEY fallback logic at lines 61-70 has a subtle issue: when TRADEAI_SECRET_KEY is set but SECRET_KEY is already in os.environ (from a parent process), the dedicated key is ignored because `os.environ.get(\"SECRET_KEY\")` returns the parent's value, not None. This means the trade-ai-agent's security configuration could silently use the wrong SECRET_KEY. Additionally, the dev placeholder string is logged in the warning message, which could appear in logs.",
      "existing_code": "    if not os.environ.get(\"SECRET_KEY\"):\n        dedicated = os.environ.get(\"TRADEAI_SECRET_KEY\", \"\").strip()\n        if dedicated:\n            os.environ[\"SECRET_KEY\"] = dedicated\n        else:\n            os.environ[\"SECRET_KEY\"] = \"tradeai-adapter-dev-key-change-in-prod\"\n            logger.warning(\n                \"tradeai adapter: SECRET_KEY 未配置，使用开发占位值；\"\n                \"生产环境必须设置 TRADEAI_SECR",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/adapters/tradeai/__init__.py",
      "start_line": 138,
      "end_line": 146,
      "category": "other",
      "severity": "medium",
      "content": "The `finally` block in `_load_vendor_isolated` (lines 130-136) restores saved modules and removes the vendor path from sys.path. However, if an exception occurs DURING the restore phase (e.g., `sys.modules.update(saved)` raises), the original app modules may be partially lost, and the vendor path may remain in sys.path. This is a fragile error recovery path that could leave the process in an inconsistent import state.",
      "existing_code": "    except Exception as e:  # pragma: no cover\n        logger.error(\"tradeai adapter: vendor 加载失败 %s: %s\", type(e).__name__, e)\n        return False\n    finally:\n        sys.modules.update(saved)\n        try:\n            sys.path.remove(str(_backend_path))\n        except ValueError:\n            pass",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/adapters/tradeai/__init__.py",
      "start_line": 149,
      "end_line": 149,
      "category": "other",
      "severity": "medium",
      "content": "The module-level execution at line 139 (`_loaded = _load_vendor_isolated()`) runs during import time. If this adapter module is imported in a multi-threaded context (e.g., concurrent workers importing simultaneously), there's a race condition on `_loaded` and the side effects in `_load_vendor_isolated` (sys.path manipulation, sys.modules mutation). The guard `if f\"{_VENDOR_PREFIX}.app\" in sys.modules` helps but isn't fully thread-safe without a lock.",
      "existing_code": "_loaded = _load_vendor_isolated()",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/goodjob/customer_pool_projection.py",
      "start_line": 57,
      "end_line": 59,
      "category": "bug",
      "severity": "medium",
      "content": "Tenant ID validation only rejects empty strings, but whitespace-only values (e.g., '   ') pass through. This can break downstream tenant isolation assumptions. Use `.strip()` on the tenant_id and reject if the stripped result is empty, not just the original.",
      "existing_code": "    tenant_id = _attr_str(row, \"tenant_id\").strip()\n    if not tenant_id:\n        raise ValueError(\"询盘缺 tenant_id，拒绝投影（多租户隔离红线）\")",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/goodjob/customer_pool_projection.py",
      "start_line": 83,
      "end_line": 89,
      "category": "bug",
      "severity": "medium",
      "content": "The idempotency key is truncated to 128 characters using simple slicing, which can cause collisions between different (inquiry_id, status, updated_at) combinations. This may lead to missed updates or duplicate suppression. Consider using a hash (e.g., base64-encoded SHA-256) instead of truncation to ensure uniqueness within the length constraint.",
      "existing_code": "def projection_idempotency_key(row: Any) -> str:\n    \"\"\"确定性幂等键：同询盘同状态同更新时间重放自动去重。\"\"\"\n    key = (\n        f\"gj-pool:{_attr_str(row, 'id')}:{_attr_str(row, 'status')}:\"\n        f\"{_attr_iso(row, 'updated_at') or _attr_iso(row, 'created_at')}\"\n    )\n    return key[:_KEY_MAX_LEN]",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/goodjob/customer_pool_projection.py",
      "start_line": 92,
      "end_line": 106,
      "category": "performance",
      "severity": "medium",
      "content": "The function docstring claims fail-safe behavior, but there's no try/except around `build_projection()` or `executor.submit()`. If either raises an exception, it will propagate to the caller and break the inquiry main flow, violating the documented contract. Add appropriate exception handling with logging to ensure silent failure on errors.",
      "existing_code": "def sync_inquiry_to_pool(executor: GoodJobExecutor, row: Any) -> str | None:\n    \"\"\"把询盘投影推送到 GoodJob 客户池；桥禁用时返回 None 静默跳过。\"\"\"\n    if not executor.enabled:\n        return None\n    projection = build_projection(row)\n    package = TaskPackage(\n        tenant_id=projection[\"tenant_id\"],\n        idempotency_key=projection_idempotency_key(row),\n        lease_ttl=_LEASE_TTL_SECONDS,\n        checkpoint=\"v",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/goodjob/trade_document_bridge.py",
      "start_line": 128,
      "end_line": 132,
      "category": "bug",
      "severity": "medium",
      "content": "The step_number parameter is cast to int but not validated for non-positive values. A step_number of 0 or negative could represent an invalid lifecycle step. Consider adding validation such as: if step_number < 1: raise ValueError(...)",
      "existing_code": "    stage = str(stage or \"\").strip()\n    sync_payload = {\n        \"order_id\": order_id,\n        \"stage\": stage,\n        \"step_number\": int(step_number),",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/goodjob/trade_document_bridge.py",
      "start_line": 132,
      "end_line": 132,
      "category": "bug",
      "severity": "medium",
      "content": "int(step_number) can raise ValueError or TypeError if step_number is a non-numeric string. This exception is not caught and will propagate unhelpfully. Consider wrapping in a try/except with a clear error message.",
      "existing_code": "        \"step_number\": int(step_number),",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/hermes/harness_gateway.py",
      "start_line": 55,
      "end_line": 63,
      "category": "security",
      "severity": "medium",
      "content": "Logging raw_input without sanitization: Line 61 stores `raw_text` in the event's payload dict under \"raw_input\". If this IntentEvent is logged (e.g., in decompose or downstream services), sensitive user input could leak into log files or external monitoring systems. Consider whether raw_input should be included in persisted/logged event data.",
      "existing_code": "event = IntentEvent(\n            # uuid 而非 hash()：保证跨进程稳定且唯一（原实现用 hash 是 bug）\n            event_id=f\"evt_{uuid.uuid4().hex[:12]}\",\n            tenant_id=str(tenant_id),\n            channel=channel or \"web\",\n            intent=(intent or text),\n            payload={**(payload or {}), \"raw_input\": text},\n            context=context or {},\n        )",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/hermes/harness_gateway.py",
      "start_line": 57,
      "end_line": 57,
      "category": "security",
      "severity": "medium",
      "content": "Lines 46-47: `event_id=f\"evt_{uuid.uuid4().hex[:12]}\"` truncates the UUID to 12 hex characters (48 bits). In a multi-worker deployment with high throughput, the collision probability increases significantly. With 2^48 possible values, at 1 million events you have ~0.03% collision probability; at 10 million events, ~30% collision probability. For an event_id that should be globally unique and stable for idempotency, this truncation is risky. Consider using the full 32-char hex UUID instead.",
      "existing_code": "            event_id=f\"evt_{uuid.uuid4().hex[:12]}\",",
      "scan": "ocr-scan-batch4.json"
    },
    {
      "path": "backend/app/services/hermes/harness_gateway.py",
      "start_line": 61,
      "end_line": 61,
      "category": "security",
      "severity": "medium",
      "content": "Lines 56-60: While raw_text is not directly logged, the `payload` dict passed to IntentEvent includes `raw_input` which contains the user's raw text. If the IntentEvent is ever serialized and logged (e.g., in exception tracebacks or debug output), PII could be exposed. Consider whether raw user input should be included in the event payload or sanitized before storage/serialization.",
      "existing_code": "            payload={**(payload or {}), \"raw_input\": text},",
      "scan": "ocr-scan-batch4.json"
    },
    {
      "path": "backend/app/services/hermes/harness_gateway.py",
      "start_line": 114,
      "end_line": 114,
      "category": "security",
      "severity": "medium",
      "content": "Line 95: `intent=str(intent) if intent else None` - if `intent` is a non-string truthy value (e.g., int, bool), it's silently coerced to string. This could produce unexpected results like `'1'` or `'True'`. Since the upstream type contract is `Optional[str]`, consider either: (a) validating intent is already a string, or (b) raising ValueError if intent is not a string. Current silent coercion violates type safety.",
      "existing_code": "        intent=str(intent) if intent else None,",
      "scan": "ocr-scan-batch4.json"
    },
    {
      "path": "backend/app/services/hermes/task_control_supervisor.py",
      "start_line": 266,
      "end_line": 269,
      "category": "bug",
      "severity": "medium",
      "content": "Multiple locations use bare `except Exception` with only logging, which can hide real configuration or data errors. Specifically: line 138-144 (policies parsing), line 213-214 (budget_used sum), line 257-258 (Paperclip approval), line 282-283 (compensation), and line 324-325 (requery). The budget_used case is particularly concerning as it silently disables a security gate. Consider narrowing exception types and adding more specific error handling.",
      "existing_code": "    try:\n        policies = (json.loads(plan_task.input_json or \"{}\") or {}).get(\"policies\") or {}\n    except Exception:  # noqa: BLE001 — 策略解析失败不阻断调度\n        policies = {}",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/hermes/task_control_supervisor.py",
      "start_line": 281,
      "end_line": 281,
      "category": "bug",
      "severity": "medium",
      "content": "The `advance_plan` function has a check-check-act race condition: it reads `running_now` and then dispatches tasks, but between these operations another process could dispatch additional tasks. The comment about committing to release row locks (line 335) acknowledges this but doesn't fully prevent it. Consider using database-level constraints or distributed locks for the concurrency gate.",
      "existing_code": "    running_now = sum(1 for t in child_tasks if t.status == \"running\")",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/hermes/task_control_supervisor.py",
      "start_line": 382,
      "end_line": 391,
      "category": "bug",
      "severity": "medium",
      "content": "After committing to release row locks, the function re-queries child tasks. Newly dispatched tasks from the current cycle may still be in `running` status, which would cause `all_terminal` to remain False and the plan stays in `executing` even though the current batch completed. While this may be intentional for streaming progress, it can also prevent the plan from being marked `done` in single-node DAGs if the Celery task hasn't updated status yet at requery time.",
      "existing_code": "    # 6. 重新检查全计划是否全部完成\n    child_tasks = db.query(AiTask).filter(AiTask.parent_task_id == plan_task.id).all()\n    all_terminal = True\n    all_succeeded = True\n    for task in child_tasks:\n        if task.status not in (\"done\", \"skipped\", \"failed\", \"cancelled\"):\n            all_terminal = False\n            break\n        if task.status in (\"failed\", \"cancelled\"):\n            all_succeeded = False",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/oauth_login.py",
      "start_line": 0,
      "end_line": 0,
      "category": "security",
      "severity": "medium",
      "content": "Dev mode authorize URL encodes provider and code directly in query parameters without any signature or encryption. While low-risk in development, this allows trivial state forgery and code replay. If this pattern is ever replicated in production code, it would be a serious vulnerability. Consider at least signing the state parameter even in dev mode to establish the correct pattern.",
      "existing_code": "def _dev_authorize_url(provider: str, state: str) -> str:\n    q = urllib.parse.urlencode(\n        {\n            \"provider\": provider,\n            \"code\": f\"dev:{provider}\",\n            \"state\": state,\n        }\n    )\n    return f\"{_redirect_uri()}?{q}\"",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/oauth_login.py",
      "start_line": 113,
      "end_line": 128,
      "category": "security",
      "severity": "medium",
      "content": "Security: Dev mode bypass exposes `code` in the redirect URL query string via `_dev_authorize_url`. Although this only applies in non-production environments, the callback URL (`/login/oauth-callback?provider=...&code=dev:...&state=...`) may be logged in server access logs, browser history, or Referer headers. For WeChat dev mode, the callback can also resolve to an actual founder account ID (`expected_founder_wechat_provider_id()`), potentially leaking real internal account identifiers through the URL. Consider hashing or opaque encoding the dev code rather than embedding it in the query string, and ensure dev callback endpoints do not log the full URL.",
      "existing_code": "def _dev_authorize_url(provider: str, state: str) -> str:\n    \"\"\"_dev_authorize_url。\n\n    参数说明：\n    :param provider: 参数 provider\n    :param state: 参数 state\n    :return: 返回处理结果。\n    \"\"\"\n    q = urllib.parse.urlencode(\n        {\n            \"provider\": provider,\n            \"code\": f\"dev:{provider}\",\n            \"state\": state,\n        }\n    )\n    return f\"{_redirect_uri()}?{q}\"",
      "scan": "ocr-scan-batch4.json"
    },
    {
      "path": "backend/app/services/oauth_login.py",
      "start_line": 426,
      "end_line": 449,
      "category": "security",
      "severity": "medium",
      "content": "Security/Logging: PII (openid prefixes) leaked into logs. Lines 297, 327, 368, and ~413 all log `openid[:8]` which leaks the first 8 characters of a user's OpenID — a persistent, globally unique identifier tied to a real person (QQ and WeChat OAuth paths). While not a secret token, it is PII and could be used for user tracking across services. Mask or hash the identifier before logging (e.g. `hashlib.sha256(openid.encode()).hexdigest()[:8]`), or omit it entirely.",
      "existing_code": "        logger.info(\n            \"[QQ OAuth] Token 获取成功 (expires_in=%ss)\",\n            token_body.get(\"expires_in\", \"?\"),\n        )\n        # ── Step 2: access_token → openid ──\n        logger.info(\"[QQ OAuth] Fetching openid...\")\n        openid_r = client.get(\n            \"https://graph.qq.com/oauth2.0/me\",\n            params={\"access_token\": access, \"fmt\": \"json\"},\n        )\n        openid_r.rai",
      "scan": "ocr-scan-batch4.json"
    },
    {
      "path": "backend/app/services/oauth_login.py",
      "start_line": 465,
      "end_line": 467,
      "category": "bug",
      "severity": "medium",
      "content": "Bug: The QQ response parser regex `^\\s*\\w+\\s*\\(\\s*(.+)\\s*\\)\\s*;?\\s*$` with `re.DOTALL` uses a greedy `.+` that can over-match when the JSON body itself contains balanced parentheses. For example, a response like `callback({\"data\": {\"nested\": \"(val)\"}});` would capture `{\"data\": {\"nested\": \"(val)\"}});` including the trailing `);`, causing `json.loads` to fail. A more robust approach is to either: (1) find the matching closing parenthesis by counting depth, or (2) use a simpler heuristic — strip known prefixes like `callback(` and suffixes like `);` with `str.removeprefix`/`str.removesuffix` after confirming the format. This is a real risk because QQ's API can return nested JSON with parentheses in string values.",
      "existing_code": "    m = re.match(r\"^\\s*\\w+\\s*\\(\\s*(.+)\\s*\\)\\s*;?\\s*$\", text, re.DOTALL)\n    if m:\n        text = m.group(1).strip()",
      "scan": "ocr-scan-batch4.json"
    },
    {
      "path": "backend/app/services/oauth_login.py",
      "start_line": 578,
      "end_line": 580,
      "category": "bug",
      "severity": "medium",
      "content": "WeChat provider ID generation prioritizes unionid over openid, but if unionid is unavailable in subsequent logins (e.g., due to scope changes), the same user will get a different provider_id (wechat_<openid> vs wechat_<unionid>). This can cause duplicate account creation. The provider ID should be deterministic for the same user regardless of which identifier is available.",
      "existing_code": "    # 优先使用 unionid（多应用统一标识），其次 openid\n    pid = unionid or openid\n    return f\"wechat_{pid}\", nickname",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/payment_pkg/payment_service_impl.py",
      "start_line": 134,
      "end_line": 137,
      "category": "other",
      "severity": "medium",
      "content": "Amount parsing edge case in `_verify_notify_amount_guard`: when `paid_raw` is a dict (e.g. nested amount object), it falls back to `order.amount`. However, `int(round(float(paid_raw) * 100))` can produce incorrect results for string values containing commas, whitespace, or non-numeric characters. Add validation or try/except around the float conversion, and prefer explicit numeric fields from the payload over implicit coercion.",
      "existing_code": "        try:\n            paid = int(round(float(paid_raw) * 100)) if isinstance(paid_raw, str) and \".\" in str(paid_raw) else int(paid_raw)\n        except (TypeError, ValueError):\n            paid = int(order.amount or 0)",
      "scan": "ocr-scan-batch5-payment.json"
    },
    {
      "path": "backend/app/services/payment_pkg/payment_service_impl.py",
      "start_line": 313,
      "end_line": 319,
      "category": "performance",
      "severity": "medium",
      "content": "Inconsistent error handling across native payment flows: each factory (`create_native_payment`, `create_egress_addon_native`, `create_token_pack_native`) calls `self.db.rollback()` manually in production error branches. If the caller's outer transaction also expects to commit, the manual rollback can leave the session in an inconsistent state. Consider either wrapping all DB mutations in a single transaction scope or documenting that callers must not hold an outer transaction while invoking these methods. Also, the non-production mock fallback path calls `self.db.commit()` after potentially having already flushed subscription changes — if the channel call fails later, partial DB state can remain.",
      "existing_code": "        except Exception as exc:\n            if settings.ENVIRONMENT == \"production\":\n                self.db.rollback()\n                raise RuntimeError(\n                    f\"{pay_channel} 支付下单失败（生产环境不降级 mock）：{exc}\"\n                ) from exc\n            logger.warning(\"%s 支付 API 调用失败，降级为 mock 模式: %s\", pay_channel, exc)",
      "scan": "ocr-scan-batch5-payment.json"
    },
    {
      "path": "backend/app/services/payment_pkg/payment_service_impl.py",
      "start_line": 448,
      "end_line": 453,
      "category": "maintainability",
      "severity": "medium",
      "content": "Missing `raise` after `self.db.commit()` in `_mark_paid_and_provision`'s exception handler can mask errors if a subsequent call implicitly depends on an exception being raised. More critically, if `self.db.rollback()` fails (e.g. due to a stale connection), the exception would be lost and the method could return `order` inconsistently. Add an explicit `raise` after rollback/revert logging to preserve error visibility.",
      "existing_code": "            self.db.rollback()\n            logger.exception(\n                \"[BUG-02 fix] 发放权益失败，订单回滚 pending 等待渠道重试: order=%s\",\n                order_no,\n            )\n            reverted = (",
      "scan": "ocr-scan-batch5-payment.json"
    },
    {
      "path": "backend/app/services/payment_service.py",
      "start_line": 0,
      "end_line": 0,
      "category": "bug",
      "severity": "medium",
      "content": "Potential inconsistency in subscription handling: `create_native_payment` creates or updates a subscription record before creating the payment order. If the payment order creation fails (e.g., database error), the subscription is already in an inconsistent state. The rollback in the except block should handle this, but the logic assumes atomicity that may not hold across all database backends.",
      "existing_code": "        # 3. 创建或更新订阅记录\n        subscription = self.db.query(TenantSubscription).filter(\n            TenantSubscription.tenant_id == tenant_id,\n            TenantSubscription.plan_id == plan_id,\n            TenantSubscription.status == \"active\",\n        ).first()\n        if not subscription:\n            subscription = TenantSubscription(...)\n            self.db.add(subscription)\n            self.db",
      "scan": "ocr-scan-batch5-payment.json"
    },
    {
      "path": "backend/app/services/payment_service.py",
      "start_line": 636,
      "end_line": 643,
      "category": "bug",
      "severity": "medium",
      "content": "Inconsistent error handling: The Alipay notify handler logs an amount mismatch but returns None (line ~508), silently dropping the notification. This prevents the payment gateway from retrying and may cause the order to remain pending indefinitely. Consider returning success to the gateway and handling the mismatch asynchronously, or at minimum logging at ERROR level and continuing to provision.",
      "existing_code": "                if paid_cents != int(order.amount):\n                    logger.warning(\n                        \"alipay amount mismatch order=%s expected=%s got=%s\",\n                        order_no,\n                        order.amount,\n                        paid_cents,\n                    )\n                    return None",
      "scan": "ocr-scan-batch5-payment.json"
    },
    {
      "path": "backend/app/api/v1/routes/acquisition.py",
      "start_line": 51,
      "end_line": 61,
      "category": "bug",
      "severity": "low",
      "content": "DUPLICATE IMPORT: `get_read_session` is imported twice (lines 51 and 61). Also `claim_inquiry` is imported twice (lines 46 and 60 as `_claim_fn`). This is redundant and confusing.",
      "existing_code": "from app.core.db_sessions import get_read_session\nfrom app.services.acquisition.quote_guard import leadtime_gate, quote_validity_view\nfrom app.services.acquisition.suppression_list import suppression_store\nfrom app.services.acquisition.payment_risk import payment_risk_gate\nfrom app.services.acquisition.knowledge_queue import knowledge_queue_store\nfrom app.services.acquisition.risk_rescan import ri",
      "scan": "ocr-scan-batch7-acq-route.json"
    },
    {
      "path": "backend/app/api/v1/routes/acquisition.py",
      "start_line": 499,
      "end_line": 505,
      "category": "performance",
      "severity": "low",
      "content": "INEFFICIENT LIST CONVERSION: Multiple places convert to list unnecessarily, e.g., `list(body.reasons or [])` when `body.reasons` is already a List[str] from Pydantic validation. This creates unnecessary allocations.",
      "existing_code": "exp = record_ops_loss(\n        _resolve_db(db),\n        tenant_id=card.tenant_id,\n        inquiry_id=inquiry_id,\n        reasons=list(body.reasons or []),\n        note=body.note or \"\",\n    )",
      "scan": "ocr-scan-batch7-acq-route.json"
    },
    {
      "path": "backend/app/api/v1/routes/acquisition.py",
      "start_line": 607,
      "end_line": 619,
      "category": "bug",
      "severity": "low",
      "content": "LOGIC ERROR: In the sample endpoint, `body.qty` and `body.fee_amount` are already added to kwargs in the loop (lines 608-615), then redundantly checked again (lines 616-619). This is not harmful but indicates unclear logic about when these fields should be included.",
      "existing_code": "kwargs: dict[str, Any] = {}\n    for field_name in (\n        \"status\", \"product\", \"spec\", \"qty\", \"unit\",\n        \"fee_amount\", \"fee_currency\", \"fee_status\",\n        \"courier\", \"tracking_no\", \"shipped_at\", \"note\",\n    ):\n        val = getattr(body, field_name, None)\n        if val is not None and val != \"\":\n            kwargs[field_name] = val\n    if body.qty:\n        kwargs[\"qty\"] = body.qty\n    if",
      "scan": "ocr-scan-batch7-acq-route.json"
    },
    {
      "path": "backend/app/api/v1/routes/acquisition.py",
      "start_line": 1274,
      "end_line": 1277,
      "category": "bug",
      "severity": "low",
      "content": "UNUSED VARIABLE: `last` is assigned in the loop but the loop result overwrites it each iteration. The variable serves no purpose since only the final value matters.",
      "existing_code": "last = None\n    for _ in range(max(1, min(20, int(body.times)))):\n        last = acq_rate_limiter.check(tenant_id=body.tenant_id, action=body.action)\n    return last or {\"allowed\": True, \"code\": \"ok\"}",
      "scan": "ocr-scan-batch7-acq-route.json"
    },
    {
      "path": "backend/app/api/v1/routes/acquisition.py",
      "start_line": 1530,
      "end_line": 1534,
      "category": "bug",
      "severity": "low",
      "content": "`card.sample` may be `None` after `ops_card_store.materialize` if the card was just created and no sample object was attached. Accessing `card.sample.status` in that case will raise `AttributeError: 'NoneType' object has no attribute 'status'`. Add a guard: `card.sample.status if card.sample else 'none'`.",
      "existing_code": "    if intent_analysis.get(\"intent\") == \"request_sample\":\n        card = ops_card_store.update_sample(\n            body.inquiry_id,\n            status=\"requested\" if (card.sample.status in (\"none\", \"\", None)) else card.sample.status,\n        )",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/api/v1/routes/auth.py",
      "start_line": 66,
      "end_line": 66,
      "category": "other",
      "severity": "low",
      "content": "Variable `_email_code_ttl_store` is declared but never read or written anywhere in this file. It appears to be dead code left over from an earlier implementation attempt.",
      "existing_code": "_email_code_ttl_store: dict[str, float] = {}",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/payment.py",
      "start_line": 127,
      "end_line": 127,
      "category": "documentation",
      "severity": "low",
      "content": "The module imports `APIResponse` from `app.core.response` but uses it inconsistently — some endpoints declare `response_model=APIResponse` while others omit it entirely. This makes OpenAPI schema incomplete and client generators may miss documented response shapes. Standardize by adding the decorator consistently, or remove it globally and rely on pydantic models instead.",
      "existing_code": "@router.get('/channels/status', operation_id='payment_get_channels_status', response_model=APIResponse, summary='获取支付渠道配置与在线状态', description='查询微信支付与支付宝渠道配置就绪状态、真实商户连接状态及Mock模式状态。需要有效JWT管理员权限。')",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/payment.py",
      "start_line": 315,
      "end_line": 318,
      "category": "maintainability",
      "severity": "low",
      "content": "The generic `/notify` endpoint logs a warning advising migration to channel-specific routes, but still processes the request through the same `_emit_payment_success_event` path. If a client accidentally or intentionally continues using this endpoint in production, audit trails will be mixed and signature enforcement may differ per-channel. Consider deprecating the route with a 410 Gone after a transition period rather than continuing to serve it alongside the channel-specific handlers.",
      "existing_code": "    log.warning(\n        \"[Payment] 遗留通用 /notify 端点被调用，建议迁移至 /notify/wechat 与 /notify/alipay。\"\n        \"该端点仅在配置 PAYMENT_WEBHOOK_SECRET 且严验签通过时放行；严验模式下无可信签名直接拒绝（B-P0 F4）。\"\n    )",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/payment.py",
      "start_line": 485,
      "end_line": 487,
      "category": "maintainability",
      "severity": "low",
      "content": "`create_stripe_session` imports `PaymentService` from the internal package path `app.services.payment_pkg.payment_service_impl` on line 341, while the module-level import on line 13 brings in `app.services.payment_service`. These are different symbols and may mask a naming/aliasing issue. If the internal import is intentional, add a comment explaining why; otherwise use the module-level import consistently. This dual-import pattern increases maintenance risk when one path is updated and the other is not.",
      "existing_code": "        from app.services.payment_pkg.payment_service_impl import PaymentService\n        svc = PaymentService(db)\n        created = svc.create_payment_order(",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/payment.py",
      "start_line": 550,
      "end_line": 552,
      "category": "maintainability",
      "severity": "low",
      "content": "Same issue in the Stripe webhook handler: `PaymentService` is imported again from the internal path on line 408, diverging from the module-level import on line 13. This inconsistency makes refactoring/error-tracing harder. Align with the top-level import unless there is a documented reason.",
      "existing_code": "        from app.services.payment_pkg.payment_service_impl import PaymentService\n        svc = PaymentService(db)\n        order = svc._mark_paid_and_provision(order_no, sync_data={",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/tenants.py",
      "start_line": 708,
      "end_line": 712,
      "category": "performance",
      "severity": "low",
      "content": "The `generate_tenant_site_content` route (line 388) and `onboarding_autopilot_run` route (line 725) perform CPU-intensive or long-running AI service calls inside an async handler. Even if those functions are properly async, they may still perform significant work that blocks the event loop. Consider offloading to a task queue (Celery, ARQ, or asyncio.to_thread) for operations that are not purely I/O-bound, to avoid starving other requests.",
      "existing_code": "async def onboarding_autopilot_run(\n    body: OnboardingAutopilotRequest,\n    db: Session = Depends(get_db),\n    current_user: User = Depends(get_current_user),\n):",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/api/v1/routes/tenants.py",
      "start_line": 1323,
      "end_line": 1325,
      "category": "security",
      "severity": "low",
      "content": "In `_resolve_register_client_ip` (lines 1322-1325), the `x-forwarded-for` header is trusting the first entry without validation. While this is common practice, in environments where the proxy layer does not strip or validate XFF headers, a client can send `X-Forwarded-For: <attacker-ip>, <real-ip>` and the rate limiter will track the attacker's IP instead of the real client IP, allowing them to bypass the rate limit by rotating the first XFF entry. This is a minor concern since it requires proxy misconfiguration but worth noting.",
      "existing_code": "fwd = request.headers.get(\"x-forwarded-for\")\n    if fwd:\n        client_ip = fwd.split(\",\")[0].strip()",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/core/admin_auth.py",
      "start_line": 35,
      "end_line": 35,
      "category": "maintainability",
      "severity": "low",
      "content": "Correctness: The admin_session_revoked key format uses f\"admin_session_revoked:{user.id}:{jti}\" but there's no corresponding cleanup mechanism shown. If sessions are revoked frequently, this Redis key can grow unbounded. Additionally, the code assumes user.id is a string-compatible value - if it's an integer or UUID object, the f-string conversion should be explicit. Consider whether UUID objects need special handling.",
      "existing_code": "            blacklisted = redis_client.get(f\"admin_session_revoked:{user.id}:{jti}\")",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/core/admin_auth.py",
      "start_line": 120,
      "end_line": 132,
      "category": "style",
      "severity": "low",
      "content": "Style: Multiple functions have verbose docstrings with generic ':param request: 参数 request' style documentation that adds no value. The checker functions in particular have boilerplate docstrings that should be removed or made meaningful. This adds noise without improving readability.",
      "existing_code": "    async def checker(\n        request: Request,\n        user: User = Depends(get_current_user),\n        db: Session = Depends(get_db),\n    ) -> User:\n        \"\"\"checker。\n\n        参数说明：\n        :param request: 参数 request\n        :param user: 参数 user\n        :param db: 参数 db\n        :return: 返回处理结果。\n        \"\"\"",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/core/database.py",
      "start_line": 230,
      "end_line": 233,
      "category": "bug",
      "severity": "low",
      "content": "get_pool_status() accesses engine.pool without checking if engine is initialized. During startup or after rebind failures, engine may not have a pool attribute, causing AttributeError. Should handle the case where pool is unavailable more gracefully.",
      "existing_code": "def get_pool_status() -> dict:\n    \"\"\"返回连接池状态（用于健康检查）\"\"\"\n    try:\n        pool = engine.pool",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/login_bruteforce.py",
      "start_line": 0,
      "end_line": 0,
      "category": "bug",
      "severity": "low",
      "content": "`_identity_key` strips and lowercases `username_or_email` but does not validate that the result is non-empty. If an attacker sends an empty or whitespace-only username, the key becomes `hashlib.sha256(f\"{ip}|\").hexdigest()`, which is a valid but meaningless key. This doesn't cause a crash but wastes store entries. More importantly, `_memory_check` and `_memory_record_failure` both call `_identity_key` but the check happens before any input validation, so an attacker can probe the store with arbitrary empty usernames without triggering the rate limit on real usernames.",
      "existing_code": "def _identity_key(ip: str, username_or_email: str) -> str:\n    raw = f\"{ip}|{username_or_email.strip().lower()}\"\n    return hashlib.sha256(raw.encode(\"utf-8\")).hexdigest()",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/__init__.py",
      "start_line": 37,
      "end_line": 38,
      "category": "maintainability",
      "severity": "low",
      "content": "The `_now_iso()` function uses `datetime.now(timezone.utc)` but should use `datetime.utcnow()` or `datetime.now(timezone.utc)` consistently. The current implementation is correct, but `datetime.now(timezone.utc)` is preferred over the deprecated `datetime.utcnow()`. No issue here — this is fine. However, the function is called repeatedly and could be simplified to a module-level constant if the ISO string doesn't need to change within a request.",
      "existing_code": "def _now_iso() -> str:\n    return datetime.now(timezone.utc).isoformat()",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/__init__.py",
      "start_line": 140,
      "end_line": 141,
      "category": "maintainability",
      "severity": "low",
      "content": "The duplicate `if key[1]:` check on line 140 is redundant — it's already inside the `if key[1]:` block opened on line 107. This dead branch will never execute (the inner assignment on line 141 is unreachable because if we reach it, we've already returned at line 139). Remove the outer wrapping and the inner duplicate guard.",
      "existing_code": "if key[1]:\n                self._by_email[key] = buyer.buyer_id",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/__init__.py",
      "start_line": 374,
      "end_line": 378,
      "category": "maintainability",
      "severity": "low",
      "content": "The `score_display` method mutates `self.buyer_grade_reason` when computing on-the-fly — this is a side effect that persists back to the in-memory card object. This may be intended, but it means calling `score_display()` twice with no grade set will only set the reason once (the first call), then on subsequent calls `grade` is still empty (since buyer_score might change) causing repeated grade computation. The method should either explicitly document this mutation or avoid it by using a local variable.",
      "existing_code": "        grade = self.buyer_grade or \"\"\n        if not grade and self.buyer_score:\n            grade, reason = score_grade(self.buyer_score)\n            if not self.buyer_grade_reason:\n                self.buyer_grade_reason = reason",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/baseline_bench.py",
      "start_line": 3,
      "end_line": 3,
      "category": "documentation",
      "severity": "low",
      "content": "The docstring mentions '并发诚实率' (concurrency honesty rate) but both benchmark functions run sequentially. This is misleading documentation. Either implement actual concurrent testing or update the docstring to reflect sequential execution.",
      "existing_code": "\"\"\"E-6 混沌/压测基线：dispatch 幂等与并发诚实率（不依赖外部负载工具）。",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/baseline_bench.py",
      "start_line": 73,
      "end_line": 73,
      "category": "style",
      "severity": "low",
      "content": "Line 58 uses `is False` for identity comparison. While this works for bool singletons, it is semantically incorrect for checking return values. Use `== False` or better yet `not r2.get('ok')` to check truthiness.",
      "existing_code": "    blocked = (r2.get(\"ok\") is False) or (r2.get(\"code\") in (\"collision\", \"already_owner\"))",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/billing_explain.py",
      "start_line": 32,
      "end_line": 39,
      "category": "bug",
      "severity": "low",
      "content": "The `_reason_plain` function uses substring matching with `in` operator on lowercase strings (lines 28-34). The check `if \"dispatch\" in r` will match unintended reasons containing 'dispatch' (e.g., 'redispatch', 'unexpected_dispatch_action'). Consider using more specific prefixes or exact pattern matching to avoid false matches.",
      "existing_code": "    if \"ops_dispatch\" in r or \"dispatch\" in r:\n        return \"获客智能派发\"\n    if \"ops_win\" in r:\n        return \"成交经验入环\"\n    if \"ops_loss\" in r:\n        return \"流失经验入环\"\n    if \"reply_ingest\" in r:\n        return \"询盘进线建档\"",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/dispatch_service.py",
      "start_line": 49,
      "end_line": 52,
      "category": "maintainability",
      "severity": "low",
      "content": "Late imports on lines 46-47 and 54-57 reduce code readability and make it harder to track dependencies. While this is a common pattern in FastAPI apps to avoid circular imports, consider whether these could be moved to module level with proper ordering, or at minimum document why they're deferred.",
      "existing_code": "    from app.schemas.hermes_orchestration import IntentEvent\n    from app.services.hermes import planner_service as ps\n\n    event = IntentEvent(",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/dispatch_service.py",
      "start_line": 60,
      "end_line": 73,
      "category": "performance",
      "severity": "low",
      "content": "Line 75: The list comprehension builds the full `nodes` list eagerly even when auto_dispatch=False and the function returns early on line 78. For large graphs, this is wasted work. Consider deferring node construction or checking auto_dispatch first.",
      "existing_code": "    nodes = [\n        {\n            \"id\": n.id,\n            \"executor\": n.executor,\n            \"capability\": n.capability,\n            \"depends_on\": list(n.depends_on or []),\n        }\n        for n in graph.nodes\n    ]\n    result: dict[str, Any] = {\n        \"plan_id\": graph.plan_id,\n        \"graph_source\": source,\n        \"node_count\": len(graph.nodes),\n        \"nodes\": nodes,",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/dispatch_service.py",
      "start_line": 86,
      "end_line": 89,
      "category": "maintainability",
      "severity": "low",
      "content": "The session check on line 88-93 returns early with dispatch_error='db_unavailable', but this is inconsistent with the behavior when auto_dispatch=False (line 75-77). When auto_dispatch=True and session is None, the function still marks dispatched=False but provides no useful error context beyond 'db_unavailable'. Consider whether this should raise an exception or return a more descriptive error.",
      "existing_code": "    if session is None:\n        result[\"dispatch_error\"] = \"db_unavailable\"\n        result[\"persistence_note\"] = \"无数据库会话，跳过任务落库/派发（诚实失败）\"\n        return result",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/dispatch_service.py",
      "start_line": 96,
      "end_line": 99,
      "category": "maintainability",
      "severity": "low",
      "content": "Line 92: `getattr(node_tasks[0], \"parent_task_id\", \"\")` returns an empty string if the attribute doesn't exist, then line 93 checks `if plan_task_id:` which will be falsy for empty string. However, the error handling path on lines 100-103 sets dispatch_error='no_plan_task_id' but doesn't mark the plan as failed - it just returns with dispatched=False. This might be acceptable but worth confirming the intended behavior.",
      "existing_code": "        plan_task_id = str(getattr(node_tasks[0], \"parent_task_id\", \"\")) if node_tasks else \"\"\n        result[\"task_ids\"] = [str(getattr(t, \"id\", \"\")) for t in node_tasks]\n        result[\"plan_task_id\"] = plan_task_id\n        if plan_task_id:",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 30,
      "end_line": 31,
      "category": "bug",
      "severity": "low",
      "content": "Line 30-31: `if db is None` returns early with `{recorded: False, reason: 'db_unavailable'}`. However, `db` could be a falsy value other than `None` (e.g., an empty string `\"\"`, or a SQLAlchemy session that failed to initialize). A falsy-but-non-None `db` would pass this check and then likely raise an exception inside the `try` blocks, which gets caught and logged. This is not incorrect per se, but the check is too narrow — it only guards against `None`, not other invalid states of `db`.",
      "existing_code": "    if db is None:\n        return {\"recorded\": False, \"reason\": \"db_unavailable\", \"task_type\": task_type}",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 35,
      "end_line": 36,
      "category": "performance",
      "severity": "low",
      "content": "Performance - Lazy imports on every call: `EvolutionEngine` is imported inside the function on line 31, and `record_terminal_state` on line 54. These imports happen on every function call, adding overhead. While Python caches imports, repeated import statements still have lookup overhead. Consider moving these to module level if this is a hot path.",
      "existing_code": "        from app.services.evolution.engine import EvolutionEngine\n        eng = EvolutionEngine(db)",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 47,
      "end_line": 49,
      "category": "maintainability",
      "severity": "low",
      "content": "Lines 46-47 and 70: Both `error_message` and `output_summary` truncate `detail` to 500 characters (`detail[:500]`). While this prevents excessively long strings from being stored, it silently discards relevant failure context that the evolution engine and terminal hook need to learn from. If a real error message exceeds 500 chars, the evolution feedback loop will receive incomplete data. Consider logging the full detail when truncation occurs, or using a larger limit with a hard ceiling (e.g., 2000 chars).",
      "existing_code": "            error_message=None if success else detail[:500],\n            input_summary=inquiry_id,\n            output_summary=detail[:500],",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 47,
      "end_line": 49,
      "category": "maintainability",
      "severity": "low",
      "content": "String truncation masking errors: Error messages are truncated to 500 characters via `detail[:500]` on lines 42 and 60. For multi-line stack traces or detailed error messages, this could lose critical debugging information. If `detail` contains structured error information, important context may be silently discarded.",
      "existing_code": "            error_message=None if success else detail[:500],\n            input_summary=inquiry_id,\n            output_summary=detail[:500],",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 55,
      "end_line": 56,
      "category": "maintainability",
      "severity": "low",
      "content": "Bare except with noqa suppression: The `except Exception` blocks here (lines 48-50 and 58-62) are intentionally broad with noqa: BLE001, but this is worth noting. The first catches any EvolutionEngine failure and falls back to terminal_hook. The second catches any terminal_hook failure and returns a failure record. While the fallback strategy is reasonable, the original exception details are lost in the first except (only logged at DEBUG level), making production debugging difficult if EvolutionEngine consistently fails.",
      "existing_code": "    except Exception as exc:  # noqa: BLE001\n        logger.debug(\"EvolutionEngine 写入失败，尝试 terminal_hook: %s\", exc)",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 80,
      "end_line": 80,
      "category": "documentation",
      "severity": "low",
      "content": "The `reasons: list[str]` parameter in `record_ops_loss` has no default value, making it a required positional-or-keyword argument. Callers must always provide it. This is not a bug per se, but it is worth noting: if a caller passes an empty list `[]`, `detail` becomes `\"\"` (since `\"; \".join([])` returns `\"\"`) and then `detail or \"loss\"` correctly falls back to `\"loss\"`. However, the `\" | {note}\"` suffix is appended only when `note` is truthy — if both `reasons=[]` and `note=\"\"`, detail is `\"\"` and falls back to `\"loss\"`. This edge case is handled correctly. No issue here.",
      "existing_code": "def record_ops_loss(db: Any, *, tenant_id: str, inquiry_id: str, reasons: list[str], note: str = \"\") -> dict[str, Any]:",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 80,
      "end_line": 80,
      "category": "maintainability",
      "severity": "low",
      "content": "Line 80: `reasons: list[str]` is a required parameter with no default. If this signature is later changed to have a default (e.g., `reasons: list[str] = None`), it would become a mutable default argument bug. As-is, it is safe but rigid — callers must always provide a list. Consider whether an optional `reasons=None` with internal `reasons or []` handling (as done on line 81) would be more caller-friendly without introducing the mutable-default risk.",
      "existing_code": "def record_ops_loss(db: Any, *, tenant_id: str, inquiry_id: str, reasons: list[str], note: str = \"\") -> dict[str, Any]:",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 80,
      "end_line": 81,
      "category": "maintainability",
      "severity": "low",
      "content": "Mutable default argument: The parameter `reasons: list[str]` in `record_ops_loss` (line 72) has no default value, requiring callers to always provide a list. If callers pass a mutable list object, modifications to it could have unintended side effects. Consider whether a copy is needed if the function mutates the list internally.",
      "existing_code": "def record_ops_loss(db: Any, *, tenant_id: str, inquiry_id: str, reasons: list[str], note: str = \"\") -> dict[str, Any]:\n    detail = \"; \".join(reasons or []) + (f\" | {note}\" if note else \"\")",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 123,
      "end_line": 123,
      "category": "maintainability",
      "severity": "low",
      "content": "The `db` parameter is never used in the function body. Callers already pass `None` (see `acquisition/__init__.py` line 612: `win_loss_summary(None, ...)`). Consider removing it, or if it's reserved for a future feature (e.g., falling back to DB queries when `cards` is not supplied), add a brief comment indicating the intent to avoid confusion.",
      "existing_code": "def win_loss_summary(db: Any, *, tenant_id: str = \"\", cards: list[Any] | None = None) -> dict[str, Any]:",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 125,
      "end_line": 125,
      "category": "other",
      "severity": "low",
      "content": "Potential infinite recursion in win_loss_summary: The function imports `REASON_HINTS` from `app.services.acquisition.loss_report` on line 98. If that module itself imports from this module (circular import), it could cause issues. This is a latent risk that depends on the import graph.",
      "existing_code": "    from app.services.acquisition.loss_report import REASON_HINTS",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/experience_feed.py",
      "start_line": 138,
      "end_line": 139,
      "category": "documentation",
      "severity": "low",
      "content": "Lines 123-124: `max(loss_reasons.items(), ...)` and `max(win_reasons.items(), ...)` will raise `ValueError: max() arg is an empty sequence` when there are no lost/won items respectively. Although the code has a guard (`if loss_reasons`), the `win_loss_summary` function at lines 123-124 directly calls `max()` without first checking whether the dict is empty — wait, actually it does check. Let me re-read... Actually the guard is present. However, the `top_loss` line 124 and `top_win` line 125 use conditional expressions correctly. The actual issue is: there is no bug here — the empty-case guards are correct. This finding is a false alarm; removing.",
      "existing_code": "    top_loss = max(loss_reasons.items(), key=lambda x: x[1])[0] if loss_reasons else \"\"\n    top_win = max(win_reasons.items(), key=lambda x: x[1])[0] if win_reasons else \"\"",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/fulfillment_nodes.py",
      "start_line": 97,
      "end_line": 97,
      "category": "maintainability",
      "severity": "low",
      "content": "The outer reminder condition tests `pi_no is not None` but since pi_no is always a str (defaulting to `\"\"`), this is always true. The inner elif then checks `not pi_no` (truthy for empty string). The structure misleads: the guard does not prevent entry, and the branch reads as though it guards against None when it actually tests emptiness. Simplify to only `key in (\"pi\", \"deposit\")`.",
      "existing_code": "        if status == \"overdue\" or (status in (\"pending\", \"active\") and key in (\"pi\", \"deposit\") and pi_no is not None):",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/growth_ops.py",
      "start_line": 56,
      "end_line": 56,
      "category": "maintainability",
      "severity": "low",
      "content": "upsert_content uses a silent fallback pattern: tenant_id parameter is optional with empty string default, but the code does `tenant_id or item.tenant_id` which means updates without tenant_id will preserve the old value. This makes tenant isolation unreliable - callers may not realize they're updating a record with a different tenant_id than expected.",
      "existing_code": "item.tenant_id = tenant_id or item.tenant_id",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/growth_ops.py",
      "start_line": 91,
      "end_line": 92,
      "category": "performance",
      "severity": "low",
      "content": "list_contents iterates all items and builds a new list, then sorts by inquiry_count. For large catalogs this is O(n log n) and creates unnecessary dict copies. The sort is also unstable and could reorder items with equal inquiry counts unpredictably.",
      "existing_code": "out.sort(key=lambda x: x[\"inquiry_count\"], reverse=True)\n    return out",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/intent_classifier.py",
      "start_line": 26,
      "end_line": 26,
      "category": "maintainability",
      "severity": "low",
      "content": "The 't/t' keyword in payment_discuss has no trailing space, while 'tt ' and 'lc ' use trailing-space padding to avoid substring matches. Since the input is lowercased, 't/t' will match inside strings like 'what's the t/t amount' — which is likely intended. However, this inconsistency in pattern design suggests a risk: similar keywords like 'saso' (no padding) vs 'ce ' (padded) may produce unintended partial matches. Consider unifying the matching strategy across all keywords.",
      "existing_code": "        [\"付款\", \"定金\", \"t/t\", \"tt \", \"信用证\", \"l/c\", \"lc \", \"payment\", \"deposit\", \"账期\", \"全款\"],",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/intent_classifier.py",
      "start_line": 73,
      "end_line": 73,
      "category": "maintainability",
      "severity": "low",
      "content": "The `country` parameter in `classify_reply(text: str, country: str = \"\")` is never referenced in the function body. This suggests either incomplete implementation or that the classifier was designed to handle geo-specific intents but lacks that logic. At minimum, document why it's unused or remove it to avoid confusion.",
      "existing_code": "def classify_reply(text: str, country: str = \"\") -> dict[str, Any]:",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/intent_classifier.py",
      "start_line": 111,
      "end_line": 111,
      "category": "maintainability",
      "severity": "low",
      "content": "The confidence score uses a linear formula: min(0.95, 0.45 + 0.15 * best_hits). There is no documentation of downstream thresholds (e.g., is 0.2 vs 0.6 treated differently?). If this value drives automated decisions, the formula and thresholds should be documented or extracted as named constants for clarity and maintainability.",
      "existing_code": "    conf = min(0.95, 0.45 + 0.15 * best_hits)",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/knowledge_queue.py",
      "start_line": 33,
      "end_line": 37,
      "category": "maintainability",
      "severity": "low",
      "content": "The pre-scan correctly identified a critical shared mutable seed issue. `_SEED` is a module-level list of dicts, and the `KnowledgeQueueStore.__init__` unpacks them directly with `KnowledgeItem(**s)`. While this creates new `KnowledgeItem` instances, subsequent mutations to `done`, `done_at`, or `done_by` do not affect the seed dicts themselves (dataclass fields are distinct), so this specific concern is mitigated for new instances. However, if the seed dicts were mutated elsewhere or if the dataclass was configured differently, this could be a risk. As-is, it's acceptable.",
      "existing_code": "_SEED: list[dict[str, Any]] = [\n    {\n        \"id\": \"know-cert-official\",\n        \"title\": \"认证官方清单怎么查\",\n        \"category\": \"认证合规\",",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/loss_report.py",
      "start_line": 35,
      "end_line": 38,
      "category": "maintainability",
      "severity": "low",
      "content": "A bare `continue` inside a loop without an outer try/except leaves no traceback information. If getattr(card, 'loss_reasons', None) raises (e.g. on a poorly behaved custom object), the exception propagates but it's unclear whether the caller can distinguish normal missing attributes from unexpected broken ones. Recommend wrapping card iteration in `try/except Exception` and logging/attaching the error to the item rather than letting unknown failures surface inconsistently.",
      "existing_code": "    for card in cards:\n        reasons = list(getattr(card, \"loss_reasons\", None) or [])\n        if not reasons and getattr(card, \"stage\", \"\") != \"lost\":\n            continue",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/loss_report.py",
      "start_line": 35,
      "end_line": 40,
      "category": "maintainability",
      "severity": "low",
      "content": "The function silently accepts any iterable of objects without validating their shape. If `cards` contains items missing expected fields, AttributeError or NoneType errors can appear downstream when consumers assume `inquiry_id`, `buyer_display`, etc. are strings. Consider adding a lightweight validation layer (or explicit type guard) early in the function so malformed inputs raise with a clear message rather than producing confusing downstream failures.",
      "existing_code": "    for card in cards:\n        reasons = list(getattr(card, \"loss_reasons\", None) or [])\n        if not reasons and getattr(card, \"stage\", \"\") != \"lost\":\n            continue\n        total_lost += 1\n        norm = [_normalize_reason(r) for r in (reasons or [\"其他\"])]",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/nps_rescue.py",
      "start_line": 51,
      "end_line": 53,
      "category": "other",
      "severity": "low",
      "content": "`int(onboarding_view.get(\"done_count\") or 0)` and `int(onboarding_view.get(\"total\") or 5)` will raise `ValueError` / `TypeError` if the view holds a non-numeric string (e.g. `\"\"` or `\"abc\"`). Wrap these in a helper that defaults on parse failure, mirroring the same pattern already used for NPS.",
      "existing_code": "    if onboarding_view:\n        onb_done = int(onboarding_view.get(\"done_count\") or 0)\n        onb_total = int(onboarding_view.get(\"total\") or 5)",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/services/acquisition/nps_rescue.py",
      "start_line": 65,
      "end_line": 66,
      "category": "maintainability",
      "severity": "low",
      "content": "The billing_view check only appends a rescue action when balance is None, but doesn't validate that the billing integration is actually configured. If billing_view exists but has other required fields missing, this check alone may miss critical integration gaps. Consider checking for more comprehensive billing configuration validity.",
      "existing_code": "    if billing_view and (billing_view.get(\"balance\") is None):\n        rescue_actions.append(\"计费账本未接入时余额不可见，可先完成联调配置\")",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/onboarding.py",
      "start_line": 11,
      "end_line": 11,
      "category": "maintainability",
      "severity": "low",
      "content": "**Module-level mutable list of mutable dicts**: `STEPS` is a module-level `list[dict]`. Although the current code only reads from it, any code that imports `STEPS` (e.g., `from ...onboarding import STEPS`) can mutate the shared list or its inner dicts in place, affecting every other importer. This is a well-known shared-state hazard. Consider freezing the structure (e.g., use `tuple` of namedtuples/frozensets, or at minimum document the intent and avoid reassigning via `.append`/`.sort` at call sites).",
      "existing_code": "STEPS: list[dict[str, Any]] = [",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/services/acquisition/onboarding.py",
      "start_line": 11,
      "end_line": 11,
      "category": "maintainability",
      "severity": "low",
      "content": "Module-level mutable `STEPS` list is never mutated in this file, but it's exported as part of the module API. Any caller (including tests or other services) that mutates it (e.g., appending, modifying `done` flags) will affect all subsequent calls. If immutability is intended, convert to a tuple: `STEPS: tuple[dict[str, Any], ...]`. Currently no evidence of mutation, so low severity.",
      "existing_code": "STEPS: list[dict[str, Any]] = [",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/onboarding.py",
      "start_line": 66,
      "end_line": 71,
      "category": "maintainability",
      "severity": "low",
      "content": "Mutable default argument `ops_store: Any = None` is fine, but `tenant_id: str = \"demo\"` silently falls back to a hardcoded demo tenant when called without arguments. In production this could lead to filtering cards by the wrong tenant or returning unexpected results without any warning. Consider making this a required parameter or raising an error for the demo value.",
      "existing_code": "def build_onboarding(\n    tenant_id: str = \"demo\",\n    *,\n    ops_store: Any = None,\n    has_dispatch: bool = False,\n) -> dict[str, Any]:",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/onboarding.py",
      "start_line": 81,
      "end_line": 81,
      "category": "maintainability",
      "severity": "low",
      "content": "The condition `if not tenant_id` on line 59 combined with `getattr(c, \"tenant_id\", \"\") in (\"\", tenant_id)` means that when `tenant_id` is the empty string (or falsy), ALL cards are included regardless of their actual tenant_id. This could return a vastly oversized card set in the edge case where an empty string tenant_id is passed. Verify this is intentional and document the behavior.",
      "existing_code": "                if not tenant_id or getattr(c, \"tenant_id\", \"\") in (\"\", tenant_id)",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/onboarding.py",
      "start_line": 85,
      "end_line": 85,
      "category": "bug",
      "severity": "low",
      "content": "The truthiness check `getattr(c, 'last_touch_at', '')` on line 63 treats both `None` and empty string `''` as falsy (correct), but also treats any timestamp-like object that evaluates to False (e.g. `datetime(1970, 1, 1)` with epoch-zero semantics, or integer `0`) as untouched. If the data model uses epoch-zero sentinels or integer timestamps (0 = no touch), this is a correctness issue. Verify the actual type and sentinel used for `last_touch_at` in the ops card model before relying on truthiness.",
      "existing_code": "            has_touch = any(getattr(c, \"last_touch_at\", \"\") for c in cards)",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/ops_card_pg.py",
      "start_line": 68,
      "end_line": 71,
      "category": "performance",
      "severity": "low",
      "content": "Every call to save_ops_card re-executes the CREATE TABLE IF NOT EXISTS statement within the same transaction as the INSERT. Since patch_store_persistence already invokes ensure_table at startup, this is redundant on the normal path. It adds an extra round-trip to the database on every write and can mask the real error: if the DDL fails (e.g., permission denied on a read-only replica), the function falls into the except block and reports a misleading 'save_ops_card failed' message. Remove the DDL call here and rely on ensure_table for schema creation.",
      "existing_code": "    try:\n        from sqlalchemy import text\n        session.execute(text(DDL))\n        payload = card.to_dict() if hasattr(card, \"to_dict\") else {}",
      "scan": "ocr-scan-batch3.json"
    },
    {
      "path": "backend/app/services/acquisition/ops_card_pg.py",
      "start_line": 174,
      "end_line": 174,
      "category": "other",
      "severity": "low",
      "content": "In restore_ops_card, the payment assignment uses a conditional expression that might not work correctly if payment dict is empty.",
      "existing_code": "        card.payment = OpsCardPayment(**{k: payment.get(k, getattr(OpsCardPayment(), k)) for k in payment} if payment else OpsCardPayment())",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/payment_risk.py",
      "start_line": 33,
      "end_line": 35,
      "category": "maintainability",
      "severity": "low",
      "content": "Line 34: `tenant_id` is declared as a parameter but never used anywhere in the function body. This is dead code that should be removed or documented if it's part of an interface contract.",
      "existing_code": "    inquiry_id: str = \"\",\n    tenant_id: str = \"\",\n) -> dict[str, Any]:",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/payment_risk.py",
      "start_line": 88,
      "end_line": 98,
      "category": "maintainability",
      "severity": "low",
      "content": "Lines 88-97: The deposit_ratio reduction logic can create inconsistent behavior. When `r >= 0.3`, score decreases by 1, but the threshold checks are ordered: `r <= 0` (add 2), `r < 0.2` (add 1), `r >= 0.3` (subtract 1). The range 0.2 <= r < 0.3 receives no bonus/malus, which seems intentional but should be documented.",
      "existing_code": "            if r <= 0:\n                score_risk += 2\n                reasons.append(\"定金比例 0：高风险\")\n            elif r < 0.2:\n                score_risk += 1\n                reasons.append(\"定金比例偏低（<20%）\")\n            elif r >= 0.3:\n                score_risk -= 1\n                if score_risk < 0:\n                    score_risk = 0\n                reasons.append(\"定金比例较安全（≥30%）\")",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/payment_risk.py",
      "start_line": 108,
      "end_line": 109,
      "category": "maintainability",
      "severity": "low",
      "content": "Line 109: `auto_pi_allowed = False if auto_pi else True` is logically equivalent to `auto_pi_allowed = not auto_pi`, but the conditional expression is unnecessarily complex. Consider simplifying for readability.",
      "existing_code": "        level = \"medium\"\n        auto_pi_allowed = False if auto_pi else True",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/quote_guard.py",
      "start_line": 61,
      "end_line": 64,
      "category": "maintainability",
      "severity": "low",
      "content": "When `expired=True`, the FX branch appends `\" 注意汇率可能已变。\"` regardless of `fx_note`. If `fx_note` contains useful context (e.g., lock expiry details), it is silently dropped on the expiry path, reducing diagnostic value for the caller.",
      "existing_code": "    if fx_locked:\n        plain += \" 已锁汇率。\"\n    elif expired:\n        plain += \" 注意汇率可能已变。\"",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/sample_flow.py",
      "start_line": 77,
      "end_line": 77,
      "category": "style",
      "severity": "low",
      "content": "The fallback status='none' when getattr returns None silently masks potential data issues. If a sample exists but has no status attribute or None status, it's treated as 'none' without any logging or warning, making debugging harder.",
      "existing_code": "        status = getattr(sample, \"status\", None) or \"none\"",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/suppression_list.py",
      "start_line": 69,
      "end_line": 78,
      "category": "maintainability",
      "severity": "low",
      "content": "Missing input validation for `tenant_id`: Unlike `email` which has validation, `tenant_id` has no length limits or format checks. A tenant_id of None or empty string defaults to 'demo', but arbitrary strings including very long ones are accepted. This could lead to unexpected behavior in multi-tenant scenarios or consume excessive memory.",
      "existing_code": "    def remove(\n        self,\n        *,\n        email: str,\n        tenant_id: str = \"demo\",\n        confirm: bool = False,\n        note: str = \"\",\n    ) -> dict[str, Any]:\n        email_n = self._norm(email)\n        key = (tenant_id or \"demo\", email_n)",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/suppression_list.py",
      "start_line": 96,
      "end_line": 118,
      "category": "maintainability",
      "severity": "low",
      "content": "Inconsistent email validation in check_outreach(): The `check_outreach` method calls `self.is_suppressed(email_n, tenant_id)` with `email_n` (already normalized), but then reconstructs the key as `(tenant_id or \"demo\", email_n)`. However, if `email_n` is empty after normalization, the method returns early. The real issue is that `is_suppressed` doesn't validate the email - it trusts the caller already normalized it. This is a design inconsistency: `add` validates at entry time, but `check_outreach` relies on pre-validation. If a caller passes an invalid email that somehow gets past the `not email_n` check, `is_suppressed` will silently return False without validation.",
      "existing_code": "    def is_suppressed(self, email: str, tenant_id: str = \"demo\") -> bool:\n        return (tenant_id or \"demo\", self._norm(email)) in self._by_key\n\n    def check_outreach(\n        self,\n        *,\n        email: str,\n        tenant_id: str = \"demo\",\n        channel: str = \"email\",\n        mode: str = \"personalized\",\n    ) -> dict[str, Any]:\n        \"\"\"外发前闸门。抑制中 → allowed=False。\"\"\"\n        email_n =",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/suppression_list.py",
      "start_line": 136,
      "end_line": 143,
      "category": "performance",
      "severity": "low",
      "content": "Missing bounds check in list_all(): The method returns all entries without pagination. In a multi-tenant production scenario with thousands of suppression entries, this could return very large result sets, causing memory issues or slow API responses. Consider adding optional `limit` and `offset` parameters for pagination.",
      "existing_code": "    def list_all(self, tenant_id: str = \"\") -> list[dict[str, Any]]:\n        out = []\n        for (tid, email), e in self._by_key.items():\n            if tenant_id and tid != tenant_id:\n                continue\n            out.append(e.__dict__)\n        out.sort(key=lambda x: x.get(\"created_at\") or \"\", reverse=True)\n        return out",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/suppression_list.py",
      "start_line": 145,
      "end_line": 154,
      "category": "performance",
      "severity": "low",
      "content": "Inefficient string building in report(): The `plain` summary uses string concatenation with `+` operator and creates an unmaterialized generator expression inside `\"；\".join()`. While minor for small lists, if the suppression list grows large, this becomes O(n²) for concatenation and the generator creates unnecessary overhead. Pre-materialize the items into a list first: `parts = [f\"{k} {v}\" for k, v in by_reason.items()]; plain = \"抑制名单 \" + str(len(items)) + \" 人。\" + \"；\".join(parts) if parts else \"\"`",
      "existing_code": "    def report(self, tenant_id: str = \"demo\") -> dict[str, Any]:\n        items = self.list_all(tenant_id=tenant_id)\n        by_reason: dict[str, int] = {}\n        for i in items:\n            r = i.get(\"reason\") or \"unknown\"\n            by_reason[r] = by_reason.get(r, 0) + 1\n        plain = (\n            f\"抑制名单 {len(items)} 人。\"\n            + (\"；\".join(f\"{k} {v}\" for k, v in by_reason.items()) if by",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/adapters/__init__.py",
      "start_line": 1,
      "end_line": 9,
      "category": "documentation",
      "severity": "low",
      "content": "No issues found. The `__init__.py` is a package marker with only a docstring — it has no executable code, imports, or definitions to review.",
      "existing_code": "# -*- coding: utf-8 -*-\n# Copyright (c) 2026 吕博旺 (131025199403304817). All rights reserved.\n\"\"\"Adapter 层（Execution Plane 平级适配器集合，总纲 §3.1）。\n\n本包只放外部能力的适配桥接，禁止业务逻辑；所有适配器必须：\n1. 不持有链路状态（状态归 ai_tasks / Control Plane）；\n2. 不绕过 Policy / 租户隔离；\n3. 可整体替换（冻结原则 §1.2-2）。\n\"\"\"",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/adapters/tradeai/__init__.py",
      "start_line": 85,
      "end_line": 101,
      "category": "maintainability",
      "severity": "low",
      "content": "Lines 76-92: The `_normalize_cors_origins` function mutates the `env` dict parameter directly. While this works because `os.environ` is passed, the function signature suggests it operates on a generic dict. If called elsewhere with a different dict, this mutation could have unintended side effects. Consider using a clearer pattern like returning the normalized value instead of mutating in-place.",
      "existing_code": "def _normalize_cors_origins(env: dict) -> bool:\n    \"\"\"若 env 里 CORS_ORIGINS 是逗号分隔串（非 JSON 列表），改成 JSON 列表串。\n\n    返回是否发生改动。单元素 / 已 JSON / 空值均不动（无歧义）。\n    \"\"\"\n    raw = (env.get(\"CORS_ORIGINS\") or \"\").strip()\n    if not raw:\n        return False\n    if raw.startswith(\"[\"):\n        return False  # 已是 JSON 列表格式\n    items = [x.strip() for x in raw.split(\",\") if x.strip()]\n    if len(items) <= 1:\n       ",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/adapters/tradeai/__init__.py",
      "start_line": 138,
      "end_line": 140,
      "category": "other",
      "severity": "low",
      "content": "Line 124: The bare `except Exception` in `_load_vendor_isolated` catches all exceptions including those during module restore in the `finally` block. This masks errors and makes debugging vendor loading failures difficult. The comment suggests it should be pragma: no cover, but it's still broad. Consider catching specific exceptions or at least re-raising after logging for unexpected error types.",
      "existing_code": "    except Exception as e:  # pragma: no cover\n        logger.error(\"tradeai adapter: vendor 加载失败 %s: %s\", type(e).__name__, e)\n        return False",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/adapters/tradeai/__init__.py",
      "start_line": 175,
      "end_line": 184,
      "category": "other",
      "severity": "low",
      "content": "Lines 150-156: The `__getattr__` implementation uses `getattr(importlib.import_module(_EXPORTS[name]), name)` which can raise AttributeError in two ways: (1) if the module doesn't export the named attribute, or (2) if importlib.import_module fails. The original exception context is lost, making debugging harder. Consider using `raise ... from e` to preserve the traceback chain for import failures.",
      "existing_code": "def __getattr__(name: str) -> Any:\n    \"\"\"__getattr__。\n\n    参数说明：\n    :param name: 参数 name\n    :return: 返回处理结果。\n    \"\"\"\n    if name in _EXPORTS:\n        return getattr(importlib.import_module(_EXPORTS[name]), name)\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/adapters/tradeai/__init__.py",
      "start_line": 219,
      "end_line": 222,
      "category": "other",
      "severity": "low",
      "content": "Line 190-206: The `make_context` function creates UUIDs without any collision guarantee beyond hex length. In a high-throughput scenario with many concurrent workflows, the probability of collision increases. Consider using UUID4 (which `uuid.uuid4()` already does) with proper randomness, or adding a tenant prefix to ensure cross-tenant uniqueness.",
      "existing_code": "    ctx = ctx_cls(\n        workflow_id=workflow_id or f\"wf-{uuid.uuid4().hex[:12]}\",\n        execution_id=execution_id or f\"exec-{uuid.uuid4().hex[:12]}\",\n    )",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/goodjob/__init__.py",
      "start_line": 1,
      "end_line": 3,
      "category": "documentation",
      "severity": "low",
      "content": "The file is currently just a module stub with a copyright header and docstring. It appears incomplete for a service module named `goodjob/__init__.py`. If this is intentional (e.g., a placeholder), consider adding a clear comment or raising it with the team. If the module is supposed to expose API surface, those exports are missing.",
      "existing_code": "# -*- coding: utf-8 -*-\n# Copyright (c) 2026 吕博旺 (131025199403304817). All rights reserved.\n\"\"\"GoodJob 附属桥服务（批次 B：customer_pool 投影 + 单证委派）。\"\"\"",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/goodjob/customer_pool_projection.py",
      "start_line": 83,
      "end_line": 89,
      "category": "maintainability",
      "severity": "low",
      "content": "The idempotency key falls back to `created_at` when `updated_at` is None, but the module's design assumes `updated_at` is the authoritative change timestamp. This fallback changes key semantics and may cause unnecessary re-projections or missed deduplication. Since the code requires `updated_at` to be present for proper operation (as indicated by the type hints and design), consider failing loudly or handling this as a data integrity issue rather than silently falling back.",
      "existing_code": "def projection_idempotency_key(row: Any) -> str:\n    \"\"\"确定性幂等键：同询盘同状态同更新时间重放自动去重。\"\"\"\n    key = (\n        f\"gj-pool:{_attr_str(row, 'id')}:{_attr_str(row, 'status')}:\"\n        f\"{_attr_iso(row, 'updated_at') or _attr_iso(row, 'created_at')}\"\n    )\n    return key[:_KEY_MAX_LEN]",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/hermes/harness_gateway.py",
      "start_line": 19,
      "end_line": 19,
      "category": "maintainability",
      "severity": "low",
      "content": "Line 33: The import `from typing import Any, Dict, Optional` uses the older typing module style. In Python 3.9+, these can be imported from builtins or used directly (dict, list, etc.). Not a bug, but worth noting for consistency with modern Python style.",
      "existing_code": "from typing import Any, Dict, Optional",
      "scan": "ocr-scan-batch4.json"
    },
    {
      "path": "backend/app/services/hermes/harness_gateway.py",
      "start_line": 94,
      "end_line": 99,
      "category": "documentation",
      "severity": "low",
      "content": "The docstring describes `process_inbound_webhook` as a 'synchronous entry-point wrapper', but it's defined as an async function. If called from a sync context without `await` or `asyncio.run()`, this will raise `TypeError: object dict can't be used in 'await'`. The docstring is misleading and should be corrected to reflect the async nature, or a sync wrapper provided if true sync support is intended.",
      "existing_code": "async def process_inbound_webhook(db: Session, payload: Dict[str, Any]) -> Dict[str, Any]:\n    \"\"\"n8n / 外部 webhook 入站入口。\n\n    期望 payload 至少含：tenant_id、text（或 intent）。\n    返回真实拆解结果；缺少必填字段即**明确抛错**，不再返回硬编码字符串。\n    \"\"\"",
      "scan": "ocr-scan-batch4.json"
    },
    {
      "path": "backend/app/services/hermes/harness_gateway.py",
      "start_line": 113,
      "end_line": 115,
      "category": "maintainability",
      "severity": "low",
      "content": "Double payload.get('context') call: Lines 113 and 115 call payload.get() twice for the same key, which is inefficient. While minor, this pattern reduces readability and could cause confusion if the dict changes between calls in a non-deterministic context.",
      "existing_code": "context=payload.get(\"context\") if isinstance(payload.get(\"context\"), dict) else None,\n        intent=str(intent) if intent else None,\n        payload=payload.get(\"payload\") if isinstance(payload.get(\"payload\"), dict) else None,",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/hermes/harness_gateway.py",
      "start_line": 113,
      "end_line": 115,
      "category": "test",
      "severity": "low",
      "content": "Lines 92-94: The webhook handler validates that context and payload are dicts via isinstance checks, but doesn't validate nested values. Downstream `decompose()` or DB writes may expect specific types for nested fields. Consider adding schema validation or type checking for nested dict values to catch data integrity issues early at this boundary.",
      "existing_code": "        context=payload.get(\"context\") if isinstance(payload.get(\"context\"), dict) else None,\n        intent=str(intent) if intent else None,\n        payload=payload.get(\"payload\") if isinstance(payload.get(\"payload\"), dict) else None,",
      "scan": "ocr-scan-batch4.json"
    },
    {
      "path": "backend/app/services/hermes/task_control_supervisor.py",
      "start_line": 135,
      "end_line": 139,
      "category": "maintainability",
      "severity": "low",
      "content": "In `_resolve_input_from`, when `src_data` is found via `node_tasks` but `json.loads` fails, an empty dict `{}` is used. This silently suppresses data integrity errors and could lead to incorrect input resolution without any warning. Consider logging a warning when JSON parsing fails for upstream node outputs.",
      "existing_code": "        if src_data is None and src_node in node_tasks:\n            try:\n                src_data = json.loads(node_tasks[src_node].output_json or \"{}\")\n            except Exception:\n                src_data = {}",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/oauth_login.py",
      "start_line": 78,
      "end_line": 80,
      "category": "maintainability",
      "severity": "low",
      "content": "Dead code: The `_SENSITIVE_BODY_KEYS` constant includes `\"token\"` as a sensitive key. However, `_mask_body` is only called in error/log paths for QQ and WeChat token responses. In those contexts the key is typically `\"access_token\"` or `\"refresh_token\"`, which are already covered. The standalone key `\"token\"` is unlikely to appear in any OAuth response body, making this entry effectively dead coverage. More importantly, if any future response *does* contain a top-level `\"token\"` key, it would be masked — so this is a minor coverage gap concern rather than a bug. I flag it because it suggests the set was assembled without verifying actual response shapes.",
      "existing_code": "_SENSITIVE_BODY_KEYS = frozenset(\n    {\"access_token\", \"accessToken\", \"refresh_token\", \"openid\", \"unionid\", \"token\", \"secret\", \"app_secret\"}\n)",
      "scan": "ocr-scan-batch4.json"
    },
    {
      "path": "backend/app/services/oauth_login.py",
      "start_line": 92,
      "end_line": 95,
      "category": "documentation",
      "severity": "low",
      "content": "Style/Documentation: Several functions have empty or useless docstrings with the pattern `\"\"\"_function_name。\"\"\"` followed by `参数说明：:param ... :return: 返回处理结果。` which is boilerplate generated by a tool and adds no information. Examples: `_redirect_uri`, `_dev_authorize_url`, `_provider_configured`, `_exchange_feishu`, `_exchange_dingtalk`. These should either have meaningful docstrings describing behavior, or be removed entirely since the function names are self-explanatory. This also clutters the file and makes genuine documentation harder to find.",
      "existing_code": "def _redirect_uri() -> str:\n    \"\"\"_redirect_uri。\n    :return: 返回处理结果。\n    \"\"\"",
      "scan": "ocr-scan-batch4.json"
    },
    {
      "path": "backend/app/services/oauth_login.py",
      "start_line": 473,
      "end_line": 480,
      "category": "bug",
      "severity": "low",
      "content": "_parse_qq_response's fallback for non-JSON responses blindly splits on '&' and assumes all '=' containing text is query-style. If the response is a malformed JSON string containing '=' characters (e.g., a base64-encoded value), this could produce incorrect results. Additionally, the fallback doesn't validate that the parsed result is a dict with expected keys before returning it.",
      "existing_code": "        if \"=\" in text and not text.startswith(\"{\"):\n            result = {}\n            for part in text.split(\"&\"):\n                if \"=\" in part:\n                    k, v = part.split(\"=\", 1)\n                    result[k.strip()] = urllib.parse.unquote(v.strip())\n            if result:\n                return result",
      "scan": "ocr-scan-batch2.json"
    },
    {
      "path": "backend/app/services/payment_pkg/payment_service_impl.py",
      "start_line": 101,
      "end_line": 101,
      "category": "maintainability",
      "severity": "low",
      "content": "Inconsistent channel validation: some methods check `pay_channel not in (\"wechat\", \"alipay\")` while others default to `\"wechat\"` without validation. Ensure all entry points enforce the same whitelist to prevent injection of unsupported channel strings into downstream methods.",
      "existing_code": "        ch = (channel or \"wechat\").lower()",
      "scan": "ocr-scan-batch5-payment.json"
    },
    {
      "path": "backend/app/services/payment_service.py",
      "start_line": 5,
      "end_line": 14,
      "category": "maintainability",
      "severity": "low",
      "content": "Unnecessary re-imports: `hashlib`, `hmac`, and `os` are imported at the module level but also imported again inside methods (e.g., `verify_notify`). This is redundant and misleading, suggesting these are local or conditional imports. Remove the redundant inline imports and rely on the top-level declarations.",
      "existing_code": "import hashlib\nimport json\nimport logging\nimport time\nimport uuid\nfrom datetime import datetime, timezone\nfrom typing import Optional\n\nimport requests\nfrom sqlalchemy.orm import Session",
      "scan": "ocr-scan-batch5-payment.json"
    },
    {
      "path": "backend/app/core/config.py",
      "start_line": 0,
      "end_line": 0,
      "category": "bug",
      "severity": "",
      "content": "The _parse_bool_with_strip function handles string booleans but is only applied to REDIS_ENABLED and LOGIN_BF_USE_REDIS via field_validator. Other boolean fields like MODEL_GATEWAY_ENABLED, RLS_PILOT_ENABLED, etc. use Pydantic's default bool parsing, creating inconsistent environment variable handling across the config.",
      "existing_code": "@field_validator(\"REDIS_ENABLED\", \"LOGIN_BF_USE_REDIS\", mode=\"before\")\n    @classmethod\n    def validate_bool_settings(cls, v: Any) -> bool:\n        return _parse_bool_with_strip(v)",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/config.py",
      "start_line": 0,
      "end_line": 0,
      "category": "maintainability",
      "severity": "",
      "content": "The Settings class has 10+ properties with duplicated pattern logic (checking Optional[bool] flag, falling back to ENVIRONMENT == 'production'). This repetitive boilerplate is error-prone and hard to maintain. Consider extracting a helper method like `_scheduler_active(flag, default_env='production')` to reduce duplication.",
      "existing_code": "@property\n    def nvidia_customer_probe_scheduler_active(self) -> bool:\n        flag = self.NVIDIA_CUSTOMER_PROBE_SCHEDULER_ENABLED\n        if flag is not None:\n            return bool(flag)\n        return (self.ENVIRONMENT or \"\").strip().lower() == \"production\"",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/config.py",
      "start_line": 38,
      "end_line": 38,
      "category": "performance",
      "severity": "",
      "content": "The _env_file list comprehension filters out None values but doesn't validate that remaining path strings point to existing files. If ENV_FILE is set to a non-existent path, pydantic-settings will silently ignore it rather than raising an error, making debugging difficult.",
      "existing_code": "_env_file = [p for p in _env_candidates if p]",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/config.py",
      "start_line": 40,
      "end_line": 41,
      "category": "maintainability",
      "severity": "",
      "content": "The class contains ~150+ configuration fields spanning multiple domains (AI providers, database, Redis, scheduling, browser runtime, media storage, etc.) with no clear modular organization. This monolithic structure makes the file difficult to navigate, test, and maintain. Consider splitting into domain-specific settings classes.",
      "existing_code": "class Settings(BaseSettings):\n    model_config = ConfigDict(env_file=_env_file, env_file_encoding=\"utf-8\", extra=\"allow\")",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/config.py",
      "start_line": 113,
      "end_line": 113,
      "category": "bug",
      "severity": "",
      "content": "CORS_ORIGINS property does not validate that parsed origins are well-formed URLs. Malformed entries (e.g., 'http://', 'localhost', non-URL strings) will be passed through, potentially causing runtime errors in FastAPI's CORS middleware or allowing invalid origins.",
      "existing_code": "return [origin.strip() for origin in raw.split(\",\") if origin.strip()]",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/config.py",
      "start_line": 921,
      "end_line": 924,
      "category": "security",
      "severity": "",
      "content": "SECRET_KEY and JWT_SECRET_KEY share the same persistence file (logs/auto-secret-dev.txt). If this file is compromised or accidentally committed, both the application signing key and all JWT tokens are exposed. Consider separating them into distinct files or adding stricter OS-level access controls beyond chmod 0600.",
      "existing_code": "self._persist_dev_secret(\"secret\", generated)\n                warnings.warn(\n                    \"[DEV] SECRET_KEY 为占位符,已生成强随机密钥并写入 logs/auto-secret-dev.txt\"\n                    \"(跨重启复用,JWT 不会失效)\", stacklevel=2)",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/config.py",
      "start_line": 967,
      "end_line": 969,
      "category": "security",
      "severity": "",
      "content": "_init_production_key_validation() only checks a small subset of keys (JWT_SECRET_KEY, REDIS_PASSWORD, MINIO_ACCESS_KEY, MINIO_SECRET_KEY, SMTP_PASSWORD, DATABASE_URL). It misses many sensitive fields like AI_OPENAI_API_KEY, AI_ANTHROPIC_API_KEY, and other provider API keys that could be left as empty strings or weak values in production. This creates a false sense of security.",
      "existing_code": "for key_name in [\"JWT_SECRET_KEY\", \"REDIS_PASSWORD\",\n                             \"MINIO_ACCESS_KEY\", \"MINIO_SECRET_KEY\",\n                             \"SMTP_PASSWORD\"]:",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/config.py",
      "start_line": 1098,
      "end_line": 1100,
      "category": "documentation",
      "severity": "",
      "content": "The comment claims '文件已在 .gitignore 中,不会泄露' but there's no verification that this file is actually in .gitignore. If the gitignore entry is missing, secrets will be committed. Consider adding an explicit check or using environment variables exclusively for secrets.",
      "existing_code": "# Unix: 限制文件权限为 0600(仅所有者读写)\n            # Windows: chmod 无效,但 NTFS 继承自父目录权限;\n            # 无论如何,文件已在 .gitignore 中,不会泄露到版本库",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/core/config.py",
      "start_line": 1101,
      "end_line": 1106,
      "category": "security",
      "severity": "",
      "content": "The dev secret persistence relies on .gitignore to prevent leakage, but the security comment claims '文件已在 .gitignore 中,不会泄露到版本库'. If .gitignore is missing this entry or someone commits the file anyway, secrets are exposed. Additionally, os.chmod(f, 0o600) silently fails on Windows (caught by broad except), weakening the security claim in mixed environments.",
      "existing_code": "try:\n                import os\n                os.chmod(f, 0o600)\n            except (OSError, NotImplementedError):  # noqa: BLE001\n                # Windows 或受限环境:忽略,文件安全由 NTFS ACL / .gitignore 保护\n                pass",
      "scan": "ocr-scan-critical-full.json"
    },
    {
      "path": "backend/app/services/acquisition/tender_engine.py",
      "start_line": 112,
      "end_line": 113,
      "category": null,
      "severity": "",
      "content": "No validation on `amount`: `set_payment` and `upsert` accept arbitrary floats, including negative values and values indistinguishable from corrupt input. The `advance()` path and `view()` output display the amount without sanity checks. At minimum validate `amount >= 0` and consider a currency-specific max. The docstring mentions `amount` but the rules do not enforce it.",
      "existing_code": "        if amount:\n            proj.amount = float(amount)",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/tender_engine.py",
      "start_line": 117,
      "end_line": 119,
      "category": null,
      "severity": "",
      "content": "Missing-key mutation risk in `view()`: returns `**self.checklist(tender_id)` (which unpacks a fresh dict) but then also passes `self.checklist(tender_id)` again when constructing the final dict. More importantly, callers who mutate `proj.docs_ready` in-place (e.g. via `mark_doc`) are mutating the instance's internal dict directly. If `checklist()` returned a shallow copy (`dict(proj.docs_ready)`), external mutations of the returned dict would not leak into the record — but here the docs dict is the real one. Consider copying mutable state on read or documenting the aliasing contract.",
      "existing_code": "    def checklist(self, tender_id: str) -> dict[str, Any]:\n        proj = self.upsert(tender_id=tender_id)\n        docs = dict(proj.docs_ready or {})",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/tender_engine.py",
      "start_line": 157,
      "end_line": 159,
      "category": null,
      "severity": "",
      "content": "Credit-risk heuristic is incomplete: `risky_terms` scans for a fixed set of keywords/numbers (`账期`, `credit`, `oa`, `lc`, `90`, `120`, `180`). Terms like `net60`, `DP`, `TT`, `30 days`, or other legitimate long-payment variants slip through and get classified as `auto_pi_allowed=True`, contradicting the module docstring rule '账期/风险未过人审禁止标可自动 PI'. Consider expanding the keyword set, normalizing term parsing, or explicitly rejecting any term not in a whitelist.",
      "existing_code": "        # 长账期 + 未信用确认 → 禁自动 PI\n        risky_terms = any(x in (terms or \"\").lower() for x in (\"账期\", \"credit\", \"oa\", \"lc\", \"90\", \"120\", \"180\"))\n        proj.auto_pi_allowed = bool(credit_ok) and not risky_terms",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/tender_engine.py",
      "start_line": 176,
      "end_line": 181,
      "category": null,
      "severity": "",
      "content": "Concurrent check-then-act race: `advance()` calls `checklist()` and then later mutates `proj.stage` / appends to `proj.history`. The module-level singleton `tender_engine._by_id` is unprotected — concurrent calls for the same `tender_id` can interleave, causing lost updates, duplicate history entries, or inconsistent stage transitions. If this engine is used under any multi-threaded or async load (web server, Celery tasks, etc.), a `threading.Lock` or `asyncio.Lock` keyed by `tender_id` (or a single lock) should guard all read-modify-write sequences.",
      "existing_code": "    def advance(self, tender_id: str, to_stage: str, note: str = \"\") -> dict[str, Any]:\n        proj = self.upsert(tender_id=tender_id)\n        to_stage = (to_stage or \"\").strip().lower()\n        if to_stage not in STAGES:\n            return {\"ok\": False, \"message\": f\"非法阶段 {to_stage}\", \"stages\": list(STAGES)}\n        checklist = self.checklist(tender_id)",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/tender_engine.py",
      "start_line": 190,
      "end_line": 192,
      "category": null,
      "severity": "",
      "content": "Stage transition logic allows arbitrary jumps: `advance()` rejects non-STAGES names and guards `tender_submit` with the doc checklist, but `won` / `lost` can be set from *any* preceding stage (including `lead` or `qualify`), bypassing the qualification gate entirely. Line 188–189 `pass` silently permits this. Either enforce a strict transition table (e.g. only `evaluation -> won|lost`) or at minimum require the checklist to be ready before reaching a terminal state.",
      "existing_code": "        if to_stage in (\"won\", \"lost\") and proj.stage not in (\"evaluation\", \"tender_submit\", \"bid_prep\"):\n            # 允许从任意阶段结果登记，但提示\n            pass",
      "scan": "ocr-scan-batch6-acq-rest.json"
    },
    {
      "path": "backend/app/services/acquisition/tender_engine.py",
      "start_line": 198,
      "end_line": 205,
      "category": null,
      "severity": "",
      "content": "Invalid keyword argument in return value construction: in `advance()`, the dict literal contains the bare name `checklist` as a key (line 212). This raises `SyntaxError: keyword can't be an expression` because `checklist` is being used as a keyword argument name without a value assignment. The intent was likely `\"checklist\": self.checklist(tender_id)`. Fix to `\"checklist\": self.checklist(tender_id)`.",
      "existing_code": "        return {\n            \"ok\": True,\n            \"tender_id\": proj.tender_id,\n            \"stage\": proj.stage,\n            \"stage_label\": STAGE_LABELS.get(proj.stage, proj.stage),\n            \"checklist\": self.checklist(tender_id),\n            \"plain\": f\"已进入阶段：{STAGE_LABELS.get(proj.stage, proj.stage)}\",\n        }",
      "scan": "ocr-scan-batch6-acq-rest.json"
    }
  ]
}