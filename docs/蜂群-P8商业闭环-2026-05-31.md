# 蜂群 P8 商业闭环 — 2026-05-31

> 承接 P7：审计日期筛选、支付宝探针、Staging 支付自检。

## 范围（W27–W29）

| ID | 交付 | 边界 |
|----|------|------|
| W27 | 改派审计日期区间 | `start_at` / `end_at`（ISO 或 YYYY-MM-DD）；询盘页 RangePicker |
| W28 | 支付宝沙箱探针 | `POST /payment/ops/alipay/probe`（super_admin）；不落库 precreate |
| W29 | Staging 自检 | `POST /payment/ops/staging/self-check` + CLI `staging_payment_self_check.py` |

## 后端

### W27

- `parse_audit_datetime()` — 纯日期按 UTC 日边界
- `GET /assignment-audit/export?start_at=&end_at=&source_channel=`

### W28

- `probe_alipay_sandbox()` — 配置检查 + 可选 precreate
- `POST /api/v1/payment/ops/alipay/probe`

### W29

- `run_staging_payment_self_check(db)` — 微信证书刷新 + 双渠道进程内 notify
- 总体 `ok` 以 **notify 闭环** 为准；密钥未配时 config 项为 informational
- `public_notify_hint` — 外网 HTTPS 回调配置提示

## 前端

| 页面 | 变更 |
|------|------|
| `/admin/inquiries` | 改派审计导出 + 日期 RangePicker |
| `/admin/finance/payment-ops` | 支付宝探针、Staging 自检按钮与结果区 |

## 测试

```powershell
python -m pytest tests/unit/test_commercial_loop_p8.py -q
python scripts/staging_payment_self_check.py
python scripts/staging_payment_self_check.py --http --base-url http://127.0.0.1:8001
```

## 后续（P9）

- Staging 外网 notify 验收脚本（ngrok + 真实网关回调）
- 改派审计列表预览 API（分页，非仅 CSV）
- 支付运维：微信 precreate 探针对称能力
