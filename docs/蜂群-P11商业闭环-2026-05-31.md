# 蜂群 P11 商业闭环 — 2026-05-31

> 承接 P10：公网 notify 端到端 POST、审计操作人显示名、探针历史落库。

## 范围（W36–W38）

| ID | 交付 | 边界 |
|----|------|------|
| W36 | 公网 notify 端到端 | `run_public_notify_e2e_check` + `POST /ops/staging/public-notify-check` |
| W37 | 审计操作人姓名 | `query_assignment_audit_rows` join User → `actor_user_name` |
| W38 | 运维审计落库 | `payment_ops_audit` 表 + `GET /ops/audit` + 探针/自检自动写入 |

## 后端

### W36

- `run_public_notify_e2e_check(db, public_url=, discover_ngrok=)` — 经 ngrok 公网 POST 双渠道 notify
- `POST /api/v1/payment/ops/staging/public-notify-check?public_url=&discover_ngrok=`
- `staging_notify_http_check.py --via-public` — CLI 同源

### W37

- `_enrich_actor_names()` — 批量 join `users.username`
- CSV 增加 `actor_user_name` 列

### W38

- 模型 `PaymentOpsAudit`（migration `037_payment_ops_audit`）
- `log_payment_ops_action()` / `list_payment_ops_audit_page()`
- 探针、证书刷新、staging 自检、公网探测、公网 notify 验收均自动落库
- `GET /api/v1/payment/ops/audit?page=&page_size=&action=`

## 前端

| 页面 | 变更 |
|------|------|
| `/admin/inquiries` | 审计 Drawer 增加「操作人」列 |
| `/admin/finance/payment-ops` | 「公网 notify 验收」按钮 + 运维操作历史表 |

## 测试

```powershell
python -m pytest tests/unit/test_commercial_loop_p11.py -q
python scripts/staging_notify_http_check.py --base-url http://127.0.0.1:8001 --via-public --discover-ngrok
```

## 后续（P12）

- 公网 notify 验收：真实 ngrok + 严验签模式分支
- 运维审计：按 action / 时间筛选 + 详情 Drawer
- 审计 CSV：操作人姓名列在前端导出预览中同步展示
