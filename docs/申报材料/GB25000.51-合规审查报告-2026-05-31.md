# GB/T 25000.51 合规审查报告（项目改造 · 第二阶段）

---

## 文档信息

| 项目 | 内容 |
|------|------|
| **软件名称** | 优丁建材 AI-SaaS 智能营销平台 |
| **版本号** | V2.0 |
| **依据标准** | GB/T 25000.51-2016（RUSP 质量要求与测试细则） |
| **审查类型** | 开发方工程合规审查 + 送检前改造基线 |
| **审查日期** | 2026-05-31 |
| **源码仓（唯一）** | `C:\Users\97907\Desktop\上线网站` |
| **工具/安装包引用** | `UJ/website CodeBuddy`（仅 ECC 路径等，不整仓打开） |
| **知识库** | `Desktop/出海计`（文档镜像，非运行源） |

---

## 一、工具 · 环境 · 调用逻辑（必须记住）

### 1.1 三层边界

| 层 | 路径 | 用途 | 送检/部署 |
|----|------|------|-----------|
| **产品源码** | `上线网站/` | 改 Bug、跑测、构建 | ✅ |
| **开发工具** | `~/.devstack/`、`.cursor` Junction、CodeGraph 索引 | AI/ECC，gitignore | ❌ |
| **历史大仓** | `website CodeBuddy/` | ECC 包、对照，~6.9GB | ❌ 不部署 |

### 1.2 工具链与调用顺序

```text
[本地开发]
  scripts/start-dev-admin.ps1
    → API :8001  backend/youding_dev.db (SQLite)
    → Admin :5173  Vite dev
    → 账号 admin / admin123

[前端工程门禁]
  cd frontend/admin
  npm run cert:gate
    → run-certification-gate.mjs
      → audit-page-stubs.mjs      (P0/P1/P2 占位页)
      → check-nav-routes.mjs      (菜单 79 路由)
      → check-nested-routes.mjs   (嵌套 Tab)
      → find-corrupt-vue.mjs      (损坏 Vue)
      → check-interactive-stubs.mjs (菜单内假数据/死按钮)
    → 输出 docs/certification-gate-admin-latest.json

[仓库级送检门禁]
  powershell -File scripts/run-provincial-cert-gate.ps1
    → cert:gate → npm run build → check_mounted_routes.py → pytest tests/unit
    → 输出 docs/provincial-cert-gate-latest.json

[后端单测]
  cd backend && python -m pytest tests/unit -q

[文档同步 Obsidian]
  powershell -File scripts/sync-to-chuhaiji-kb.ps1
```

### 1.3 运行时调用逻辑（送检演示路径）

```text
浏览器 → Admin (Vue3 + Ant Design Vue)
  → api/index.ts | utils/api.ts (JWT + 401 静默刷新)
  → /api/v1/*  FastAPI
  → SQLite(开发) / PostgreSQL(送检 staging)
```

**认证**：`optional_auth` / `get_current_user`；超管路径 `super_admin|admin`。  
**数据中心**：`GET /api/v1/super-admin/aggregation` ← `aggregation.vue` 须走 `utils/api.ts` 带 Token。

### 1.4 改造原则（本阶段）

- 只改 **`上线网站`** 内产品代码与 `docs/申报材料`
- 不新增平行配置文件；ECC 仍从 `CodeBuddy/v1.10.0/ECC-1.10.0` 路径安装
- 工程门禁绿 ≠ 检测机构全项合格（72h、Locust、CMA 报告另计）

---

## 二、2026-05-31 实测结果

| 检查项 | 命令/工具 | 结果 | 证据 |
|--------|-----------|------|------|
| 管理端 cert:gate | `npm run cert:gate` | **PASS** | P0/P1/P2=0；interactive=0 |
| 后端 unit | `pytest tests/unit` | **337 passed** | 2026-05-31 |
| views Math.random | ripgrep | **0 处** | 功能性/真实性 |
| 源码仓 | — | **上线网站** | 非 CodeBuddy |
| 省级一键门禁 | `run-provincial-cert-gate.ps1` | **READY** | 2026-05-31 四步全绿 |

---

## 三、GB/T 25000.51 质量特性审查

| 特性 | 子特性 | 工程结论 | 证据 | 送检前待办 |
|------|--------|----------|------|------------|
| **功能性** | 完备性、正确性 | **合格（工程）** | cert:gate 全绿；79 路由 0 缺失 | PM 演示截图 12 模块 |
| **性能效率** | 时间、资源 | **部分合格** | `run-performance-smoke.py` 就绪 | staging Locust 1000 用户报告 |
| **兼容性** | 互操作 | **合格** | Chrome/Edge；Vue3+AntDV4 | — |
| **易用性** | 可识别、防错 | **合格** | 全中文；删除确认 | — |
| **可靠性** | 成熟、容错 | **部分合格** | 门禁 0 stub；API 空态 | staging **72h** 长跑 |
| **信息安全** | 保密、完整 | **部分合格** | JWT；v-html 收敛 | 生产 Postgres；CVE 周期升级 |
| **维护性** | 模块化、可分析 | **合格** | 模块化路由；cert 自动化 | 可选恢复 `useDebounce` 钩子（标准建议，非门禁阻断） |
| **可移植性** | 适应、安装 | **合格** | Docker Compose；deploy/ | staging preflight 0 fail |

**综合（工程门禁）**：**可进入送检材料整理与 staging 实机阶段**。  
**综合（检测机构）**：须 CMA/CNAS 第三方 + 72h/并发/Postgres 附件。

---

## 四、本阶段改造清单（已完 / 进行中 / 待办）

### 已完成（P0 工程）

- [x] cert:gate P0/P1/P2 清零
- [x] hierarchy / article-generator / landing 接 API
- [x] test_ai_dashboard 401 修复
- [x] pytest 337/337
- [x] 源码仓固定为 `上线网站`；文档同步链改正
- [x] 开发工具与产品代码隔离（gitignore + SOURCE-REPO）

### 进行中（P1 送检包）

- [x] 更新 `软件产品自测报告-GB25000.51-2026.md` 日期与附件路径
- [x] `run-provincial-cert-gate.ps1` → **READY**（cert + build + routes + 337 unit）
- [x] `送检演示脚本.md` + `送检用例追溯矩阵.md` + `STAGING-送检环境清单.md`
- [x] `省级软件评测-达标总清单.md` 基线刷新至 2026-05-31
- [ ] PM 签字、12 模块截图、浏览器回归表填写

### 待 staging（P0 对外声明）

- [ ] ARCH-C01 Postgres + Redis + HTTPS
- [ ] `run_production_preflight.py` → 0 fail
- [ ] 72h 可靠性、`load_test.py` 1000 并发归档
- [ ] G4 真实 AI Key（gates-check）

---

## 五、改造执行命令（复制即用）

```powershell
# 1. 启动开发环境（唯一入口）
cd "C:\Users\97907\Desktop\上线网站"
powershell -ExecutionPolicy Bypass -File scripts/start-dev-admin.ps1

# 2. 工程合规全扫
powershell -ExecutionPolicy Bypass -File scripts/run-provincial-cert-gate.ps1

# 3. 隔离校验（工具不进送检包）
powershell -ExecutionPolicy Bypass -File scripts/verify-dev-isolation.ps1

# 4. 文档 → Obsidian
powershell -ExecutionPolicy Bypass -File scripts/sync-to-chuhaiji-kb.ps1
```

---

## 六、附件索引

| 附件 | 路径 |
|------|------|
| 管理端门禁 JSON | `docs/certification-gate-admin-latest.json` |
| 省级门禁 JSON | `docs/provincial-cert-gate-latest.json` |
| 自测报告 | `docs/申报材料/软件产品自测报告-GB25000.51-2026.md` |
| 达标总清单 | `docs/省级软件评测-达标总清单.md` |
| 送检演示脚本 | `docs/送检演示脚本.md` |
| Staging 清单 | `docs/申报材料/STAGING-送检环境清单.md` |
| 用例追溯矩阵 | `docs/送检用例追溯矩阵.md` |
| 源码仓说明 | `docs/SOURCE-REPO.md` |
| 工具隔离 | `docs/DEV-TOOLS-ISOLATION.md` |
| 代码规范（知识库） | `出海计/standards/GB25000-代码规范化标准.md` |

---

**审查结论**：工程侧 **GB/T 25000.51 门禁已通过**，可推进 **staging 实机 + 申报材料定稿 + 省级评测中心送检**。  
**审查人**：研发 QA（自动扫描 + 人工复核）  
**PM 审核**：____________ **日期**：____________
