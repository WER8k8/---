# 开发进度看板

> **最后更新**：2026-05-25 · 产品模块 **156/200（约 78%）**  
> 运行 `python scripts/module_progress.py` 可刷新终端快照与 `frontend/admin/src/data/module-progress.json`。  
> **多 IDE 同步**：`powershell -ExecutionPolicy Bypass -File scripts/sync-dev-docs-to-ides.ps1`  
> 索引：[`多IDE开发进度与配置索引.md`](多IDE开发进度与配置索引.md)  
> 管理端：**系统 → 开发进度**（`/admin/system/progress-board`）。

## 图例

- 黑色条：**█** = 已完成，**░** = 未完成（终端）
- 管理端：黑底 `#111` 为完成，浅灰 `#e8e8e8` 为剩余

## 两层进度

| 层级 | 含义 | 数据来源 |
|------|------|----------|
| **排期研发** | Sprint A～O 任务 | `docs/dev-backlog-sprint.json` |
| **产品功能模块** | 七步闭环、三轨计费、财务、枢纽等全景 | `docs/module-progress.json`（权重估算） |

排期 100% 不等于产品商用 100%：后者含 PM 签字、演示域、P2 差异化等。

## 最新快照

见 `docs/module-progress-latest.txt` 与 `docs/dev-progress-latest.txt`。
