# 主管理后台（frontend/admin）开发地址，无末尾斜杠。与矩阵不同端口时必须配置，否则无法带 token 跳转。
# VITE_MAIN_ADMIN_ORIGIN=http://localhost:5174

# 主后台登录页路径（需与主应用实际路由一致；主应用若无 /login 可改为 / 或其它路径）
# VITE_MAIN_ADMIN_LOGIN_PATH=/login

# 可选：API 基路径（留空则走 Vite 代理 /api）
# VITE_API_BASE_URL=
