# 06 — 租户用户开通/邀请流程补 tenant_id 写入

**What to build:** 新用户落地时正确赋 tenant_id（注册、超管建租户成员、邀请链接），否则 T01 加的列永远是 NULL、隔离无从生效。本票闭合"写入侧"，让新数据自带租户归属。可先于 T02/T03 并行起草，但必须在 T03 推广前完成，以免新写入无归属。

**Blocked by:** 01（列存在才能写）

**Status:** ready-for-agent

- [ ] 定位所有创建 User 的路径（注册/后台建用户/邀请/UJ 首登建本地会话映射）
- [ ] 每条路径按上下文写入 tenant_id（无租户上下文则显式 NULL=平台级并留审计）
- [ ] 补用例：经各入口创建的租户用户，其 tenant_id 正确
- [ ] 不回归平台级 admin 创建路径（保持 NULL）
