---
trigger: always_on
---
全国县域建材 SEO 矩阵 AI 全自动后台系统 —— 最终完整版总技术文档
（最终定稿・全覆盖・无遗漏・闭环完整・直接交付 Trae 执行，严格对照执行，零妥协、零删减、零修改）
一、项目整体定位（总目标・不可变更，核心前提）
1.  系统定位：商用级、私有化部署、AI 全流程自动化的全国县域 SEO 霸屏分发后台，聚焦建材行业（轻集料混凝土、保温砂浆、聚氨酯颗粒、保温建材、厂家直供等核心关键词）。
2.  核心目标：覆盖全国2800+个县/区，实现任意县城用户搜索建材相关关键词时，百度手机首页必出现我方官网+多平台矩阵内容，避开百度竞价内卷，实现全国免费精准获客。
3.  责任边界：需求方完全不懂代码、不会调试、不会修改、不会排查Bug，所有设计、架构、编码、自测、优化、AI逻辑全部由你（Trae）独立完成，需求方仅做最终验收与使用，不参与任何技术环节。
二、核心业务总规则（不可违反，开发底线）
1.自动化要求：人工工作量压缩至5%以内，95%以上工作由AI全自动完成，无需需求方介入任何操作。
2.素材提供：禁止让需求方提供设计稿、原型图、架构图、文案、模板、关键词表，所有素材、模板、词库均由你（Trae）通过AI生成或自主搭建。
3.功能要求：所有功能必须真实可用、真实有效、可落地、可长期稳定运行，严禁Demo化、玩具化、半成品化，杜绝“看似能用、实际无效”的敷衍开发。
4.代码要求：代码必须极致精准、零Bug、零语法错误、零逻辑漏洞、零运行崩溃，严禁胡编乱造、敷衍凑数，每一行代码都需可追溯、可复用。
5.交付要求：部署即上线、登录即使用、无需改代码、无需调配置、开箱即用，需求方无需掌握任何技术，即可正常操作。
三、整体技术栈规范（固定不可修改，技术基础）
所有技术选型以“稳定、高效、适配建材SEO场景、可私有化部署”为核心，严格遵循以下规范，不可随意替换技术栈：
•前端：Vue3 + Element Plus 标准后台UI框架（优先采用vue-element-plus-admin基础模板优化，适配企业级后台布局，减少冗余开发）；
•后端：Node.js（优先选择稳定版本，注重接口性能与异步处理能力，适配多平台分发队列需求）；
•数据库：MySQL 8.0+（表结构严谨，支持大数据量存储与高效查询，适配县域词库、关键词库、发布日志等海量数据场景）；
•任务队列：定时任务 + 异步发布队列（核心用于防风控、防封号，实现多平台错峰发布、批量发布，避免并发过高导致账号异常）；
•AI能力：内置大模型，自动完成文案生成、伪原创、组词、改写、内容适配、逻辑判断、异常处理，无需额外调用第三方AI接口（避免依赖与费用）；
•部署：支持Linux服务器独立部署，提供一键部署脚本（Shell脚本）与完整部署文档，明确运行环境依赖、部署步骤、常见问题解决，确保需求方可自行完成部署。
四、后台 UI 与结构设计规范（AI 自动完成，无需需求方提供）
UI设计与结构由你（Trae）通过AI完成，无需需求方提供任何参考，严格遵循以下规范：
1.布局规范：采用企业级标准后台布局，左侧固定菜单 + 顶部导航 + 面包屑 + 主内容区，布局清晰、操作便捷，适配PC端常用分辨率。
2.界面风格：商务简洁、统一规范，无花哨视觉元素、无排版错乱、无按钮失效，色彩以商务蓝为主，符合建材行业调性。
3.菜单规范：菜单层级严格固定为6大一级模块，不可合并、不可删减、不可改名，模块顺序固定如下：
        
1. 系统全局设置
        
2. 全国地域词库管理
        
3. 行业关键词库与AI组词
        
4. AI文案模板与全自动生成
       
5. 多平台账号管理与AI分发中心
        
6. 发布记录、数据看板与收录监控
      
4.交互规范：所有列表支持搜索、筛选、分页、排序、批量操作、导出Excel；所有操作必须有成功/失败提示、二次确认（高危操作）、异常反馈、日志记录；全局交互流畅，无卡顿、无白屏、无接口超时、无数据错乱，页面加载时间≤1秒。
五、全功能模块详细技术指标（全覆盖・不可缺漏，核心开发内容）
按6大一级模块逐一开发，每个模块功能、技术指标必须全部实现，无缺漏、无简化，严格对照以下要求落地：
模块1：系统全局设置（基础配置，全局生效）
•基础信息配置：支持配置品牌名、官网域名、联系电话、公司简介，设置为全局变量，一处修改全系统同步生效（无需手动修改其他模块内容）。
•外链统一规则：可配置外链自动插入位置（文末/正文/简介）、外链格式、是否生成短链、是否统一添加参数（用于SEO追踪），规则设置后，AI生成文案、分发内容时自动遵循。
•AI生成配置：支持选择AI模型、设置创作长度（500-2000字可调）、伪原创强度（低/中/高可调）、通顺度阈值、广告合规度检测开关，配置后AI自动按规则生成内容。
•发布风控规则：可配置单平台每日发布上限、发布间隔时间（1-60分钟可调）、同账号发布频率限制、自动错峰发布（避开平台风控高峰时段），规则生效后，AI分发时自动遵循。
•账号安全：支持管理员权限设置（仅1个超级管理员）、操作日志全记录（记录所有操作人、操作时间、操作内容、操作结果）、密码加密存储（采用bcrypt加密算法）、账号登录异常提醒，保障系统安全。
模块2：全国地域词库管理（核心基础，必做完善）
•词库基础：内置完整全国省—市—县/区三级结构，一次性完整入库2800+县域，无缺失、无重复、无错误（参考最新民政部行政区划数据）。
•操作功能：支持一键展开全省县域、批量勾选县域（用于批量发布）、单独禁用县域（无需发布的区域）、按省份筛选县域，操作便捷。
•AI维护：AI自动对词库进行去重、纠错、行政区划更新（若有县域名称、归属调整，AI自动同步更新），无需人工干预。
•导出导入：支持县域列表一键导出Excel、批量导入县域（备用，用于紧急补充）、筛选检索（按省份、县域名称搜索）。
模块3：行业关键词库与AI智能组词（SEO核心，精准获客）
•基础词库：内置建材行业全量词库，涵盖产品词（轻集料混凝土、保温砂浆等）、厂家词、工程词、供货词、价格词、同城词，无缺失、无无效词，AI自动维护更新。
•手动操作：支持手动新增、编辑、分组关键词，支持批量导入导出关键词，满足个性化需求。
•AI组词引擎：支持无限模板公式（可自定义添加模板），例如：
        
{县城} + {产品} + 厂家
        
{县城} + {产品} + 现货直供
        
{县城} + 保温建材 + 工程供货
        
一键自动生成5万～10万条县域长尾词，AI自动去重、自动过滤无效词（无意义、无搜索量的词），确保关键词精准适配县域SEO场景。
模块4：AI文案模板与全自动生成（核心AI能力，内容产出）
•模板管理：内置多套高转化率SEO商用模板（适配建材行业，涵盖产品介绍、厂家优势、工程案例、价格咨询等场景），AI自动维护模板，无需需求方编写、修改。
•全局变量替换：AI自动识别文案中的全局变量（{县城名} {产品名} {品牌} {官网链接} {电话}），并精准替换，确保每个县域、每个产品的文案个性化。
•文案生成：每个县域独立生成1篇专属文章，不重复、不搬运、不通篇替换，AI自动完成原创创作、段落逻辑梳理、通顺度优化、广告合规检测（过滤极限词、违规词）。
•伪原创功能：AI自动完成同义词替换、段落重排、句式改写，确保文案重复率≤10%（适配百度SEO收录规则）。
•操作功能：支持批量生成文案（勾选多个县域、多个产品一键生成）、单篇预览（生成后可预览效果）、在线编辑（手动微调，备用）、一键存入发布池（直接用于后续分发）。
•稳定性要求：文案生成过程稳定不崩溃、不卡顿、不乱码、不丢失内容，批量生成1000篇文案无异常。
模块5：多平台账号管理与AI全自动分发（核心业务，霸屏关键）
平台分类固定如下，不可删减、不可更改，所有平台均需实现账号绑定与AI分发功能：
•平台列表：
        
百度系：百家号、百度贴吧、百度知道、百度文库、百度爱采购
        
阿里系：阿里博客、优酷号、阿里云社区
        
腾讯系：企鹅号、腾讯新闻号、QQ空间
        
字节系：头条号、西瓜视频、抖音
        
高权重博客：新浪博客、网易博客、搜狐博客、和讯博客
        
建材垂直B2B：慧聪、九正建材、中国建材网、广材网、建材之家
        
高权重论坛与目录：天涯、西祠胡同、站长目录、爱站目录
      
•账号能力（核心技术落地）：
        
1.  有官方API的平台（如百度爱采购、阿里云社区）：必须走OAuth官方授权，实现Token自动刷新，无需手动重新授权；

2.  无官方API的平台：AI自动模拟登录（适配平台登录机制，规避验证码、滑块验证）+ Cookie持久化存储，基于JWT+Cookie实现登录状态持久化，后台长期保活，无需重复登录，登录状态有效期≥7天；
        
3.  账号管理：支持账号新增、编辑、删除、禁用，可查看账号登录状态、发布记录，异常账号自动标记。
      
•平台配置：每个平台独立配置标题字数限制、图片规则（是否需要配图、图片尺寸）、外链允许位置、是否允许发布、单日发布限额，配置后AI分发时自动适配。
•AI发布能力：
        
1.  发布模式：支持立即发布、定时发布（设置具体发布时间）、分批间隔发布（按配置的间隔时间批量分发）；
        
2.  平台适配：AI自动适配各平台格式、排版、标签、分类，无需人工调整；
        
3.  防风控：同内容多平台自动小幅改写（避免平台判定重复发布）、错峰发布、频率控制（遵循全局风控规则）；
        
4.  状态管理：发布进度实时显示、成功/失败状态明确标注、失败原因详细记录（如账号异常、内容违规、平台限制）、失败自动重试3次（每次间隔5分钟）；
        
5.  成功率要求：发布成功率≥85%，无大规模封号、无频繁拦截，若出现账号异常，AI自动标记并提醒。
      
模块6：发布记录、数据看板与收录监控（效果追踪，闭环管理）
•发布日志：记录全量发布信息，包括发布时间、县域、关键词、平台、发布状态、内容链接、平台返回信息、收录状态，日志永久留存，不可删除。
•日志操作：支持按平台、县域、日期、发布状态（成功/失败/待发布）筛选检索，支持导出Excel（用于数据统计）。
•数据可视化看板：直观展示今日发布量、总发布量、发布成功率、平台分布（各平台发布占比）、县域覆盖热力图（展示已覆盖县域、未覆盖县域），数据实时更新（延迟≤5分钟）。
•收录监控（核心技术落地）：集成百度收录自动检测接口，基于站长工具开放平台百度收录API实现，需配置APIKey并处理接口异常（如额度不足、APIKey缺失），AI自动判断发布内容是否被百度收录、是否有排名、是否进入百度手机首页，每日自动检测1次，检测结果实时更新至看板，未收录内容自动标记并触发AI优化策略（重新生成文案、重新分发）。
•异常提醒：异常账号自动预警（如登录失败、封号）、发布失败自动提醒（弹窗+日志标注）、数据异常自动标记（如某平台发布成功率骤降）。
六、AI 全流程自动化规则（必须100%实现，核心要求）
AI自动化是本系统核心，所有以下流程必须由AI全自动完成，无需人工干预，人工仅需完成3步操作，其余全部由AI落地：
1.AI自动完成：系统架构设计、数据库表结构设计、UI布局设计、菜单结构设计、前后端代码编写（无需人工写一行代码）；
2.AI自动完成：全国2800+县域词库入库、建材行业词库构建、关键词组合生成、文案标题自动生成；
3.AI自动完成：全文案创作、伪原创降重、合规检测、外链自动植入、内容格式优化（适配各平台）；
4.AI自动完成：多平台账号登录状态维护、发布内容适配、风控策略执行、频率控制、失败重试、异常处理（如账号异常、发布失败）；
5.AI自动完成：数据统计（发布量、成功率等）、收录检测、效果判断（是否收录、是否排名）、问题自检（如代码异常、数据错乱）、全流程日志记录；
6.人工仅需3步：① 进入系统全局设置，配置基础信息（品牌、域名等）；② 进入全国地域词库管理，勾选需要覆盖的县域；③ 启动AI全自动运行，其余所有工作（组词、生成文案、分发、监控）全部由AI完成。
七、代码质量极致强制约束（最高优先级・零妥协，开发红线）
因需求方完全不懂代码，你（Trae）必须严格遵守以下约束，零妥协、零敷衍，确保系统稳定商用：
1.零Bug要求：严禁编写任何有Bug、有语法错误、有逻辑漏洞、有崩溃风险的代码，所有代码必须经过反复自测，确保无任何运行异常。
2.代码规范：代码必须极度精准、结构清晰、模块化开发（按功能模块拆分，便于后续维护），变量命名规范（见名知意）、关键逻辑全中文注释（注释覆盖率≥90%），严禁乱命名、无注释。
3.异常处理：必须全覆盖所有异常场景，包括空值、越界、接口超时、重复提交、权限越权、数据库异常、网络异常、API调用异常等，所有异常均需有处理逻辑，杜绝系统崩溃、数据丢失。
4.数据库规范：数据库表结构必须严谨，字段类型精准（如县域ID用int、县域名称用varchar）、索引完整（优化查询速度）、非空约束、关联正确（无关联错误）、无冗余字段、无数据冲突，交付时提供可直接执行的SQL脚本（无需需求方修改）。
5.接口规范：所有前后端接口必须稳定、返回格式统一（JSON格式）、无报错、无超时（接口响应时间≤2秒）、无数据丢失，接口文档完整（用于后续维护，无需需求方查看）。
6.自测要求：交付前，你（Trae）必须自行完成全功能测试（每个按钮、每个功能逐一测试）、边界测试（极端场景测试，如批量生成10万条关键词、同时分发100个平台）、压力测试（模拟100人同时操作）、72小时稳定性测试（系统连续运行72小时无异常），确保零Bug交付。
7.交付要求：严禁交付半成品、Demo版、测试版、残缺版，必须交付可直接商用的完整正式版，系统所有功能均可正常使用，无任何缺失。
8.售后责任：若交付后出现任何代码错误、功能异常、无法运行、无法部署等问题，你（Trae）必须无条件免费逐行修复、完整整改，直至系统100%可用，无需需求方承担任何成本。
八、系统验收总规范（逐条达标才算通过，验收标准）
需求方仅按以下标准验收，所有条款必须100%达标，未达标则视为交付失败，你（Trae）需无条件整改：
（一）功能验收（100%通过，核心验收项）
1.所有菜单、按钮、表单、弹窗、搜索、筛选、导出、批量操作全部可用，无任何失效项、无操作无响应情况。
2.县域词库完整（2800+个，无缺失、无重复），关键词生成正常（一键生成5-10万条，无无效词），AI文案生成正常（每个县域专属文案，无错乱、无丢失、无重复）。
3.多平台账号绑定、OAuth授权、模拟登录全部稳定，登录状态可长期保活，无频繁登录失效情况。
4.一键分发、定时发布、分批发布全流程可正常跑完，发布进度显示准确，成功/失败状态标注清晰，失败可自动重试，发布成功率≥85%。
5.发布记录、数据看板、收录监控全部正常展示，数据准确（与实际发布情况、收录情况一致），收录检测实时有效。
6.全局配置、变量、外链规则一处修改，全系统同步生效，无延迟、无遗漏。
（二）性能与稳定性验收
1.后台切换流畅，大数据量批量操作（如批量生成1000篇文案、批量勾选200个县域）不卡顿、不崩溃、不白屏。
2.系统连续运行72小时无闪退、无进程挂死、无数据错乱、无接口异常，发布队列稳定运行，不阻塞、不重复发布、不漏发。
3.AI生成文案、多平台分发无卡顿，批量生成、批量分发效率满足商用需求（每日可生成1万+篇文案、分发1万+条内容）。
（三）AI效果有效性验收
1.AI生成文章通顺、原创（重复率≤10%）、合规（无极限词、违规词）、符合SEO规则（关键词自然植入，无堆砌），无乱码、无逻辑混乱。
2.发布内容可正常被百度收录，随机抽取10个县域、10个关键词测试，至少8个县域可实现百度手机首页占位（我方官网或矩阵内容）。
3.全流程AI自动化程度≥95%，人工仅需完成3步基础操作，其余全部由AI自动完成，无需人工干预。
（四）交付验收标准
1.交付完整资料：完整前后端源码（无缺失、无加密）+ 数据库SQL脚本（可直接执行）+ 完整依赖配置文件（package.json等）。
2.交付部署相关：一键部署脚本（Shell脚本，双击可执行）、详细部署文档（图文结合，步骤清晰）、运行环境说明（Linux系统版本、依赖软件版本）、默认登录账号密码（超级管理员，可修改）。
3.部署体验：按部署文档操作，部署后直接可登录、直接可使用，无需修改任何代码、无需调试任何配置、无需额外安装软件，开箱即用。
九、最终总承诺（Trae 必须完全遵守，不可违背）
你（Trae）必须严格按照本总文档的所有条款，完整、精准、零删减、零妥协、零Bug开发整套系统，无任何例外：
1.所有设计、架构、代码、AI逻辑、功能、界面、自测、整改，全部由你（Trae）独立完成，无需需求方提供任何帮助、任何素材、任何参考。
2.需求方完全不懂代码、不调试、不改Bug、不提供任何素材，你（Trae）必须承担所有技术责任，确保交付的系统直接可用、稳定商用、真实有效、全自动运行。
3.若未按本文档要求开发，出现功能缺失、Bug、无法运行、无法部署、自动化程度不达标等任何问题，你（Trae）必须无条件免费整改，直至系统100%符合所有要求，且不额外收取任何费用。
4.系统交付后，提供终身免费Bug修复服务（因需求方误操作导致的问题除外），确保系统长期稳定运行，适配建材SEO长期霸屏需求。
备注：本文档为最终定稿，无任何可修改、可协商条款，你（Trae）接收本文档即视为认可所有条款，严格按此执行，交付符合要求的完整系统。

新增多种安全便捷的登录方式，适配企业级后台使用场景，提升登录灵活性与安全性 +qq+微信+邮箱验证=唯一性
综合以上每日数据发到微信上