# 优丁建材 — 上线部署包

本目录从开发仓库导出，**仅含网站源码与部署文件**，已与以下内容分离：

- IDE 本地规则与索引
- 审计报告、测试工程、评估脚本
- `node_modules`、`.venv`、`.nuxt`、构建缓存
- 本地 `.env` 密钥（请从模板新建）

## 目录说明

| 目录 | 用途 |
|------|------|
| `backend/` | Python 主后端 FastAPI |
| `frontend/admin/` | 管理后台 Vite + Vue |
| `frontend/` | 官网 Nuxt |
| `seo-backend/` | SEO 矩阵 Node API |
| `seo-admin/` | SEO 矩阵管理端 |
| `docker/` `deploy/` | 容器与 Nginx 配置 |
| `workers/` | 后台任务 |
| `scripts/` | 上线检查与部署脚本 |

## 部署步骤

1. **环境变量**
   ```powershell
   copy .env.prod.example .env.prod
   notepad .env.prod
   ```
   必填：域名、数据库、`JWT_SECRET_KEY` 等（详见 `DEPLOY.md`）

2. **Docker 一键部署（推荐）**
   ```powershell
   .\deploy.ps1
   ```

3. **或分步调试**
   - 后端：`cd backend` → 创建 venv → `pip install -r requirements.txt` → 迁移 → 启动
   - 管理端：`cd frontend/admin` → `npm ci` → `npm run build`
   - 官网：`cd frontend` → `npm ci` → `npm run build`

4. **生产前检查**
   ```powershell
   python scripts\check_production_secrets.py
   python scripts\check_env.py
   ```

## 与开发仓库的关系

| 开发仓库 | 本目录 |
|----------|--------|
| 含 IDE 规则、Agent、完整 scripts | 仅上线所需 |
| 含 `node_modules` / `.venv` | 需在服务器 `npm ci` / `pip install` |
| 含 `.env` 本地密钥 | 用 `.env.prod.example` 新建 |

## 同步开发修复到本目录

开发仓库修完 BUG 后，在 **website CodeBuddy** 根目录执行：

```powershell
# 全量同步（推荐发布后使用）
powershell -ExecutionPolicy Bypass -File scripts\sync-to-production.ps1

# 仅同步改动的目录（日常修 BUG 更快）
powershell -ExecutionPolicy Bypass -File scripts\sync-to-production.ps1 -Paths "frontend/admin/src/views/system-health"
```

## 重新导出

在开发仓库执行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\export-production-bundle.ps1
```
